---
phase: 02-browser-core
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/main/browserRuntime.ts
  - src/main/main.ts
  - src/preload/index.ts
  - src/renderer/components/BrowserTabStrip.tsx
  - src/renderer/components/NavigationBar.tsx
  - src/renderer/App.tsx
  - src/renderer/styles/global.css
autonomous: true
requirements:
  - BROW-01
  - BROW-02
  - BROW-03
must_haves:
  truths:
    - User can create, close, and activate tabs from a visible top tab strip per D-01 and D-02.
    - User can navigate active tab using omnibox plus back, forward, reload, and stop controls per D-05 and D-06.
    - Active tab state (title, URL, loading, history ability) is reflected in the renderer shell.
  artifacts:
    - path: src/main/browserRuntime.ts
      provides: Main-process tab lifecycle and navigation command handlers.
    - path: src/renderer/components/BrowserTabStrip.tsx
      provides: UI for tab list, active tab highlighting, create/close actions.
    - path: src/renderer/components/NavigationBar.tsx
      provides: Omnibox and navigation controls wired to browser commands.
  key_links:
    - from: src/renderer/components/NavigationBar.tsx
      to: src/renderer/lib/omnibox.ts
      via: Omnibox submit resolves to URL/search target before navigate call.
      pattern: resolveOmniboxInput\(
    - from: src/main/browserRuntime.ts
      to: src/preload/index.ts
      via: IPC handlers and state events supply active tab updates.
      pattern: ipcMain\.handle\(
---

<objective>
Implement browser tab and navigation runtime so core browsing behavior works end-to-end in a single window.

Purpose: Deliver the executable browser shell required by BROW-01, BROW-02, and BROW-03 with decision fidelity to D-01 through D-06.
Output: Main-process tab manager, browser command IPC wiring, and renderer tab plus navigation chrome.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-browser-core/02-CONTEXT.md
@.planning/phases/02-browser-core/02-RESEARCH.md
@.planning/phases/02-browser-core/02-UI-SPEC.md
@.planning/phases/02-browser-core/02-01-PLAN.md
@copilot-instructions.md

<interfaces>
From src/shared/browser.ts (created in 02-01):
- BrowserTabState
- BrowserNavigationRequest
- OmniboxResolution

From src/shared/ipc.ts (extended in 02-01):
- browser tab and navigation channel constants
- PathfinderApi browser methods and event subscription signatures
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build main-process tab runtime and browser command handlers</name>
  <read_first>
- src/main/main.ts
- src/shared/browser.ts
- src/shared/ipc.ts
- src/preload/index.ts
- .planning/phases/02-browser-core/02-RESEARCH.md
  </read_first>
  <files>src/main/browserRuntime.ts, src/main/main.ts, src/preload/index.ts</files>
  <behavior>
- Test 1: New tab creation returns tab id and marks it active.
- Test 2: Closing active tab activates the nearest remaining tab.
- Test 3: Back/forward/reload/stop commands execute on active tab and state events update canGoBack/canGoForward.
  </behavior>
  <action>Create src/main/browserRuntime.ts with BrowserRuntime class managing ordered tab records (Map plus ordered id array) and activeTabId. Use Electron WebContentsView per tab, attaching the active view to the main window content bounds below chrome height. Implement methods createTab(initialUrl), activateTab(tabId), closeTab(tabId), navigate(tabId, target), back(tabId), forward(tabId), reload(tabId), stop(tabId), and getTabSnapshotList(). In src/main/main.ts instantiate BrowserRuntime after BrowserWindow creation, wire ipcMain.handle for each browser channel, and broadcast tab-state updates to renderer via mainWindow.webContents.send('browser:state', payload). Update preload browser event subscription methods if needed so state broadcasts reach renderer callbacks.</action>
  <verify>
    <automated>npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- src/main/browserRuntime.ts contains `class BrowserRuntime` with methods `createTab`, `activateTab`, and `closeTab`.
- src/main/main.ts registers handlers for `IPC_CHANNELS.browserCreateTab` and `IPC_CHANNELS.browserNavigate`.
- src/main/main.ts sends tab updates with `webContents.send('browser:state'`.
- src/main/browserRuntime.ts includes logic that reassigns active tab after close.
  </acceptance_criteria>
  <done>Main process can manage tab lifecycle and execute browser navigation commands for the active tab set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement renderer tab strip and navigation controls per D-01 to D-06</name>
  <read_first>
- src/renderer/App.tsx
- src/renderer/styles/global.css
- src/renderer/lib/omnibox.ts
- src/shared/browser.ts
- .planning/phases/02-browser-core/02-UI-SPEC.md
  </read_first>
  <files>src/renderer/components/BrowserTabStrip.tsx, src/renderer/components/NavigationBar.tsx, src/renderer/App.tsx, src/renderer/styles/global.css</files>
  <behavior>
- Test 1: Tab strip renders all tabs and highlights active tab.
- Test 2: New Tab and Close controls call preload methods and refresh tab list.
- Test 3: Omnibox submit routes through resolver and invokes navigate; back/forward controls disable when unavailable.
  </behavior>
  <action>Create BrowserTabStrip component rendering ordered tabs with buttons for activate and close plus a New Tab button. Create NavigationBar component with back, forward, reload, stop buttons and omnibox input; wire submit through resolveOmniboxInput using search template `https://duckduckgo.com/?q={query}`. Update App.tsx state to load initial tabs via window.pathfinder.listTabs(), subscribe to onBrowserState events, track active tab, and pass command handlers to both components. In global.css add explicit classes for active tab, disabled nav buttons, and omnibox loading state using existing token variables.</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- src/renderer/components/BrowserTabStrip.tsx contains `onCreateTab` and `onCloseTab` callback usage.
- src/renderer/components/NavigationBar.tsx imports `resolveOmniboxInput`.
- src/renderer/components/NavigationBar.tsx contains buttons labeled Back, Forward, Reload, and Stop.
- src/renderer/App.tsx calls `window.pathfinder.listTabs()` and subscribes to `onBrowserState`.
- src/renderer/styles/global.css defines selectors for active tab and disabled navigation button state.
  </acceptance_criteria>
  <done>Renderer browser chrome is fully wired to main runtime for tab and navigation behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer command input to main runtime | User-provided addresses and tab commands cross trust boundary |
| web content to app shell | External web pages load inside tab web contents while app chrome remains privileged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | T | src/main/browserRuntime.ts | mitigate | Validate tab ids and normalize navigation targets before invoking loadURL |
| T-02-06 | E | src/main/main.ts | mitigate | Keep tab command handlers allowlisted and deny unknown channel operations |
| T-02-07 | D | src/main/browserRuntime.ts | mitigate | Enforce deterministic tab close policy and guard null active-tab operations to prevent runtime crashes |
| T-02-08 | I | src/renderer/components/NavigationBar.tsx | accept | Omnibox visibly displays user-entered text; no sensitive data is transmitted beyond intended target navigation |
</threat_model>

<verification>
Run lint, typecheck, and build after runtime plus UI wiring. Confirm tabs can be listed and navigation commands compile through typed API calls.
</verification>

<success_criteria>
1. BROW-01 behavior exists with tab create, close, and switch from visible controls.
2. BROW-02 behavior exists with active-tab back, forward, reload, and stop controls.
3. BROW-03 behavior exists with omnibox URL-or-search routing and stable state synchronization.
</success_criteria>

<output>
After completion, create `.planning/phases/02-browser-core/02-02-SUMMARY.md`
</output>
