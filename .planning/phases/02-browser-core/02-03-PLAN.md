---
phase: 02-browser-core
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/main/downloadManager.ts
  - src/main/sessionStore.ts
  - src/main/browserRuntime.ts
  - src/main/main.ts
  - src/shared/ipc.ts
  - src/preload/index.ts
  - src/renderer/components/DownloadShelf.tsx
  - src/renderer/App.tsx
autonomous: true
requirements:
  - BROW-04
  - BROW-05
must_haves:
  truths:
    - File downloads expose progress and completion/failure states to the user per D-07 and D-08.
    - Unexpected crash/restart restores prior tab list, ordering, and active tab per D-03 and D-09.
  artifacts:
    - path: src/main/downloadManager.ts
      provides: Main-process download lifecycle tracking and renderer event emission.
    - path: src/main/sessionStore.ts
      provides: Session snapshot persistence and restore helpers.
    - path: src/renderer/components/DownloadShelf.tsx
      provides: Visible download progress and terminal state UX.
  key_links:
    - from: src/main/downloadManager.ts
      to: src/renderer/App.tsx
      via: Download state event payloads consumed by renderer shelf state.
      pattern: browser:downloads
    - from: src/main/sessionStore.ts
      to: src/main/browserRuntime.ts
      via: Restored snapshot tab ordering bootstraps runtime state at startup.
      pattern: BrowserSessionSnapshot
---

<objective>
Add download telemetry and crash-session recovery to complete browser-core reliability requirements.

Purpose: Fulfill BROW-04 and BROW-05 so baseline browsing survives crashes and exposes transparent download progress.
Output: Download manager with progress events, session persistence/restore subsystem, and renderer download shelf integration.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-browser-core/02-CONTEXT.md
@.planning/phases/02-browser-core/02-RESEARCH.md
@.planning/phases/02-browser-core/02-VALIDATION.md
@.planning/phases/02-browser-core/02-02-PLAN.md
@copilot-instructions.md

<interfaces>
From src/shared/browser.ts:
- BrowserSessionSnapshot
- DownloadState
- BrowserTabState

From src/shared/ipc.ts:
- download event subscription channels and listDownloads request
- browser state update channels used by renderer sync
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement download lifecycle manager and IPC download stream per D-07 and D-08</name>
  <read_first>
- src/main/main.ts
- src/main/browserRuntime.ts
- src/shared/browser.ts
- src/shared/ipc.ts
- .planning/phases/02-browser-core/02-CONTEXT.md
  </read_first>
  <files>src/main/downloadManager.ts, src/main/main.ts, src/shared/ipc.ts, src/preload/index.ts</files>
  <behavior>
- Test 1: Download start creates tracked item with id, file name, and pending/in-progress state.
- Test 2: Progress updates emit receivedBytes and totalBytes values.
- Test 3: Completion and failure states propagate to renderer subscribers.
  </behavior>
  <action>Create src/main/downloadManager.ts exposing DownloadManager class that registers `session.defaultSession.on('will-download')`, assigns stable ids, stores download state map, and emits updates through callback hooks. For each download item, call setSaveDialogOptions with defaultPath when configured; keep prompt behavior enabled by default and only bypass prompt when explicit default path configuration is set. Emit states `in_progress`, `completed`, `failed`, and `cancelled` with byte counters. Update src/shared/ipc.ts with download list and event channel constants and payload typing. Update src/main/main.ts to instantiate DownloadManager and broadcast updates via `webContents.send('browser:downloads', payload)`. Extend preload methods to expose listDownloads and onDownloadState subscription APIs.</action>
  <verify>
    <automated>npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- src/main/downloadManager.ts contains `class DownloadManager` and `will-download` event registration.
- src/main/downloadManager.ts emits states including `completed` and `failed`.
- src/main/main.ts sends updates using `webContents.send('browser:downloads'`.
- src/preload/index.ts exposes `listDownloads` and `onDownloadState` methods.
  </acceptance_criteria>
  <done>Download flow supports prompt/path behavior and visible lifecycle updates through typed IPC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement crash-session snapshot restore and renderer download shelf</name>
  <read_first>
- src/main/browserRuntime.ts
- src/main/main.ts
- src/renderer/App.tsx
- src/shared/browser.ts
- .planning/phases/02-browser-core/02-CONTEXT.md
  </read_first>
  <files>src/main/sessionStore.ts, src/main/browserRuntime.ts, src/main/main.ts, src/renderer/components/DownloadShelf.tsx, src/renderer/App.tsx</files>
  <behavior>
- Test 1: Runtime snapshot writes ordered tabs and active tab id to disk on state changes and before app quit.
- Test 2: Startup reads valid snapshot and restores tabs plus active tab selection.
- Test 3: Renderer displays download shelf rows with progress percentage and terminal status text.
  </behavior>
  <action>Create src/main/sessionStore.ts with functions loadSessionSnapshot(userDataPath), saveSessionSnapshot(userDataPath, snapshot), and clearInvalidSnapshot(userDataPath) using JSON file `browser-session.json`. Update browser runtime to expose exportSnapshot() and restoreFromSnapshot(snapshot) helpers using ordered tab arrays. In main startup flow, load snapshot before initial tab creation; if snapshot invalid or empty, fallback to one blank tab. Register save calls on key state changes and `before-quit`. Create DownloadShelf component rendering download rows with file name, progress percent, and final status text. Update App.tsx to subscribe to onDownloadState, keep latest map/array state, and render DownloadShelf in browser shell.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- src/main/sessionStore.ts contains `loadSessionSnapshot` and `saveSessionSnapshot`.
- src/main/sessionStore.ts references `browser-session.json`.
- src/main/main.ts contains startup restore path with fallback blank tab logic.
- src/renderer/components/DownloadShelf.tsx renders percentage based on `receivedBytes` and `totalBytes`.
- src/renderer/App.tsx subscribes to `onDownloadState` and renders `DownloadShelf`.
  </acceptance_criteria>
  <done>Crash restore and download visibility requirements are implemented and integrated into browser shell runtime.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external network response to download subsystem | Remote content metadata enters local file save flow |
| persisted session file to startup runtime | On-disk JSON state is loaded into privileged startup logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | T | src/main/sessionStore.ts | mitigate | Validate snapshot schema and tab URL values before restore, fallback on parse/shape failure |
| T-02-10 | D | src/main/downloadManager.ts | mitigate | Track item lifecycle with guarded callbacks to prevent event storms from crashing renderer updates |
| T-02-11 | I | src/renderer/components/DownloadShelf.tsx | accept | Download file names are user-visible by design; no secret fields displayed in shelf |
| T-02-12 | E | src/main/main.ts | mitigate | Keep restore and download operations in main process only; renderer interacts through allowlisted preload API |
</threat_model>

<verification>
Run lint, typecheck, and build after wiring download/session components. Confirm restore and download event paths compile with shared contracts.
</verification>

<success_criteria>
1. BROW-05 is satisfied with prompt/path-aware downloads and visible progress states.
2. BROW-04 is satisfied with startup restoration of prior tab set and active tab after crash.
3. Browser core phase now covers all BROW requirements without deferred placeholders.
</success_criteria>

<output>
After completion, create `.planning/phases/02-browser-core/02-03-SUMMARY.md`
</output>
