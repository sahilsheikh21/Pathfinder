---
phase: 02-browser-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/browser.ts
  - src/shared/ipc.ts
  - src/preload/index.ts
  - src/renderer/global.d.ts
  - src/renderer/lib/omnibox.ts
autonomous: true
requirements:
  - BROW-03
must_haves:
  truths:
    - Browser input is resolved deterministically as URL navigation or search query fallback.
    - Renderer can call browser-core operations only through typed preload methods.
  artifacts:
    - path: src/shared/browser.ts
      provides: Browser tab, navigation, download, and session contract types.
    - path: src/renderer/lib/omnibox.ts
      provides: Pure resolver for omnibox input routing.
    - path: src/preload/index.ts
      provides: Typed browser-core bridge methods on window.pathfinder.
  key_links:
    - from: src/preload/index.ts
      to: src/shared/ipc.ts
      via: Browser core channel constants and payload typing.
      pattern: IPC_CHANNELS\.browser
    - from: src/renderer/lib/omnibox.ts
      to: src/shared/browser.ts
      via: Shared resolver output typing for navigation intent.
      pattern: OmniboxResolution
---

<objective>
Define browser-core contracts and omnibox routing primitives before tab-runtime implementation.

Purpose: Establish stable interfaces first so downstream tasks can implement tab and navigation behavior without contract churn.
Output: Shared type contracts, IPC channel expansion, preload bridge signatures, and deterministic omnibox resolver utility.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-browser-core/02-CONTEXT.md
@.planning/phases/02-browser-core/02-RESEARCH.md
@.planning/phases/02-browser-core/02-UI-SPEC.md
@copilot-instructions.md

<interfaces>
From src/shared/ipc.ts:
```ts
export const IPC_CHANNELS = {
  appGetVersion: 'app:getVersion',
  appGetPlatform: 'app:getPlatform'
} as const
```

From src/preload/index.ts:
```ts
const api: PathfinderApi = {
  getVersion: async () => ipcRenderer.invoke(IPC_CHANNELS.appGetVersion),
  getPlatform: async () => ipcRenderer.invoke(IPC_CHANNELS.appGetPlatform)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create browser-core shared contracts and channel map</name>
  <read_first>
- src/shared/ipc.ts
- src/preload/index.ts
- .planning/phases/02-browser-core/02-CONTEXT.md
- .planning/phases/02-browser-core/02-RESEARCH.md
  </read_first>
  <files>src/shared/browser.ts, src/shared/ipc.ts</files>
  <behavior>
- Test 1: Shared browser-core types compile and are importable from renderer and main process modules.
- Test 2: IPC channel constants include explicit browser tab and navigation operations, not dynamic channel strings.
  </behavior>
  <action>Create src/shared/browser.ts exporting concrete interfaces: BrowserTabState { id, title, url, isActive, isLoading, canGoBack, canGoForward }, BrowserNavigationRequest { tabId, input }, OmniboxResolution { kind: 'url' | 'search', target: string }, BrowserSessionSnapshot { tabs, activeTabId, savedAt }, and DownloadState { id, fileName, state, receivedBytes, totalBytes, savePath }. Update src/shared/ipc.ts by adding a browser channel namespace with constants browser:listTabs, browser:createTab, browser:activateTab, browser:closeTab, browser:navigate, browser:back, browser:forward, browser:reload, browser:stop, browser:getDownloads, browser:onState, and browser:onDownloads. Extend PathfinderApi type with matching Promise-returning methods and event subscription signatures aligned to these channels.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/shared/browser.ts contains `export interface BrowserTabState`.
- src/shared/browser.ts contains `kind: 'url' | 'search'` in OmniboxResolution.
- src/shared/ipc.ts contains `browserListTabs` and `browserNavigate` channel constants.
- src/shared/ipc.ts contains PathfinderApi signatures for tab create, close, activate, and navigate operations.
  </acceptance_criteria>
  <done>Browser-core contracts are defined and available for all downstream implementation tasks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement preload signatures and omnibox resolver utility per D-04</name>
  <read_first>
- src/preload/index.ts
- src/renderer/global.d.ts
- src/shared/browser.ts
- src/shared/ipc.ts
- .planning/phases/02-browser-core/02-CONTEXT.md
  </read_first>
  <files>src/preload/index.ts, src/renderer/global.d.ts, src/renderer/lib/omnibox.ts</files>
  <behavior>
- Test 1: window.pathfinder typing includes browser methods and compile-time checked callback signatures.
- Test 2: Resolver maps full URLs and hostname-like input to URL intent, and other text to search intent.
  </behavior>
  <action>Update src/preload/index.ts so api includes concrete browser methods that call ipcRenderer.invoke with the new browser channel constants and strongly typed payload objects. Add onBrowserState and onDownloadState registration methods that subscribe with ipcRenderer.on and return unsubscribe closures. Update src/renderer/global.d.ts so Window.pathfinder uses the expanded PathfinderApi type. Create src/renderer/lib/omnibox.ts exporting resolveOmniboxInput(input: string, defaultSearchTemplate: string): OmniboxResolution with exact behavior: trim input, if input starts with http:// or https:// return { kind: 'url', target: input }, else if input matches hostname pattern ^[\w.-]+\.[a-z]{2,}([/:?#].*)?$ prepend https:// and return url, else URL-encode input and inject into search template `{query}` token then return { kind: 'search', target }.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/preload/index.ts contains api methods invoking `IPC_CHANNELS.browser` prefixed entries.
- src/preload/index.ts contains unsubscribe-returning listener registration methods.
- src/renderer/global.d.ts references `PathfinderApi` for `window.pathfinder`.
- src/renderer/lib/omnibox.ts exports `resolveOmniboxInput` and uses hostname regex `^[\w.-]+\.[a-z]{2,}([/:?#].*)?$`.
  </acceptance_criteria>
  <done>Renderer and preload are contract-ready for browser runtime implementation, and omnibox routing behavior is deterministic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer to preload | Untrusted renderer input crosses into privileged bridge methods |
| preload to main IPC | Browser command payloads cross process boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | src/renderer/lib/omnibox.ts | mitigate | Normalize and classify input via explicit URL/hostname/search resolver to prevent malformed navigation targets |
| T-02-02 | E | src/preload/index.ts | mitigate | Expose allowlisted browser methods only and return typed unsubscribe closures instead of raw ipcRenderer |
| T-02-03 | S | src/shared/ipc.ts | mitigate | Use fixed browser channel constants and typed payload signatures to prevent spoofed channel usage |
| T-02-04 | R | src/preload/index.ts | accept | Event trace persistence is deferred; runtime behavior remains low-risk in this contract phase |
</threat_model>

<verification>
Run `npm run typecheck` and ensure no renderer or preload typing regressions occur after channel and API expansion.
</verification>

<success_criteria>
1. Shared browser-core types and IPC channels exist with compile-safe imports.
2. Preload and renderer type declarations expose browser APIs with strict signatures.
3. Omnibox resolver behavior satisfies D-04 deterministic routing rules.
</success_criteria>

<output>
After completion, create `.planning/phases/02-browser-core/02-01-SUMMARY.md`
</output>
