---
phase: 15-theming-and-shortcuts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/browser.ts
  - src/shared/ipc.ts
  - src/preload/index.ts
  - src/main/settingsStore.ts
  - src/main/main.ts
autonomous: true
requirements:
  - SET-02
  - SET-05
must_haves:
  truths:
    - Appearance and shortcut preferences are part of one canonical settings snapshot owned by main process.
    - Appearance and shortcut payloads are validated before persistence.
    - Invalid or conflicting shortcut assignments are rejected with typed validation errors.
  artifacts:
    - path: src/shared/browser.ts
      provides: Shared appearance and shortcut settings contracts.
    - path: src/main/settingsStore.ts
      provides: Canonical appearance/shortcut persistence, migration, and validation logic.
    - path: src/shared/ipc.ts
      provides: Typed IPC channels and Pathfinder API methods for appearance/shortcut settings operations.
  key_links:
    - from: src/main/main.ts
      to: src/main/settingsStore.ts
      via: Main process handlers route appearance/shortcut save operations through the canonical store.
      pattern: settingsStore
    - from: src/preload/index.ts
      to: src/shared/ipc.ts
      via: Preload wrappers invoke typed appearance/shortcut channel constants.
      pattern: IPC_CHANNELS
---

<objective>
Extend the canonical settings backend and typed IPC bridge to support appearance personalization and shortcut customization.

Purpose: Provide a secure, validated foundation for SET-02 and SET-05 before renderer UX work begins.
Output: Shared contracts, main-store persistence/validation, and wired main/preload handlers for appearance + shortcut settings.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-theming-and-shortcuts/15-CONTEXT.md
@.planning/phases/15-theming-and-shortcuts/15-RESEARCH.md
@.planning/phases/15-theming-and-shortcuts/15-VALIDATION.md
@.planning/phases/15-theming-and-shortcuts/15-UI-SPEC.md
@src/shared/browser.ts
@src/shared/ipc.ts
@src/preload/index.ts
@src/main/settingsStore.ts
@src/main/main.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared appearance and shortcut settings contracts</name>
  <read_first>
- src/shared/browser.ts
- src/shared/ipc.ts
- .planning/phases/15-theming-and-shortcuts/15-CONTEXT.md
- .planning/phases/15-theming-and-shortcuts/15-RESEARCH.md
  </read_first>
  <files>src/shared/browser.ts, src/shared/ipc.ts</files>
  <behavior>
- Test 1: shared types define appearance settings (theme mode, font-size model, sidebar position preferences) and shortcut binding map.
- Test 2: typed validation error contract includes shortcut conflict/invalid-binding cases.
- Test 3: IPC channels and PathfinderApi expose get/save operations for appearance and shortcuts.
  </behavior>
  <action>Add explicit types in src/shared/browser.ts for appearance and shortcuts: `BrowserThemeMode`, `BrowserFontScalePreset`, `BrowserSidebarPosition`, `BrowserAppearanceSettings`, `BrowserShortcutCommandId`, `BrowserShortcutBinding`, `BrowserShortcutSettings`, save request/result envelopes, and validation error codes including `invalid-binding` and `binding-conflict`. Extend src/shared/ipc.ts with concrete channel constants and PathfinderApi methods for `settings:saveAppearance` and `settings:saveShortcuts` (or equivalent concrete names) while preserving existing `settings:getSnapshot` contract.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/shared/browser.ts contains `type BrowserThemeMode` with values `light`, `dark`, `system`.
- src/shared/browser.ts contains `interface BrowserAppearanceSettings` and `interface BrowserShortcutSettings`.
- src/shared/browser.ts validation error codes include `invalid-binding` and `binding-conflict`.
- src/shared/ipc.ts contains appearance/shortcut save channel constants.
- src/shared/ipc.ts PathfinderApi exposes matching settings save methods.
- `npm run typecheck` exits successfully.
  </acceptance_criteria>
  <done>Shared contracts and API signatures fully define Phase 15 settings scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend canonical settings store with appearance/shortcut validation and migration-safe defaults</name>
  <read_first>
- src/main/settingsStore.ts
- src/shared/browser.ts
- .planning/phases/14-settings-system/14-CONTEXT.md
- .planning/phases/15-theming-and-shortcuts/15-RESEARCH.md
  </read_first>
  <files>src/main/settingsStore.ts</files>
  <behavior>
- Test 1: appearance and shortcut defaults are available in settings snapshots for existing and new users.
- Test 2: invalid appearance values and shortcut conflicts return typed validation errors and do not persist.
- Test 3: existing pre-Phase-15 settings files are read and upgraded without crash.
  </behavior>
  <action>Update src/main/settingsStore.ts to include persisted appearance and shortcut sections in the canonical snapshot. Add deterministic defaults: theme mode `system`, bounded font scale preset, sidebar default position, and default core shortcut bindings matching current hardcoded keys (`Ctrl+K`, `Ctrl+Shift+P`, `Ctrl+Shift+S`). Implement normalization/validation for binding format and duplicate conflicts. Add migration logic that tolerates missing appearance/shortcut fields in existing settings JSON and writes normalized upgraded snapshots.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/main/settingsStore.ts snapshot read path returns appearance and shortcut objects even for legacy files.
- src/main/settingsStore.ts default bindings include current palette and quick-search keys.
- src/main/settingsStore.ts has explicit conflict detection branch for duplicate shortcut bindings.
- invalid binding/conflict cases return typed validation errors from save methods.
- `npm run typecheck` exits successfully.
  </acceptance_criteria>
  <done>Main-process settings store safely persists and validates appearance/shortcut preferences.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire appearance and shortcut settings handlers in main and preload</name>
  <read_first>
- src/main/main.ts
- src/preload/index.ts
- src/shared/ipc.ts
- src/main/settingsStore.ts
  </read_first>
  <files>src/main/main.ts, src/preload/index.ts</files>
  <behavior>
- Test 1: main process registers handlers for appearance and shortcut save operations.
- Test 2: preload exposes wrappers with signatures matching PathfinderApi.
- Test 3: handler failures return typed validation envelopes, not raw thrown errors.
  </behavior>
  <action>In src/main/main.ts register concrete `ipcMain.handle` routes for appearance and shortcut saves through settingsStore methods introduced in Task 2. Ensure handler return values preserve typed success/validation error envelopes. In src/preload/index.ts add concrete wrappers calling the new IPC channel constants so renderer can invoke appearance and shortcut saves via `window.pathfinder`.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/main/main.ts contains handler registrations for appearance and shortcut save channels.
- src/preload/index.ts includes callable wrappers for both new settings methods.
- no handler path returns raw Error objects to renderer.
- `npm run lint` and `npm run typecheck` exit successfully.
  </acceptance_criteria>
  <done>Secure typed backend wiring is complete for appearance and shortcut preferences.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> settings IPC | Untrusted appearance/shortcut payloads cross into privileged persistence and runtime behavior |
| persisted settings -> runtime key handling | On-disk values influence input handling and UI behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | T | src/main/settingsStore.ts | mitigate | Validate and normalize every appearance/shortcut payload; reject invalid values and duplicates |
| T-15-02 | D | src/main/settingsStore.ts | mitigate | Maintain corruption-safe read/repair path and migration-safe fallback defaults |
| T-15-03 | E | src/main/main.ts settings handlers | mitigate | Enforce allowlisted channel routes and typed envelopes only; no direct renderer persistence access |
| T-15-04 | I | src/preload/index.ts | mitigate | Expose minimal, explicit settings methods and avoid leaking internal error details |
</threat_model>

<verification>
Run `npm run typecheck` after Tasks 1-2 and `npm run lint; npm run typecheck` after Task 3.
</verification>

<success_criteria>
1. Canonical settings snapshot includes validated appearance and shortcut preferences.
2. Main/preload typed handlers expose save/get flows without privilege leakage.
3. Conflict and invalid-binding detection is enforceable before renderer UX integration.
</success_criteria>

<output>
After completion, create `.planning/phases/15-theming-and-shortcuts/15-01-SUMMARY.md`
</output>
