---
phase: 15-theming-and-shortcuts
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - src/renderer/lib/shortcutBindings.ts
  - src/renderer/App.tsx
  - src/renderer/components/SettingsPanel.tsx
  - src/renderer/components/BrowserTabStrip.tsx
autonomous: true
requirements:
  - SET-05
must_haves:
  truths:
    - Core shortcut bindings are editable by users and persist via canonical settings.
    - Shortcut conflicts are blocked with explicit validation feedback.
    - Runtime keydown handling uses persisted bindings rather than hardcoded key combinations.
  artifacts:
    - path: src/renderer/lib/shortcutBindings.ts
      provides: Binding normalization, conflict detection, and key-match helpers.
    - path: src/renderer/components/SettingsPanel.tsx
      provides: Shortcut editor UI and conflict/validation states.
    - path: src/renderer/App.tsx
      provides: Runtime dispatch using configurable shortcut map.
  key_links:
    - from: src/renderer/components/SettingsPanel.tsx
      to: src/renderer/lib/shortcutBindings.ts
      via: Editor uses shared parser/validator to pre-check conflicts before save.
      pattern: shortcut
    - from: src/renderer/App.tsx
      to: window.pathfinder.settingsSaveShortcuts
      via: Saved bindings are persisted then used to evaluate keydown events.
      pattern: settingsSaveShortcuts
---

<objective>
Deliver Phase 15 shortcut customization behavior with runtime binding dispatch and conflict-safe editing.

Purpose: Fulfill SET-05 by replacing fixed hotkeys with editable core shortcuts backed by validation and persistence.
Output: Shortcut binding utility module, settings editor UX, and runtime dispatch integration using persisted bindings.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-theming-and-shortcuts/15-CONTEXT.md
@.planning/phases/15-theming-and-shortcuts/15-RESEARCH.md
@.planning/phases/15-theming-and-shortcuts/15-UI-SPEC.md
@.planning/phases/15-theming-and-shortcuts/15-01-SUMMARY.md
@.planning/phases/15-theming-and-shortcuts/15-02-SUMMARY.md
@src/renderer/App.tsx
@src/renderer/components/SettingsPanel.tsx
@src/renderer/lib/commandPalette.ts
@src/renderer/components/BrowserTabStrip.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build shortcut binding normalization and conflict engine</name>
  <read_first>
- src/renderer/App.tsx
- src/renderer/lib/commandPalette.ts
- src/shared/browser.ts
- .planning/phases/15-theming-and-shortcuts/15-RESEARCH.md
  </read_first>
  <files>src/renderer/lib/shortcutBindings.ts, src/renderer/App.tsx</files>
  <behavior>
- Test 1: parser normalizes modifier order/casing for saved bindings.
- Test 2: conflict detector identifies duplicate bindings across editable core command IDs.
- Test 3: runtime keydown matcher resolves configured bindings to concrete actions.
  </behavior>
  <action>Create `src/renderer/lib/shortcutBindings.ts` with concrete utilities for: parsing binding strings, normalizing key tokens (Ctrl/Shift/Alt/Meta order), conflict detection map generation, and `matchesKeyboardEvent` evaluation. Refactor App keydown logic to replace hardcoded checks (`Ctrl+K`, `Ctrl+Shift+P`, `Ctrl+Shift+S`) with binding-driven dispatch sourced from persisted shortcut settings.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- new file `src/renderer/lib/shortcutBindings.ts` exists and exports parser/normalize/conflict/match helpers.
- App.tsx keydown handler no longer hardcodes direct key comparison for core shortcuts.
- App.tsx dispatch maps configured shortcut command IDs to existing actions.
- `npm run typecheck` exits successfully.
  </acceptance_criteria>
  <done>Runtime shortcut execution uses configurable binding logic instead of fixed keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add editable shortcut settings UI with conflict-safe persistence</name>
  <read_first>
- src/renderer/components/SettingsPanel.tsx
- src/renderer/App.tsx
- src/renderer/lib/shortcutBindings.ts
- src/main/settingsStore.ts
  </read_first>
  <files>src/renderer/components/SettingsPanel.tsx, src/renderer/App.tsx, src/renderer/components/BrowserTabStrip.tsx</files>
  <behavior>
- Test 1: Settings panel renders editable bindings for core commands in scope.
- Test 2: local conflict checks and backend validation errors prevent invalid save.
- Test 3: visible shortcut hints update to reflect configured bindings.
  </behavior>
  <action>Extend SettingsPanel with a Shortcuts section listing editable core commands (command palette open, quick search toggle, and other core browser/automation commands defined in shared contracts). Provide input/edit controls, conflict indicators, and reset-to-default action. In App.tsx wire load/save flows through typed shortcut settings APIs and merge server-side validation errors into UI state. Update BrowserTabStrip hint text to derive from current configured command-palette binding rather than static `Ctrl+K` text.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- SettingsPanel contains a shortcuts section with editable bindings for defined core commands.
- save action blocks when conflicts are detected and shows explicit conflict messaging.
- App.tsx applies saved bindings to runtime shortcut state after successful save.
- BrowserTabStrip shortcut hint is derived from configured binding state.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit successfully.
  </acceptance_criteria>
  <done>SET-05 behavior is delivered with editable bindings and deterministic conflict handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shortcut editor input -> runtime key handler | User input can change command dispatch behavior for keyboard events |
| renderer shortcut state -> persisted canonical settings | Potentially invalid bindings cross into durable configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | T | src/renderer/lib/shortcutBindings.ts | mitigate | Normalize and validate bindings before use; reject malformed combinations |
| T-15-10 | D | src/renderer/App.tsx | mitigate | Preserve safe default bindings fallback if persisted config is invalid or unavailable |
| T-15-11 | E | src/renderer/components/SettingsPanel.tsx | mitigate | Limit editable targets to allowlisted command IDs; do not permit arbitrary command execution |
| T-15-12 | R | src/renderer/App.tsx | accept | Shortcut edit audit logging is low-risk for this phase; defer detailed audit to future instrumentation phase |
</threat_model>

<verification>
Run `npm run typecheck` after Task 1 and `npm run lint; npm run typecheck; npm run build` after Task 2.
</verification>

<success_criteria>
1. Users can edit and persist core shortcut bindings.
2. Shortcut conflicts are detected and blocked with explicit feedback.
3. Runtime keyboard dispatch uses configured bindings and maintains safe defaults.
</success_criteria>

<output>
After completion, create `.planning/phases/15-theming-and-shortcuts/15-03-SUMMARY.md`
</output>
