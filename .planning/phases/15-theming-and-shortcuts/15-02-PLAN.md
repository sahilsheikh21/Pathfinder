---
phase: 15-theming-and-shortcuts
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/renderer/components/SettingsPanel.tsx
  - src/renderer/App.tsx
  - src/renderer/theme.ts
  - src/renderer/styles/tokens.css
  - src/renderer/styles/global.css
autonomous: true
requirements:
  - SET-02
must_haves:
  truths:
    - Theme mode and font-size updates apply immediately in renderer and persist through canonical settings.
    - System theme mode responds to OS appearance changes while app is open.
    - Sidebar position preference is configurable and reflected in shell layout behavior.
  artifacts:
    - path: src/renderer/components/SettingsPanel.tsx
      provides: Appearance settings controls and validation feedback.
    - path: src/renderer/App.tsx
      provides: Appearance hydration/apply pipeline and sidebar position runtime application.
    - path: src/renderer/theme.ts
      provides: Theme application utility with system-mode live-sync behavior.
  key_links:
    - from: src/renderer/App.tsx
      to: window.pathfinder.settingsGetSnapshot/settingsSaveAppearance
      via: Renderer loads and persists appearance settings through typed preload APIs.
      pattern: settings
    - from: src/renderer/styles/tokens.css
      to: src/renderer/App.tsx
      via: Root classes/variables are toggled by appearance settings runtime state.
      pattern: theme-
---

<objective>
Implement renderer appearance personalization UX and runtime application for Phase 15.

Purpose: Deliver the SET-02 user-visible behavior contract with immediate apply + persistence semantics.
Output: Appearance controls in Settings, runtime theme/font/sidebar application, and persisted reload behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-theming-and-shortcuts/15-CONTEXT.md
@.planning/phases/15-theming-and-shortcuts/15-RESEARCH.md
@.planning/phases/15-theming-and-shortcuts/15-UI-SPEC.md
@.planning/phases/15-theming-and-shortcuts/15-01-SUMMARY.md
@src/renderer/App.tsx
@src/renderer/components/SettingsPanel.tsx
@src/renderer/theme.ts
@src/renderer/styles/tokens.css
@src/renderer/styles/global.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add appearance controls to settings UI and typed save flow</name>
  <read_first>
- src/renderer/components/SettingsPanel.tsx
- src/renderer/App.tsx
- src/shared/browser.ts
- .planning/phases/15-theming-and-shortcuts/15-CONTEXT.md
  </read_first>
  <files>src/renderer/components/SettingsPanel.tsx, src/renderer/App.tsx</files>
  <behavior>
- Test 1: Settings panel exposes appearance controls for theme mode, font-size preset, and sidebar position.
- Test 2: Save action calls typed appearance save API and surfaces validation feedback.
- Test 3: Loaded snapshot values hydrate control defaults when panel opens.
  </behavior>
  <action>Extend src/renderer/components/SettingsPanel.tsx with an Appearance section that includes concrete controls for theme mode (`light|dark|system`), font-size preset (bounded preset values defined in shared contracts), and sidebar position (left/right). In src/renderer/App.tsx load appearance settings from canonical snapshot, pass appearance draft/state and save callbacks into SettingsPanel, and wire save flow through the new preload method from Plan 01. Route typed validation errors to inline field feedback.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- SettingsPanel renders appearance controls for theme mode, font-size preset, and sidebar position.
- App.tsx invokes the typed appearance save API when appearance settings are saved.
- snapshot hydrate path populates appearance controls with persisted values.
- validation errors for appearance fields render in SettingsPanel.
- `npm run typecheck` exits successfully.
  </acceptance_criteria>
  <done>Appearance preferences are editable in Settings and saved through typed APIs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply appearance settings immediately in runtime shell and styles</name>
  <read_first>
- src/renderer/theme.ts
- src/renderer/styles/tokens.css
- src/renderer/styles/global.css
- src/renderer/App.tsx
- .planning/phases/15-theming-and-shortcuts/15-UI-SPEC.md
  </read_first>
  <files>src/renderer/theme.ts, src/renderer/styles/tokens.css, src/renderer/styles/global.css, src/renderer/App.tsx</files>
  <behavior>
- Test 1: changing theme mode applies immediately and persists after reload.
- Test 2: system mode responds to OS color-scheme changes while app remains open.
- Test 3: font-size and sidebar position preferences update shell layout/typography without restart.
  </behavior>
  <action>Update src/renderer/theme.ts to support runtime system-theme listeners for `system` mode and subtle transition timing hooks (~120-180ms) when classes change. In src/renderer/styles/tokens.css and src/renderer/styles/global.css introduce concrete root variables/classes for font-size scaling and sidebar position docking. In src/renderer/App.tsx apply appearance settings on bootstrap and on save, including root class toggles, font scale variable updates, and sidebar docking class/state updates.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- theme runtime path updates root theme classes when appearance setting changes.
- system mode listener branch exists and reacts to prefers-color-scheme updates.
- CSS files define variables/classes for font-size presets and sidebar position states.
- App shell applies sidebar position preference through deterministic class/state toggles.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit successfully.
  </acceptance_criteria>
  <done>SET-02 runtime behavior is implemented with immediate visual application and persistence.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer settings UI -> preload settings API | User-provided appearance inputs are submitted for privileged persistence |
| persisted appearance values -> renderer DOM classes/styles | Saved values influence runtime class toggles and layout behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | T | src/renderer/components/SettingsPanel.tsx | mitigate | Restrict controls to typed enum values and surface validation errors from main process |
| T-15-06 | D | src/renderer/theme.ts | mitigate | Guard system-mode listeners and fallback safely if media-query APIs are unavailable |
| T-15-07 | I | src/renderer/App.tsx | mitigate | Use typed snapshot fields only and avoid exposing raw internal error payloads in UI |
| T-15-08 | E | src/renderer/styles/global.css | accept | Layout-only class toggles carry low privilege impact; no direct privileged operation triggered |
</threat_model>

<verification>
Run `npm run typecheck` after Task 1 and `npm run lint; npm run typecheck; npm run build` after Task 2.
</verification>

<success_criteria>
1. Appearance controls are available in Settings and persist through canonical settings APIs.
2. Theme mode, font-size, and sidebar position apply immediately at runtime.
3. System mode and subtle transition requirements from context are preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/15-theming-and-shortcuts/15-02-SUMMARY.md`
</output>
