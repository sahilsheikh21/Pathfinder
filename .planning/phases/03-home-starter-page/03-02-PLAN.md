---
phase: 03-home-starter-page
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/renderer/components/HomeStarterPage.tsx
  - src/renderer/App.tsx
  - src/renderer/styles/global.css
autonomous: true
requirements:
  - HOME-01
  - HOME-02
must_haves:
  truths:
    - Home tabs render a branded starter page with compact greeting/date and centered search hero.
    - Home search submits query-only input, uses submit-time preference lookup, and opens results in a newly active tab.
    - Empty or whitespace-only queries never navigate and surface subtle inline hint feedback.
  artifacts:
    - path: src/renderer/components/HomeStarterPage.tsx
      provides: Home starter page section composition and search behavior.
    - path: src/renderer/App.tsx
      provides: Viewport routing between home UI and browser web contents by active tab URL.
    - path: src/renderer/styles/global.css
      provides: Home page layout, spacing, and state styles.
  key_links:
    - from: src/renderer/components/HomeStarterPage.tsx
      to: src/preload/index.ts
      via: Submit-time home preference lookup and search-tab creation.
      pattern: window\.pathfinder\.getHomePreferences
    - from: src/renderer/App.tsx
      to: src/shared/browser.ts
      via: Home route token check for active tab render path.
      pattern: HOME_STARTER_URL
---

<objective>
Implement the renderer home starter page and search flow with strict decision fidelity for D-01 through D-11.

Purpose: Deliver HOME-01 and HOME-02 user-visible behavior using the runtime/contracts from plan 03-01.
Output: Home page component, App viewport routing, search interaction and hint behavior, styling.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-home-starter-page/03-CONTEXT.md
@.planning/phases/03-home-starter-page/03-RESEARCH.md
@.planning/phases/03-home-starter-page/03-VALIDATION.md
@.planning/phases/03-home-starter-page/03-01-PLAN.md
@copilot-instructions.md

<interfaces>
From src/shared/browser.ts (from 03-01):
- HOME_STARTER_URL
- DEFAULT_HOME_SEARCH_TEMPLATE
- HomePreferences

From src/shared/ipc.ts (from 03-01):
- homeGetPreferences
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add HomeStarterPage component and App viewport routing</name>
  <read_first>
- src/renderer/App.tsx
- src/renderer/styles/global.css
- src/shared/browser.ts
- src/renderer/components/NavigationBar.tsx
- .planning/phases/03-home-starter-page/03-CONTEXT.md
  </read_first>
  <files>src/renderer/components/HomeStarterPage.tsx, src/renderer/App.tsx, src/renderer/styles/global.css</files>
  <behavior>
- Test 1: Active tab URL equals `HOME_STARTER_URL` renders home page instead of blank viewport.
- Test 2: Greeting/date section appears in compact header style.
- Test 3: Home layout renders centered search hero with quick-links and recent-automation sections underneath.
  </behavior>
  <action>Create `src/renderer/components/HomeStarterPage.tsx` with props for active tab id, draft query value, draft change callback, search submit callback, quick links, and recent automations. Include sections with concrete classnames: `.home-starter`, `.home-starter__header`, `.home-starter__search`, `.home-starter__quick-links`, `.home-starter__recent-automations`. Update `src/renderer/App.tsx` to compute `isHomeTab = activeTab?.url === HOME_STARTER_URL` and conditionally render `<HomeStarterPage />` inside `.browser-viewport`; keep existing browser chrome visible. Extend `src/renderer/styles/global.css` with responsive layout for centered hero and balanced density (2x3 quick-link grid and 3-slot recent section scaffold).</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/renderer/App.tsx contains `HOME_STARTER_URL` import and `isHomeTab` check.
- src/renderer/App.tsx renders `HomeStarterPage` when `isHomeTab` is true.
- src/renderer/components/HomeStarterPage.tsx contains sections with classnames `home-starter__header`, `home-starter__search`, and `home-starter__quick-links`.
- src/renderer/styles/global.css defines `.home-starter` and `.home-starter__quick-links` selectors.
  </acceptance_criteria>
  <done>Home tab route renders a branded starter page with required section structure and density.
</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement query-only search submission and draft preservation semantics</name>
  <read_first>
- src/renderer/components/HomeStarterPage.tsx
- src/renderer/App.tsx
- src/shared/browser.ts
- src/preload/index.ts
- .planning/phases/03-home-starter-page/03-CONTEXT.md
  </read_first>
  <files>src/renderer/components/HomeStarterPage.tsx, src/renderer/App.tsx</files>
  <behavior>
- Test 1: Enter key and visible Search button submit non-empty query text only.
- Test 2: Submit performs submit-time `getHomePreferences` lookup; fallback to default template on error.
- Test 3: Search opens via `createTab(target)` so the newly created result tab is active.
- Test 4: Empty/whitespace query does not navigate and displays subtle inline hint.
- Test 5: Home draft input remains unchanged after successful submit.
  </behavior>
  <action>In `HomeStarterPage`, implement controlled query input and submit handler that trims input and exits on empty value while setting inline hint text element `.home-starter__hint`. On non-empty submit, call `window.pathfinder.getHomePreferences()` at submit-time, resolve template fallback to `DEFAULT_HOME_SEARCH_TEMPLATE` on any read failure, URL-encode query, build target via `{query}` token replacement, and call `window.pathfinder.createTab(target)`. Keep draft query stored in `App.tsx` as `homeDraftByTabId: Record<string, string>` keyed by tab id; do not clear draft after successful submit. Render visible `<button type="submit">Search</button>` next to input and keep behavior query-only (no URL heuristics).</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- src/renderer/components/HomeStarterPage.tsx contains visible submit button text `Search`.
- src/renderer/components/HomeStarterPage.tsx contains `window.pathfinder.getHomePreferences(` inside submit logic.
- src/renderer/components/HomeStarterPage.tsx uses `encodeURIComponent(` when building search target.
- src/renderer/components/HomeStarterPage.tsx contains `.home-starter__hint` inline feedback element.
- src/renderer/App.tsx contains `homeDraftByTabId` state and does not clear draft on submit success.
  </acceptance_criteria>
  <done>HOME-02 search semantics are implemented exactly as decided, including query-only submission and draft persistence.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input to search submit handler | Untrusted query text becomes navigation target |
| renderer search action to main runtime | Target URL crosses IPC into privileged tab creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | T | src/renderer/components/HomeStarterPage.tsx | mitigate | Treat input as query-only and URL-encode before template injection |
| T-03-06 | I | src/renderer/components/HomeStarterPage.tsx | accept | Query text is intentionally user-visible in input and destination URL |
| T-03-07 | E | src/renderer/App.tsx | mitigate | Use typed preload API methods only; no direct Electron APIs in renderer |
| T-03-08 | D | src/renderer/components/HomeStarterPage.tsx | mitigate | Guard empty submits and inline hint path to avoid repeated invalid navigation calls |
</threat_model>

<verification>
Run `npm run typecheck` after introducing component wiring, then run full `npm run lint; npm run typecheck; npm run build` after search-flow completion.
</verification>

<success_criteria>
1. HOME-01 is satisfied with branded home tab render including compact greeting/date and section layout.
2. HOME-02 is satisfied with query-only submit flow, submit-time template lookup, empty-query guard, and active new-tab navigation.
3. Home search draft value remains stable after submit and across tab switching.
</success_criteria>

<output>
After completion, create `.planning/phases/03-home-starter-page/03-02-SUMMARY.md`
</output>
