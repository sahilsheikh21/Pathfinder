---
phase: 03-home-starter-page
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/renderer/components/HomeStarterPage.tsx
  - src/renderer/App.tsx
  - src/renderer/styles/global.css
  - src/main/homeStore.ts
  - src/main/main.ts
  - src/preload/index.ts
autonomous: true
requirements:
  - HOME-03
must_haves:
  truths:
    - Quick links render from local data with curated fallback and allow add/remove/pin editing in-home.
    - Quick-link click opens destination in the current active tab.
    - Recent automations section shows explicit non-interactive empty state with 3 reserved slots for future recency.
  artifacts:
    - path: src/renderer/components/HomeStarterPage.tsx
      provides: Quick-link editing controls, link-card actions, and recent-automation empty-state rendering.
    - path: src/main/homeStore.ts
      provides: Quick-link persistence and update semantics.
    - path: src/main/main.ts
      provides: IPC-backed mutation handlers for quick-link editing.
  key_links:
    - from: src/renderer/components/HomeStarterPage.tsx
      to: src/main/homeStore.ts
      via: upsert/remove quick-link flows over typed IPC.
      pattern: window\.pathfinder\.(upsertQuickLink|removeQuickLink)
    - from: src/renderer/components/HomeStarterPage.tsx
      to: src/renderer/App.tsx
      via: link click delegates to active-tab navigate handler.
      pattern: onNavigate
---

<objective>
Complete HOME-03 by implementing quick-link data operations and recent-automation section behavior.

Purpose: Deliver direct-launch home actions from local data with safe editing and explicit empty-state semantics.
Output: Quick-link CRUD/pin UX, active-tab launch behavior, and recent-automation empty-state contract.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-home-starter-page/03-CONTEXT.md
@.planning/phases/03-home-starter-page/03-RESEARCH.md
@.planning/phases/03-home-starter-page/03-VALIDATION.md
@.planning/phases/03-home-starter-page/03-02-PLAN.md
@copilot-instructions.md

<interfaces>
From src/shared/browser.ts:
- QuickLink
- RecentAutomationPreview

From src/shared/ipc.ts:
- homeListQuickLinks
- homeUpsertQuickLink
- homeRemoveQuickLink
- homeListRecentAutomations
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement quick-link launch and inline add/remove/pin editing</name>
  <read_first>
- src/renderer/components/HomeStarterPage.tsx
- src/renderer/App.tsx
- src/main/homeStore.ts
- src/main/main.ts
- src/preload/index.ts
- .planning/phases/03-home-starter-page/03-CONTEXT.md
  </read_first>
  <files>src/renderer/components/HomeStarterPage.tsx, src/renderer/App.tsx, src/main/homeStore.ts, src/main/main.ts, src/preload/index.ts</files>
  <behavior>
- Test 1: Home quick-links load from IPC-backed local store and display max 6 cards in order.
- Test 2: Add/remove/pin actions persist through IPC and re-render without page reload.
- Test 3: Clicking a quick link navigates the current active tab (no new tab creation).
  </behavior>
  <action>In HomeStarterPage, fetch quick links with `window.pathfinder.listQuickLinks()` on mount/home-tab activation and render exactly 6 cards (pad with curated fallback via store if needed). Add inline controls: Add Link form (title + url), Pin/Unpin toggle, and Remove button. For add/update, call `window.pathfinder.upsertQuickLink({ id?, title, url, pinned, order })`; for remove call `window.pathfinder.removeQuickLink(id)`. In App.tsx pass `onNavigate` callback so card click uses current-tab navigation path (`window.pathfinder.navigate({ tabId: activeTabId, input: url })`). Update homeStore ordering rules so pinned links sort first and then by `order`; keep stable ids for edits.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/renderer/components/HomeStarterPage.tsx contains calls to `listQuickLinks`, `upsertQuickLink`, and `removeQuickLink`.
- src/renderer/components/HomeStarterPage.tsx contains controls labeled `Add Link`, `Pin`, and `Remove`.
- src/renderer/App.tsx passes an `onNavigate` callback into `HomeStarterPage` that uses `window.pathfinder.navigate` with active tab id.
- src/main/homeStore.ts sorts quick links with pinned-first behavior.
- src/main/main.ts registers handlers for quick-link mutation channels.
  </acceptance_criteria>
  <done>Quick links are fully local-data-driven, editable in-place, and launch in the current tab as required.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add recent-automation empty-state section with future recency contract</name>
  <read_first>
- src/renderer/components/HomeStarterPage.tsx
- src/renderer/styles/global.css
- src/main/homeStore.ts
- src/main/main.ts
- .planning/phases/03-home-starter-page/03-CONTEXT.md
  </read_first>
  <files>src/renderer/components/HomeStarterPage.tsx, src/renderer/styles/global.css, src/main/homeStore.ts, src/main/main.ts, src/preload/index.ts</files>
  <behavior>
- Test 1: Recent automations section always renders with explicit empty text when list is empty.
- Test 2: Section reserves 3 visual slots and keeps cards non-interactive in this phase.
- Test 3: Data contract documents future recency basis as most-recently executed automation.
  </behavior>
  <action>Extend homeStore `listRecentAutomations()` to return a typed empty array for now while preserving schema for future execution history integration (`id`, `name`, `lastRunAt`, `status`). In HomeStarterPage, call `window.pathfinder.listRecentAutomations()` and render section title + explicit message text `No recent automations yet.` when array is empty. Render exactly 3 reserved slot placeholders (`.home-starter__recent-slot`) marked with `aria-disabled="true"` and no click handlers. Add comments near data mapper noting future source is most-recently executed entries. Update CSS styles for non-interactive placeholder slots and clear visual separation from quick links.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- src/renderer/components/HomeStarterPage.tsx contains message text `No recent automations yet.`.
- src/renderer/components/HomeStarterPage.tsx renders exactly three placeholder slot elements with class `home-starter__recent-slot`.
- src/renderer/components/HomeStarterPage.tsx applies `aria-disabled="true"` on recent placeholders and defines no click handlers for them.
- src/main/homeStore.ts contains `listRecentAutomations` returning typed records (currently empty array).
- src/renderer/styles/global.css defines `.home-starter__recent-slot` styling.
  </acceptance_criteria>
  <done>Recent automations area is explicit, non-interactive, and future-compatible without violating current phase scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-managed quick-link edits to persistence | Untrusted title/url values are stored locally and later used for navigation |
| home UI to tab navigation runtime | Quick-link click targets cross renderer-to-main boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | T | src/main/homeStore.ts | mitigate | Enforce strict URL validation and normalize stored quick-link payloads |
| T-03-10 | E | src/renderer/App.tsx | mitigate | Route quick-link clicks through existing typed `navigate` API with active-tab checks |
| T-03-11 | D | src/renderer/components/HomeStarterPage.tsx | mitigate | Keep recent-automation placeholders non-interactive to avoid invalid command paths |
| T-03-12 | R | src/main/main.ts | accept | Mutation audit log persistence is deferred to later automation/history phases |
</threat_model>

<verification>
Run `npm run typecheck` after quick-link CRUD wiring, then run full `npm run lint; npm run typecheck; npm run build` after recent-automation section completion.
</verification>

<success_criteria>
1. HOME-03 quick links render from local data, support inline editing, and launch in current tab.
2. Recent automations section renders explicit empty-state messaging with 3 reserved non-interactive slots.
3. HOME-03 behavior remains local-first and aligned with later automation-history integration.
</success_criteria>

<output>
After completion, create `.planning/phases/03-home-starter-page/03-03-SUMMARY.md`
</output>
