---
phase: 03-home-starter-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/browser.ts
  - src/shared/ipc.ts
  - src/main/browserRuntime.ts
  - src/main/main.ts
  - src/main/homeStore.ts
  - src/preload/index.ts
  - src/renderer/global.d.ts
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
must_haves:
  truths:
    - New tabs resolve to a deterministic internal home token instead of blank placeholder URLs.
    - Home preferences and quick links are read/written only through typed preload IPC methods.
    - Home preference reads always provide a search-template fallback when persisted state is missing or corrupt.
  artifacts:
    - path: src/main/homeStore.ts
      provides: Persistent home preferences and quick-link storage with fallback defaults.
    - path: src/shared/ipc.ts
      provides: Home IPC channel constants and typed Pathfinder API signatures.
    - path: src/main/browserRuntime.ts
      provides: Default new-tab URL routing to the internal home token.
  key_links:
    - from: src/preload/index.ts
      to: src/shared/ipc.ts
      via: Allowlisted home channel invocations and event-safe bridge.
      pattern: IPC_CHANNELS\.home
    - from: src/main/main.ts
      to: src/main/homeStore.ts
      via: IPC handlers for preference and quick-link persistence.
      pattern: homeStore
---

<objective>
Create the home-runtime foundation and typed contracts needed before renderer home UI work.

Purpose: Ensure HOME-01/HOME-02/HOME-03 build on stable route, storage, and IPC interfaces.
Output: Internal home route token wiring, local home store, typed home IPC bridge.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-home-starter-page/03-CONTEXT.md
@.planning/phases/03-home-starter-page/03-RESEARCH.md
@.planning/phases/03-home-starter-page/03-VALIDATION.md
@copilot-instructions.md

<interfaces>
From src/shared/browser.ts:
- BrowserTabState
- BrowserNavigationRequest
- BrowserSessionSnapshot

From src/shared/ipc.ts:
- IPC_CHANNELS
- PathfinderApi
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define home route/constants and typed home IPC contracts</name>
  <read_first>
- src/shared/browser.ts
- src/shared/ipc.ts
- src/preload/index.ts
- src/renderer/global.d.ts
- src/main/browserRuntime.ts
- .planning/phases/03-home-starter-page/03-CONTEXT.md
  </read_first>
  <files>src/shared/browser.ts, src/shared/ipc.ts, src/preload/index.ts, src/renderer/global.d.ts, src/main/browserRuntime.ts</files>
  <behavior>
- Test 1: Shared contracts export a stable home URL token and typed home payloads.
- Test 2: Renderer compile-time API includes home preferences and quick-link methods without any direct Electron access.
- Test 3: Browser runtime defaults new tabs to internal home token `about:pathfinder-home`.
  </behavior>
  <action>Update src/shared/browser.ts to export exact constants and interfaces: `HOME_STARTER_URL = 'about:pathfinder-home'`, `DEFAULT_HOME_SEARCH_TEMPLATE = 'https://duckduckgo.com/?q={query}'`, `interface HomePreferences { searchTemplate: string }`, `interface QuickLink { id: string; title: string; url: string; pinned: boolean; order: number }`, and `interface RecentAutomationPreview { id: string; name: string; lastRunAt: string | null; status: 'never-run' | 'success' | 'failed' }`. Extend src/shared/ipc.ts with channel constants `home:getPreferences`, `home:savePreferences`, `home:listQuickLinks`, `home:upsertQuickLink`, `home:removeQuickLink`, and `home:listRecentAutomations`; extend PathfinderApi with matching Promise-returning methods. Update src/preload/index.ts and src/renderer/global.d.ts so `window.pathfinder` exposes these methods. Update src/main/browserRuntime.ts so `createTab()` without initial URL resolves to `HOME_STARTER_URL` rather than `about:blank`.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/shared/browser.ts contains `export const HOME_STARTER_URL = 'about:pathfinder-home'`.
- src/shared/browser.ts contains `export const DEFAULT_HOME_SEARCH_TEMPLATE = 'https://duckduckgo.com/?q={query}'`.
- src/shared/ipc.ts contains channel constants `homeGetPreferences`, `homeSavePreferences`, and `homeListQuickLinks`.
- src/preload/index.ts contains home API methods invoking `IPC_CHANNELS.home` prefixed entries.
- src/main/browserRuntime.ts default tab creation path references `HOME_STARTER_URL`.
  </acceptance_criteria>
  <done>Contracts and runtime token routing are ready for home-page UI and persistence implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement main-process home store and home IPC handlers</name>
  <read_first>
- src/main/main.ts
- src/main/sessionStore.ts
- src/shared/browser.ts
- src/shared/ipc.ts
- src/preload/index.ts
- .planning/phases/03-home-starter-page/03-RESEARCH.md
  </read_first>
  <files>src/main/homeStore.ts, src/main/main.ts, src/preload/index.ts</files>
  <behavior>
- Test 1: Loading home preferences always returns valid `searchTemplate` with fallback default.
- Test 2: Quick-link list returns deterministic sorted output by `order` then `title`.
- Test 3: Invalid quick-link URLs are rejected before persistence.
  </behavior>
  <action>Create src/main/homeStore.ts with JSON-backed persistence file `home-starter.json` under `app.getPath('userData')`. Implement functions `getHomePreferences()`, `saveHomePreferences(input)`, `listQuickLinks()`, `upsertQuickLink(input)`, `removeQuickLink(id)`, and `listRecentAutomations()` (returns empty array for this phase). Seed curated 6 quick-link defaults when file missing. Validate quick-link URLs strictly to `http://` or `https://`; reject any other scheme. In src/main/main.ts register IPC handlers for each new home channel and wire to homeStore functions. Keep store ownership in main process only; preload remains a thin bridge.</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- src/main/homeStore.ts references `home-starter.json`.
- src/main/homeStore.ts contains `getHomePreferences` returning `DEFAULT_HOME_SEARCH_TEMPLATE` fallback.
- src/main/homeStore.ts contains URL-scheme validation rejecting non-http/https links.
- src/main/main.ts registers handlers for `IPC_CHANNELS.homeGetPreferences` and `IPC_CHANNELS.homeUpsertQuickLink`.
- src/main/main.ts registers handler for `IPC_CHANNELS.homeListRecentAutomations` returning store data.
  </acceptance_criteria>
  <done>Main process provides a secure, deterministic data source for home preferences and quick links.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer to preload | Untrusted renderer inputs cross into privileged APIs |
| preload to main IPC | Home preference and link payloads cross process boundary |
| filesystem JSON to runtime | Persisted home data is loaded into privileged main process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | E | src/preload/index.ts | mitigate | Expose only allowlisted home methods and typed payload contracts; no raw ipcRenderer leakage |
| T-03-02 | T | src/main/homeStore.ts | mitigate | Validate quick-link URL schemes and fallback invalid persisted values to safe defaults |
| T-03-03 | D | src/main/homeStore.ts | mitigate | Guard JSON parse failures and return deterministic fallback structures |
| T-03-04 | R | src/main/main.ts | accept | Full audit logging for home updates is deferred; low-risk local-only preference writes |
</threat_model>

<verification>
Run `npm run typecheck` after contract/routing changes, then `npm run lint; npm run typecheck; npm run build` after home store + handler wiring.
</verification>

<success_criteria>
1. Internal home route token and typed home contracts compile across shared, preload, main, and renderer typing surfaces.
2. Home preference and quick-link operations are available through IPC with fallback-safe local persistence.
3. Default new tabs resolve to the home token route prerequisite for renderer home page rendering.
</success_criteria>

<output>
After completion, create `.planning/phases/03-home-starter-page/03-01-SUMMARY.md`
</output>
