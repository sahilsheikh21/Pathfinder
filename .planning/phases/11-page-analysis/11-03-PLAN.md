---
phase: 11-page-analysis
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - src/renderer/App.tsx
  - src/renderer/lib/commandPalette.ts
  - src/renderer/components/AutomationSidebar.tsx
  - src/renderer/styles/global.css
autonomous: true
requirements:
  - AI-02
must_haves:
  truths:
    - Users can trigger summarize and ask flows from command palette and AI sidebar.
    - Results are grounded with inline markers and snippet cards including source metadata.
    - UI provides cancellable progress, staleness warning, refresh, and clear-context controls.
  artifacts:
    - path: src/renderer/App.tsx
      provides: App-level page-analysis state, actions, and result rendering logic.
    - path: src/renderer/lib/commandPalette.ts
      provides: Command ids for summarize/ask/refresh/clear analysis actions.
    - path: src/renderer/components/AutomationSidebar.tsx
      provides: AI section wiring for page-analysis panel content.
    - path: src/renderer/styles/global.css
      provides: Styling for citations, confidence badges, and analysis controls.
  key_links:
    - from: src/renderer/App.tsx
      to: src/preload/index.ts
      via: Typed page-analysis API calls through window.pathfinder wrappers.
      pattern: pageAnalysis
    - from: src/renderer/lib/commandPalette.ts
      to: src/renderer/App.tsx
      via: Command handlers focus AI section and invoke summarize/ask/refresh/clear actions.
      pattern: ai.analysis
---

<objective>
Implement the phase-11 renderer experience for command-first and sidebar-driven page analysis with grounded response presentation.

Purpose: Deliver the user-visible AI-02 capability using existing command/sidebar architecture and typed IPC foundations.
Output: End-to-end summarize and ask UX with citations, failures, cancellation, refresh, and context controls.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-page-analysis/11-CONTEXT.md
@.planning/phases/11-page-analysis/11-RESEARCH.md
@.planning/phases/11-page-analysis/11-VALIDATION.md
@.planning/phases/11-page-analysis/11-01-PLAN.md
@.planning/phases/11-page-analysis/11-02-PLAN.md
@src/renderer/App.tsx
@src/renderer/lib/commandPalette.ts
@src/renderer/components/AutomationSidebar.tsx
@src/renderer/styles/global.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build AI sidebar page-analysis interaction and result rendering</name>
  <read_first>
- src/renderer/App.tsx
- src/renderer/components/AutomationSidebar.tsx
- src/renderer/styles/global.css
- .planning/phases/11-page-analysis/11-CONTEXT.md
  </read_first>
  <files>src/renderer/App.tsx, src/renderer/components/AutomationSidebar.tsx, src/renderer/styles/global.css</files>
  <behavior>
- Test 1: AI panel supports summarize and ask actions with concise/detailed toggle and confidence display.
- Test 2: answers render inline citation markers and snippet cards with title/url/snippet index/timestamp.
- Test 3: failure states show actionable guidance with retry/cancel options.
  </behavior>
  <action>In App.tsx, add page-analysis UI state for request mode, current question, in-flight status, cancellable operation id, snapshot metadata, stale flag, confidence label, and result sections. Render AI panel content through AutomationSidebar ai-content slot with controls for Summarize, Ask, verbosity toggle, cancel, retry, refresh context, and clear context. Add citation card list rendering under each answer with metadata fields from typed payload. Add global.css styles for analysis controls, result sections, citation markers/cards, confidence badge, stale warning block, and actionable error panel.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- App.tsx contains summarize and ask action handlers calling page-analysis API methods.
- AI panel renders confidence indicator, stale warning, and citation cards with source metadata.
- UI includes cancel, retry, refresh-context, and clear-context controls.
- lint and typecheck pass.
  </acceptance_criteria>
  <done>Sidebar AI section provides complete interactive page-analysis UX with grounded response display.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add command palette page-analysis commands and focus behavior</name>
  <read_first>
- src/renderer/lib/commandPalette.ts
- src/renderer/App.tsx
- .planning/phases/04-command-palette/04-CONTEXT.md
- .planning/phases/11-page-analysis/11-CONTEXT.md
  </read_first>
  <files>src/renderer/lib/commandPalette.ts, src/renderer/App.tsx</files>
  <behavior>
- Test 1: command palette includes summarize and ask commands that target active tab at execution time.
- Test 2: command execution auto-opens and focuses AI sidebar section.
- Test 3: refresh and clear-context commands are available for analysis flows.
  </behavior>
  <action>Extend commandPalette registry with new ids and titles: ai.analysis.summarize, ai.analysis.ask, ai.analysis.refresh, and ai.analysis.clear. Wire dependencies from App.tsx so command execution always focuses ai-chat sidebar section and then runs requested analysis action. For ask command, accept optional inline argument and route empty-query fallback to panel prompt flow. Ensure command error handling reuses existing palette error surface and does not close panel on actionable failures.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- commandPalette file contains all four ai.analysis command ids.
- App command dependency wiring opens ai-chat section before invoking analysis action.
- Ask command supports optional argument and panel fallback for missing question.
- lint and typecheck pass.
  </acceptance_criteria>
  <done>Command-first page-analysis entry points match locked phase interaction decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire staleness TTL and per-tab context invalidation UX</name>
  <read_first>
- src/renderer/App.tsx
- src/shared/browser.ts
- .planning/phases/11-page-analysis/11-CONTEXT.md
  </read_first>
  <files>src/renderer/App.tsx, src/renderer/styles/global.css</files>
  <behavior>
- Test 1: context resets on URL change/hard reload reflected from runtime state updates.
- Test 2: short TTL staleness warning appears and offers one-click re-extract.
- Test 3: clear-context action removes per-tab session context without affecting other sidebar sections.
  </behavior>
  <action>Implement per-tab analysis context cache in App.tsx keyed by tab id and snapshot metadata, with invalidation on active-tab URL transitions. Apply short TTL logic (about 1-2 minutes) to mark same-URL context stale and show warning banner with re-extract action. Wire clear-context command/button to remove tab-specific analysis memory and reset displayed answer state. Keep side effects scoped to AI analysis state only so automation library/history behavior remains unchanged.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- App state includes per-tab analysis context cache and invalidation on URL change.
- stale warning UI appears after TTL and includes refresh action.
- clear-context action resets only analysis context for current tab.
- lint, typecheck, and build pass.
  </acceptance_criteria>
  <done>Staleness and context lifecycle behavior is implemented and user-controllable per phase decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer state to user display | Untrusted extracted/page text is rendered into UI and must remain safely escaped |
| command invocation to analysis action | Fast command execution can trigger unintended operations without clear context focus |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | I | src/renderer/App.tsx | mitigate | Render extracted snippets as plain text and avoid unsafe HTML injection paths |
| T-11-12 | S | src/renderer/lib/commandPalette.ts | mitigate | Require explicit command ids and deterministic ai-chat focus before action execution |
| T-11-13 | T | src/renderer/App.tsx | mitigate | Constrain clear/refresh actions to current-tab analysis context only |
| T-11-14 | D | src/renderer/App.tsx | mitigate | Provide cancel controls and stale warning refresh path to avoid repeated long-running calls |
| T-11-15 | R | src/renderer/App.tsx | mitigate | Surface actionable error and retry guidance with stable status metadata |
</threat_model>

<verification>
Run npm run lint; npm run typecheck after Tasks 1-2 and full lint/typecheck/build after Task 3.
</verification>

<success_criteria>
1. Users can run summary and ask flows from command and sidebar entry points on the active page.
2. Answers are grounded with citation markers/snippets and transparent confidence/staleness context.
3. Cancellation, refresh, and clear-context controls are functional and bounded to current-tab analysis state.
</success_criteria>

<output>
After completion, create .planning/phases/11-page-analysis/11-03-SUMMARY.md
</output>
