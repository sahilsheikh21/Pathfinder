---
phase: 11-page-analysis
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/preload/index.ts
  - src/main/main.ts
  - src/main/browserRuntime.ts
autonomous: true
requirements:
  - AI-02
must_haves:
  truths:
    - Main process exposes page-analysis handlers for summarize, ask, cancel, refresh, and clear-context flows.
    - Renderer bridge has typed preload methods for all page-analysis operations.
    - URL-change lifecycle events can invalidate stale per-tab analysis context.
  artifacts:
    - path: src/main/main.ts
      provides: IPC handler wiring and page-analysis manager initialization.
    - path: src/preload/index.ts
      provides: Renderer-safe wrappers for page-analysis operations.
    - path: src/main/browserRuntime.ts
      provides: Lifecycle hook surface for tab URL change and reset events.
  key_links:
    - from: src/preload/index.ts
      to: src/main/main.ts
      via: invoke wrappers call page-analysis IPC channels.
      pattern: pageAnalysis
    - from: src/main/main.ts
      to: src/main/browserRuntime.ts
      via: tab lifecycle events invalidate or refresh context snapshots.
      pattern: onTab
---

<objective>
Wire the phase-11 service into main-process IPC and preload bridges, including cancellation and context invalidation lifecycle behavior.

Purpose: Enable reliable process-boundary execution for page analysis before renderer UX integration.
Output: Production-ready handlers and preload API methods for all phase-11 operations.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-page-analysis/11-CONTEXT.md
@.planning/phases/11-page-analysis/11-RESEARCH.md
@.planning/phases/11-page-analysis/11-VALIDATION.md
@.planning/phases/11-page-analysis/11-01-PLAN.md
@src/main/main.ts
@src/preload/index.ts
@src/main/browserRuntime.ts
@src/main/llm/pageAnalysisService.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register main-process page-analysis IPC handlers</name>
  <read_first>
- src/main/main.ts
- src/main/llm/pageAnalysisService.ts
- src/shared/ipc.ts
- .planning/phases/11-page-analysis/11-CONTEXT.md
  </read_first>
  <files>src/main/main.ts</files>
  <behavior>
- Test 1: main process registers handlers for summarize, ask, cancel, refresh, clear-context, and status operations.
- Test 2: handlers map service results to typed success/failure envelopes with actionable guidance.
- Test 3: in-flight cancellation and retry paths are deterministic and do not silently auto-fallback providers.
  </behavior>
  <action>In src/main/main.ts, initialize the pageAnalysisService after llmAdapterService startup and register ipcMain handlers for all new page-analysis channels. Implement summarize and ask handlers that resolve active tab at execution time, invoke service extraction+generation, and return typed result payloads including citations and snapshot metadata. Add handlers for cancel, refreshContext, clearContext, and status retrieval. Keep provider error mapping aligned with existing llm adapter fail-fast behavior and include retry-oriented userAction values in mapped failures.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- main.ts contains ipcMain.handle registrations for every page-analysis channel defined in shared IPC.
- summarize and ask handlers call pageAnalysisService rather than direct llmGenerate shortcuts.
- cancel and clear-context operations update in-flight/context state deterministically.
- npm run typecheck exits successfully.
  </acceptance_criteria>
  <done>Main process exposes complete typed runtime operations for phase-11 analysis flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend preload bridge for page-analysis APIs</name>
  <read_first>
- src/preload/index.ts
- src/shared/ipc.ts
- src/shared/browser.ts
  </read_first>
  <files>src/preload/index.ts</files>
  <behavior>
- Test 1: preload exposes typed wrappers for summarize, ask, cancel, refresh, clear-context, and status.
- Test 2: wrappers keep existing API surface backwards compatible.
- Test 3: renderer remains isolated from Electron primitives.
  </behavior>
  <action>In src/preload/index.ts, add invoke wrappers for all page-analysis operations introduced in shared IPC. Extend the exposed Pathfinder API object with typed methods for summarize/ask requests, cancellation, context refresh, context clear, and status checks. Preserve existing automation/llm/home/browser methods exactly as-is, and do not expose raw ipcRenderer APIs to renderer.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- preload API includes callable methods for all new page-analysis operations.
- Existing Pathfinder API methods remain present and unchanged in signature.
- No direct ipcRenderer object exposure is added to window scope.
- npm run typecheck passes.
  </acceptance_criteria>
  <done>Renderer has secure typed bridge access for all page-analysis workflows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add tab lifecycle invalidation hooks</name>
  <read_first>
- src/main/browserRuntime.ts
- src/main/main.ts
- .planning/phases/11-page-analysis/11-CONTEXT.md
  </read_first>
  <files>src/main/browserRuntime.ts, src/main/main.ts</files>
  <behavior>
- Test 1: URL change/hard reload events can invalidate per-tab analysis context.
- Test 2: context invalidation does not break existing recorder/playback tab lifecycle behavior.
- Test 3: stale metadata can be surfaced to renderer status checks.
  </behavior>
  <action>Extend browser runtime lifecycle signaling so main process can detect URL navigation/reload boundaries relevant to analysis context invalidation. Wire this signal in main.ts to notify pageAnalysisService when tab context must reset (URL change/hard reload). Ensure existing recorder/playback target-loss hooks continue unchanged and both systems can coexist. Provide status metadata updates used by stale-warning and refresh flows.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- browserRuntime emits or exposes lifecycle signal used for analysis-context reset.
- main.ts subscribes to the lifecycle signal and invokes service context invalidation.
- recorder and playback lifecycle hooks still compile and run through existing paths.
- lint, typecheck, and build commands all pass.
  </acceptance_criteria>
  <done>Tab lifecycle and analysis context rules are wired for stale-safe execution behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer to preload to main | Untrusted command/UI input traverses privileged analysis handlers |
| browser runtime lifecycle to analysis state | Incorrect invalidation can cause stale or cross-tab data leakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | S | src/main/main.ts | mitigate | Restrict operations to explicit handlers and active-tab resolution checks |
| T-11-07 | T | src/preload/index.ts | mitigate | Expose typed wrappers only and avoid privileged raw API exposure |
| T-11-08 | I | src/main/main.ts | mitigate | Enforce per-tab context clear/invalidation on URL/reload lifecycle events |
| T-11-09 | D | src/main/main.ts | mitigate | Support deterministic cancellation and bounded in-flight operation tracking |
| T-11-10 | R | src/main/main.ts | mitigate | Return typed status/failure metadata for retry and stale warnings |
</threat_model>

<verification>
Run npm run typecheck after Tasks 1-2, then npm run lint; npm run typecheck; npm run build after Task 3.
</verification>

<success_criteria>
1. All page-analysis operations are invokable through typed main/preload boundaries.
2. Invalidation, cancellation, and retry-support behavior are wired at runtime level.
3. Existing browser automation flows remain intact with no regression to recorder/playback hooks.
</success_criteria>

<output>
After completion, create .planning/phases/11-page-analysis/11-02-SUMMARY.md
</output>
