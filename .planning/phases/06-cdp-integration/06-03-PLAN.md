---
phase: 06-cdp-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/main/main.ts
autonomous: true
requirements:
  - AUTO-01
must_haves:
  truths:
    - Main process exposes typed connect/disconnect/status handlers backed by the CDP bridge.
    - App startup config provides deterministic local CDP endpoint for Playwright attach.
    - App shutdown always tears down active automation bridge session state.
  artifacts:
    - path: src/main/main.ts
      provides: CDP endpoint config, bridge manager lifecycle, and typed IPC handler wiring.
  key_links:
    - from: src/main/main.ts
      to: src/main/cdpBridge.ts
      via: bridge instance creation and connect/disconnect/status handler delegation.
      pattern: automation
---

<objective>
Wire the CDP bridge into main-process lifecycle and typed IPC handlers for end-to-end Phase 6 readiness.

Purpose: Complete AUTO-01 integration by connecting contracts and bridge service to runtime startup/shutdown behavior.
Output: Main bootstrap updates for CDP endpoint config, bridge handler wiring, and teardown cleanup.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-cdp-integration/06-CONTEXT.md
@.planning/phases/06-cdp-integration/06-RESEARCH.md
@.planning/phases/06-cdp-integration/06-VALIDATION.md
@src/main/main.ts
@src/main/cdpBridge.ts
@src/main/browserRuntime.ts
@src/shared/ipc.ts
@copilot-instructions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Configure CDP endpoint and instantiate automation bridge in main bootstrap</name>
  <read_first>
- src/main/main.ts
- src/main/cdpBridge.ts
- src/main/browserRuntime.ts
- .planning/phases/06-cdp-integration/06-CONTEXT.md
  </read_first>
  <files>src/main/main.ts</files>
  <behavior>
- Test 1: main process sets a deterministic remote debugging port used for bridge connect endpoint.
- Test 2: bridge manager is created after browser runtime availability using runtime target resolver callback.
- Test 3: bridge remains scoped to main process lifecycle singleton.
  </behavior>
  <action>In `src/main/main.ts`, add CDP endpoint configuration with concrete values: `const cdpPort = Number(process.env.PATHFINDER_CDP_PORT ?? '9222')` and `app.commandLine.appendSwitch('remote-debugging-port', String(cdpPort))` during startup before app readiness. Build endpoint string `const cdpEndpoint = \`http://127.0.0.1:${cdpPort}\``. Instantiate the automation bridge manager (from `src/main/cdpBridge.ts`) in `createWindow()` after `browserRuntime` is created, passing `cdpEndpoint` and runtime target resolver callback `browserRuntime.resolveAutomationTarget`.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/main/main.ts contains `PATHFINDER_CDP_PORT` defaulting to `9222`.
- src/main/main.ts calls `app.commandLine.appendSwitch('remote-debugging-port', String(cdpPort))`.
- src/main/main.ts defines `cdpEndpoint` as `http://127.0.0.1:${cdpPort}` and passes it to bridge initialization.
- src/main/main.ts bridge initialization occurs after `browserRuntime` construction.
  </acceptance_criteria>
  <done>Main bootstrap consistently exposes local CDP endpoint and bridge initialization path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register typed automation IPC handlers and lifecycle cleanup</name>
  <read_first>
- src/main/main.ts
- src/main/cdpBridge.ts
- src/shared/ipc.ts
- src/shared/browser.ts
- .planning/phases/06-cdp-integration/06-RESEARCH.md
  </read_first>
  <files>src/main/main.ts</files>
  <behavior>
- Test 1: `automation:connect`, `automation:disconnect`, and `automation:getStatus` handlers delegate to bridge methods.
- Test 2: connect handler returns typed busy/missing-target/attach-failed reasons from bridge unchanged.
- Test 3: before-quit cleanup calls bridge shutdown and leaves no active ownership state.
  </behavior>
  <action>In `registerIpcHandlers()` inside `src/main/main.ts`, add handlers for `IPC_CHANNELS.automationConnect`, `IPC_CHANNELS.automationDisconnect`, and `IPC_CHANNELS.automationGetStatus` that delegate directly to bridge methods (`connect`, `disconnect`, `getStatus`). Do not add additional renderer-facing command channels in this phase. In `app.on('before-quit')`, call bridge `shutdown()` before process quit to ensure active Playwright CDP sessions are closed and lock state cleared. Keep all existing tab/navigation/download/quick-search/home handlers untouched.</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- src/main/main.ts contains `ipcMain.handle` registrations for automation connect/disconnect/getStatus channel constants.
- src/main/main.ts automation handlers call bridge methods and return their typed results.
- src/main/main.ts `before-quit` lifecycle includes bridge shutdown call.
- Existing non-automation handlers in src/main/main.ts remain present after changes.
  </acceptance_criteria>
  <done>Phase 6 AUTO-01 integration path is fully wired through typed IPC and main-process lifecycle controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typed automation IPC handlers to bridge manager | Untrusted renderer payloads enter privileged CDP control plane |
| app startup/shutdown lifecycle to bridge state | Lifecycle transitions can leave stale automation ownership if not cleaned up |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | S | src/main/main.ts automationConnect handler | mitigate | Require typed owner/session request structure and delegate validation to bridge |
| T-06-08 | D | src/main/main.ts lifecycle | mitigate | Invoke bridge shutdown during before-quit to prevent stale sessions and lock leaks |
| T-06-09 | E | src/main/main.ts IPC registration | mitigate | Register only connect/disconnect/status automation channels and avoid raw CDP command forwarding |
</threat_model>

<verification>
Run `npm run typecheck` after Task 1 and `npm run lint; npm run typecheck; npm run build` after Task 2.
</verification>

<success_criteria>
1. Main process initializes CDP endpoint and bridge manager deterministically.
2. Typed automation connect/disconnect/status IPC handlers are live and delegated to bridge service.
3. App shutdown reliably clears active automation bridge resources.
</success_criteria>

<output>
After completion, create `.planning/phases/06-cdp-integration/06-03-SUMMARY.md`
</output>
