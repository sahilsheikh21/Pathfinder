---
phase: 06-cdp-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/main/browserRuntime.ts
  - src/main/cdpBridge.ts
autonomous: true
requirements:
  - AUTO-01
must_haves:
  truths:
    - Main runtime can resolve a deterministic tab target for automation sessions.
    - CDP bridge enforces single-owner lock and fail-fast connect/disconnect semantics.
  artifacts:
    - path: src/main/browserRuntime.ts
      provides: Active-tab target resolver used by automation bridge.
    - path: src/main/cdpBridge.ts
      provides: Dedicated Playwright CDP bridge service with ownership and status lifecycle.
  key_links:
    - from: src/main/cdpBridge.ts
      to: src/main/browserRuntime.ts
      via: bridge target resolution callback before connectOverCDP attach.
      pattern: resolve
---

<objective>
Implement the main-process CDP bridge core with deterministic target resolution and ownership locking.

Purpose: Deliver AUTO-01 substrate behavior for controlled Playwright-over-CDP session management.
Output: Runtime target lookup helpers and dedicated CDP bridge module with connect/disconnect/status lifecycle.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-cdp-integration/06-CONTEXT.md
@.planning/phases/06-cdp-integration/06-RESEARCH.md
@.planning/phases/06-cdp-integration/06-VALIDATION.md
@src/main/main.ts
@src/main/browserRuntime.ts
@src/shared/browser.ts
@package.json
@copilot-instructions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add browser runtime automation target resolver helpers</name>
  <read_first>
- src/main/browserRuntime.ts
- src/shared/browser.ts
- .planning/phases/06-cdp-integration/06-CONTEXT.md
  </read_first>
  <files>src/main/browserRuntime.ts</files>
  <behavior>
- Test 1: runtime can resolve active tab metadata when no explicit tab id is requested.
- Test 2: runtime can resolve a specific tab id for automation when provided.
- Test 3: resolver returns null for missing/closed targets instead of throwing.
  </behavior>
  <action>In `src/main/browserRuntime.ts`, add a public method `resolveAutomationTarget(tabId?: string)` returning a typed object with concrete fields `{ tabId: string; url: string; webContentsId: number } | null`. Resolution order: (1) if `tabId` provided and exists, use it; (2) else use `activeTabId` if present; (3) otherwise return null. Populate `webContentsId` from the selected tab's `view.webContents.id` and `url` from current tab state. Keep all existing tab create/activate/navigate behavior unchanged.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/main/browserRuntime.ts exports public `resolveAutomationTarget(tabId?: string)`.
- src/main/browserRuntime.ts resolver returns object fields `tabId`, `url`, and `webContentsId`.
- src/main/browserRuntime.ts resolver includes explicit null-return branch when no target tab exists.
  </acceptance_criteria>
  <done>Runtime provides deterministic target identity required by Phase 6 connect semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dedicated Playwright CDP bridge service with ownership lock</name>
  <read_first>
- src/main/cdpBridge.ts
- src/main/browserRuntime.ts
- src/shared/browser.ts
- .planning/phases/06-cdp-integration/06-RESEARCH.md
  </read_first>
  <files>src/main/cdpBridge.ts</files>
  <behavior>
- Test 1: connect establishes session when lock is free and target resolves.
- Test 2: second connect while connected/connecting returns typed busy reason.
- Test 3: disconnect releases lock and returns deterministic disconnected state.
  </behavior>
  <action>Create `src/main/cdpBridge.ts` with a bridge factory/class (for example `createAutomationCdpBridge`) that imports `chromium` from `playwright-core` and exposes methods `connect(request)`, `disconnect(request)`, `getStatus()`, and `shutdown()`. Use explicit in-memory lock fields (`owner`, `sessionId`, `state`, `tabId`, `reason`). Implement connect logic: reject with reason `busy` when lock held, resolve target via callback, reject `missing-target` when null, call `chromium.connectOverCDP(cdpEndpoint)` for attach, set `state: connected` and session metadata on success, set `state: error` with reason `attach-failed` on failure. Implement disconnect logic: validate active session, close Playwright browser handle, clear ownership fields, and return `disconnected` status. Do not implement retries or queueing in Phase 6.</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- src/main/cdpBridge.ts exists and exports bridge constructor/factory with methods connect/disconnect/getStatus/shutdown.
- src/main/cdpBridge.ts imports `chromium` from `playwright-core` and calls `connectOverCDP(`.
- src/main/cdpBridge.ts contains explicit busy-path return with reason `busy`.
- src/main/cdpBridge.ts contains explicit missing-target return with reason `missing-target`.
- src/main/cdpBridge.ts clears owner/session fields during disconnect/shutdown paths.
  </acceptance_criteria>
  <done>Main-process bridge enforces D-04 to D-12 ownership and failure semantics for AUTO-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime tab identity to automation bridge | Target metadata crosses from browser runtime to privileged CDP attach path |
| bridge connect requests to Playwright session ownership | Untrusted connect owner/session input controls privileged automation lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | D | src/main/cdpBridge.ts | mitigate | Enforce single-owner lock and busy rejection to prevent connection storms and collisions |
| T-06-05 | T | src/main/browserRuntime.ts | mitigate | Use runtime-resolved tab ids/webContents ids and explicit null handling to avoid attaching to stale targets |
| T-06-06 | E | src/main/cdpBridge.ts | mitigate | Keep bridge API limited to connect/disconnect/status and do not expose raw CDP commands |
</threat_model>

<verification>
Run `npm run typecheck` after Task 1 and full suite after Task 2.
</verification>

<success_criteria>
1. Runtime target resolver returns deterministic tab metadata for automation attachment.
2. Bridge service connects over CDP with single-owner lock and typed reason codes.
3. Disconnect/shutdown reliably release ownership state and close bridge handles.
</success_criteria>

<output>
After completion, create `.planning/phases/06-cdp-integration/06-02-SUMMARY.md`
</output>
