---
phase: 06-cdp-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/shared/browser.ts
  - src/shared/ipc.ts
  - src/preload/index.ts
autonomous: true
requirements:
  - AUTO-01
must_haves:
  truths:
    - Automation CDP bridge dependency and typed contracts exist before main-process bridge wiring.
    - Renderer can only request connect/disconnect/status through typed preload methods.
  artifacts:
    - path: package.json
      provides: playwright-core dependency for CDP connection support.
    - path: src/shared/browser.ts
      provides: Typed automation session request/response/status models and reason codes.
    - path: src/shared/ipc.ts
      provides: Automation bridge IPC channel constants and Pathfinder API signatures.
    - path: src/preload/index.ts
      provides: Renderer-safe automation bridge method implementations.
  key_links:
    - from: src/preload/index.ts
      to: src/shared/ipc.ts
      via: ipcRenderer.invoke calls constrained to automation connect/disconnect/status channels.
      pattern: automation
---

<objective>
Define dependency and typed API contracts for the Phase 6 automation CDP bridge.

Purpose: Establish compile-safe contracts and preload boundaries before implementing session ownership logic.
Output: Playwright Core dependency, shared automation types, IPC channels, and preload bridge methods.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-cdp-integration/06-CONTEXT.md
@.planning/phases/06-cdp-integration/06-RESEARCH.md
@.planning/phases/06-cdp-integration/06-VALIDATION.md
@package.json
@src/shared/browser.ts
@src/shared/ipc.ts
@src/preload/index.ts
@copilot-instructions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Playwright Core dependency and shared automation bridge contracts</name>
  <read_first>
- package.json
- src/shared/browser.ts
- src/shared/ipc.ts
- .planning/phases/06-cdp-integration/06-CONTEXT.md
  </read_first>
  <files>package.json, src/shared/browser.ts, src/shared/ipc.ts</files>
  <behavior>
- Test 1: project manifest contains `playwright-core` dependency for CDP attach support.
- Test 2: shared types define explicit automation connect/disconnect/status contracts.
- Test 3: IPC channels and PathfinderApi signatures expose only connect/disconnect/status methods for Phase 6.
  </behavior>
  <action>Update `package.json` to add dependency `"playwright-core": "~1.59.1"`. In `src/shared/browser.ts`, add concrete types: `AutomationOwner`, `AutomationSessionState`, `AutomationDisconnectReason`, `AutomationConnectRequest`, `AutomationConnectResult`, `AutomationDisconnectRequest`, `AutomationDisconnectResult`, and `AutomationBridgeStatus`. In `src/shared/ipc.ts`, add channels `automation:connect`, `automation:disconnect`, and `automation:getStatus`; then add PathfinderApi methods `connectAutomationSession(request: AutomationConnectRequest): Promise<AutomationConnectResult>`, `disconnectAutomationSession(request: AutomationDisconnectRequest): Promise<AutomationDisconnectResult>`, and `getAutomationBridgeStatus(): Promise<AutomationBridgeStatus>`.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- package.json contains `"playwright-core": "~1.59.1"` in `dependencies`.
- src/shared/browser.ts exports all added `Automation*` contract types.
- src/shared/ipc.ts includes `automationConnect`, `automationDisconnect`, and `automationGetStatus` channel keys mapped to `automation:connect`, `automation:disconnect`, `automation:getStatus`.
- src/shared/ipc.ts PathfinderApi includes exactly the three automation bridge methods listed in the action.
  </acceptance_criteria>
  <done>Typed contract layer is ready for main-process bridge implementation without exposing raw CDP controls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose typed automation bridge methods through preload boundary</name>
  <read_first>
- src/preload/index.ts
- src/shared/ipc.ts
- src/shared/browser.ts
- .planning/phases/06-cdp-integration/06-CONTEXT.md
  </read_first>
  <files>src/preload/index.ts</files>
  <behavior>
- Test 1: preload exposes typed connect/disconnect/status automation methods on `window.pathfinder`.
- Test 2: each method invokes only its dedicated automation IPC channel.
- Test 3: existing preload subscriptions and APIs remain unchanged.
  </behavior>
  <action>In `src/preload/index.ts`, add methods to `api`: `connectAutomationSession`, `disconnectAutomationSession`, and `getAutomationBridgeStatus`. Implement each with `ipcRenderer.invoke(IPC_CHANNELS.automationConnect, request)`, `ipcRenderer.invoke(IPC_CHANNELS.automationDisconnect, request)`, and `ipcRenderer.invoke(IPC_CHANNELS.automationGetStatus)` respectively. Keep the existing `subscribe` helper, event channels, and all pre-existing Pathfinder API methods intact.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/preload/index.ts contains `connectAutomationSession`, `disconnectAutomationSession`, and `getAutomationBridgeStatus` method implementations.
- src/preload/index.ts implementations call the matching automation channel constants from src/shared/ipc.ts.
- src/preload/index.ts still exposes existing methods (tabs, navigation, downloads, quick search, home) without removals.
  </acceptance_criteria>
  <done>Renderer boundary is constrained to typed automation bridge operations (connect/disconnect/status only).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer request to preload automation methods | Untrusted renderer requests cross into privileged automation session controls |
| preload invoke to main automation channels | IPC payloads may attempt malformed owner/session data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | src/shared/ipc.ts | mitigate | Expose explicit channel constants and typed payloads for connect/disconnect/status only |
| T-06-02 | E | src/preload/index.ts | mitigate | Keep narrow contextBridge API surface and block raw electron/CDP primitives from renderer |
| T-06-03 | S | src/shared/browser.ts | mitigate | Require typed owner/session fields in contract models to support auditable ownership assertions |
</threat_model>

<verification>
Run `npm run typecheck` after each task to validate contract and preload typing.
</verification>

<success_criteria>
1. Playwright Core dependency is declared and ready for bridge implementation.
2. Shared and IPC contracts define connect/disconnect/status substrate APIs.
3. Preload exposes only typed automation bridge methods required for Phase 6.
</success_criteria>

<output>
After completion, create `.planning/phases/06-cdp-integration/06-01-SUMMARY.md`
</output>
