---
phase: 09-sidebar-and-history
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/main/automationLibraryStore.ts
  - src/main/automationHistoryStore.ts
  - src/main/automationPlayback.ts
  - src/main/main.ts
autonomous: true
requirements:
  - AUTO-04
  - AUTO-05
  - SIDE-01
must_haves:
  truths:
    - Main process owns persistence and validation for library and history data.
    - History records are generated from playback lifecycle and never store secrets.
    - Retention cap and deletion semantics are enforced centrally in main process.
  artifacts:
    - path: src/main/automationLibraryStore.ts
      provides: Local-first validated CRUD/tag/filter persistence for saved automations.
    - path: src/main/automationHistoryStore.ts
      provides: Run history persistence with status transitions, retention pruning, and query filters.
    - path: src/main/automationPlayback.ts
      provides: Hooks/callbacks to publish run lifecycle transitions for history writes.
    - path: src/main/main.ts
      provides: Manager/store wiring and IPC handler implementations for phase-9 operations.
  key_links:
    - from: src/main/automationPlayback.ts
      to: src/main/automationHistoryStore.ts
      via: playback status transitions write or update history entries.
      pattern: run lifecycle
    - from: src/main/main.ts
      to: src/main/automationLibraryStore.ts
      via: IPC handlers call validated CRUD/filter methods.
      pattern: automationLibrary
---

<objective>
Implement main-process persistence and orchestration for automation library and execution history.

Purpose: Deliver AUTO-04/AUTO-05 backend behavior and phase-9 state guarantees before renderer UI implementation.
Output: Library/history stores, playback-history synchronization, and phase-9 IPC handlers in main process.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-sidebar-and-history/09-CONTEXT.md
@.planning/phases/09-sidebar-and-history/09-RESEARCH.md
@.planning/phases/09-sidebar-and-history/09-VALIDATION.md
@src/main/main.ts
@src/main/automationPlayback.ts
@src/main/actionRecorder.ts
@src/main/homeStore.ts
@src/shared/browser.ts
@src/shared/ipc.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement local automation library store with validated CRUD/tag/filter behavior</name>
  <read_first>
- src/main/homeStore.ts
- src/main/actionRecorder.ts
- src/shared/browser.ts
- .planning/phases/09-sidebar-and-history/09-CONTEXT.md
  </read_first>
  <files>src/main/automationLibraryStore.ts</files>
  <behavior>
- Test 1: create/import/upsert operations persist items with non-empty names and uniqueness auto-suffix behavior.
- Test 2: tag updates support free-form multi-tags and filtering by text plus tag OR semantics.
- Test 3: delete removes target item and returns deterministic post-delete listing.
  </behavior>
  <action>Create src/main/automationLibraryStore.ts using the same userData JSON validation pattern as src/main/homeStore.ts. Implement methods `list(request)`, `upsert(request)`, `remove(request)`, and `run(request)` metadata resolution helpers. Enforce name validation: trim whitespace, reject empty names, and auto-suffix duplicates as `Name (2)`, `Name (3)` style. Normalize tags to lowercase trimmed unique arrays and support text search + tag OR filtering. Store fields required by context: id, name, description, tags, workflowPath or workflowDocument reference, origin (`recorded` or `imported`), updatedAt, lastRunAt.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/main/automationLibraryStore.ts exists with list/upsert/remove behavior.
- Duplicate names are auto-suffixed instead of rejected.
- Tag normalization and OR filter logic are implemented.
- Stored item model includes origin, updatedAt, and lastRunAt fields.
  </acceptance_criteria>
  <done>Automation library persistence satisfies AUTO-04 data behavior and filtering decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement execution history store with lifecycle updates, retention cap, and redaction</name>
  <read_first>
- src/main/automationPlayback.ts
- src/shared/browser.ts
- .planning/phases/09-sidebar-and-history/09-CONTEXT.md
  </read_first>
  <files>src/main/automationHistoryStore.ts</files>
  <behavior>
- Test 1: history store inserts running entries and updates them to terminal status on completion/failure/cancel.
- Test 2: history list supports newest-first order, status filter, and text search.
- Test 3: retention cap prunes oldest non-running entries to max 500 records.
  </behavior>
  <action>Create src/main/automationHistoryStore.ts with methods `recordRunStarted(input)`, `recordRunFinished(input)`, `list(request)`, `remove(request)`, `clear(request)`, `markWorkflowDeleted(workflowId)`. Persist fields: id, workflowId, workflowNameSnapshot, tagsSnapshot, status, sourceLabel, startedAt, finishedAt, durationMs, failureSnippet, failureDetail, runId, targetUrlAtStart, workflowOrigin. Enforce redaction by storing no variable values or secret payloads. Implement clear-all behavior that preserves currently running entries. Implement retention pruning immediately after each write to keep at most 500 entries.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- History store supports running-to-terminal status transitions.
- List API supports status and text filters with newest-first ordering.
- Retention cap logic keeps max 500 records while preserving active running entries.
- Persisted entry model excludes variable value/secret fields.
  </acceptance_criteria>
  <done>History persistence satisfies AUTO-05 and retention/privacy requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire playback lifecycle and IPC handlers to library/history stores</name>
  <read_first>
- src/main/main.ts
- src/main/automationPlayback.ts
- src/main/automationLibraryStore.ts
- src/main/automationHistoryStore.ts
- src/shared/ipc.ts
  </read_first>
  <files>src/main/automationPlayback.ts, src/main/main.ts</files>
  <behavior>
- Test 1: IPC handlers exist for all phase-9 library/history/sidebar preference operations.
- Test 2: runs started from any surface write history entries and update library `lastRunAt`.
- Test 3: playback completion/failure/cancel updates history status with duration and failure context.
  </behavior>
  <action>In src/main/automationPlayback.ts, add lifecycle callbacks (for example `onRunStarted`, `onRunFinished`) or equivalent hook points so main.ts can capture run transitions without polling renderer. In src/main/main.ts, instantiate automationLibraryStore and automationHistoryStore, then register IPC handlers for the channels introduced in plan 01. Map `automation:library:run` and `automation:history:rerun` to the existing playback start path and include `sourceLabel` metadata (`sidebar` for sidebar actions, `command` for command palette). On playback start, create running history entry; on terminal state, finalize history entry with duration/failure fields and update matching library item `lastRunAt`.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- src/main/main.ts contains IPC handlers for library/history/sidebar preference APIs.
- Playback lifecycle writes running and terminal history entries.
- Library items update lastRunAt on successful run start.
- Full lint/typecheck/build suite passes.
  </acceptance_criteria>
  <done>Main process runtime behavior for AUTO-04/AUTO-05 is complete and accessible to renderer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer IPC requests to main stores | Untrusted payloads could attempt invalid deletes, malformed tags, or state corruption |
| playback lifecycle to history persistence | Failure payloads may contain sensitive content and must be sanitized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | T | src/main/automationLibraryStore.ts | mitigate | Validate all request payloads and normalize names/tags before persistence |
| T-09-06 | D | src/main/automationHistoryStore.ts | mitigate | Enforce retention cap and write-time pruning to avoid unbounded growth |
| T-09-07 | I | src/main/automationHistoryStore.ts | mitigate | Redact and exclude variable values from persisted history/failure payloads |
| T-09-08 | R | src/main/main.ts | mitigate | Persist source labels, timestamps, runId, and workflow snapshots for auditable history |
| T-09-09 | E | src/main/main.ts | mitigate | Keep all mutating operations in main process and expose only typed IPC handlers |
</threat_model>

<verification>
Run `npm run typecheck` after Tasks 1 and 2, then `npm run lint; npm run typecheck; npm run build` after Task 3.
</verification>

<success_criteria>
1. Library CRUD/tags/filtering behavior is validated and persisted in main process.
2. History lifecycle, retention, and redaction behavior is enforced centrally.
3. IPC handlers and playback lifecycle wiring provide deterministic state transitions.
</success_criteria>

<output>
After completion, create `.planning/phases/09-sidebar-and-history/09-02-SUMMARY.md`
</output>
