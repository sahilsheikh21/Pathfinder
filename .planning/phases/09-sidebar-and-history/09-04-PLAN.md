---
phase: 09-sidebar-and-history
plan: 04
type: execute
wave: 4
depends_on:
  - 09-03
files_modified:
  - src/renderer/lib/commandPalette.ts
  - src/renderer/App.tsx
  - src/renderer/components/HomeStarterPage.tsx
  - src/main/main.ts
  - src/renderer/styles/global.css
autonomous: true
requirements:
  - AUTO-04
  - AUTO-05
  - SIDE-01
must_haves:
  truths:
    - Sidebar behaviors stay command-first by adding explicit toggle/open commands.
    - Home recent automations view derives from history-backed data and updates immediately.
    - Cross-surface indicators (badges/status) use one shared source of truth.
  artifacts:
    - path: src/renderer/lib/commandPalette.ts
      provides: Sidebar toggle/open command entries for keyboard-first control.
    - path: src/renderer/App.tsx
      provides: Unified orchestration for sidebar badges, history refresh cadence, and command hooks.
    - path: src/renderer/components/HomeStarterPage.tsx
      provides: Recent automation cards sourced from live history projection.
    - path: src/main/main.ts
      provides: Home recent-automations IPC projection backed by history store.
  key_links:
    - from: src/main/main.ts
      to: src/renderer/components/HomeStarterPage.tsx
      via: `home:listRecentAutomations` responds from history projection.
      pattern: recent automations
    - from: src/renderer/lib/commandPalette.ts
      to: src/renderer/App.tsx
      via: sidebar command handlers dispatch sidebar state actions.
      pattern: sidebar toggle
---

<objective>
Complete cross-surface integration and command-first polish for phase 9.

Purpose: Ensure sidebar, history, command palette, and home recent-automation experiences stay synchronized and operationally coherent.
Output: Command hooks, home projection integration, badge/status consistency, and final regression hardening.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-sidebar-and-history/09-CONTEXT.md
@.planning/phases/09-sidebar-and-history/09-RESEARCH.md
@.planning/phases/09-sidebar-and-history/09-VALIDATION.md
@src/renderer/lib/commandPalette.ts
@src/renderer/App.tsx
@src/renderer/components/HomeStarterPage.tsx
@src/main/main.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add command-palette controls for sidebar open/toggle workflow</name>
  <read_first>
- src/renderer/lib/commandPalette.ts
- src/renderer/App.tsx
- .planning/phases/09-sidebar-and-history/09-CONTEXT.md
  </read_first>
  <files>src/renderer/lib/commandPalette.ts, src/renderer/App.tsx</files>
  <behavior>
- Test 1: command palette exposes deterministic sidebar commands (toggle/open library/open history).
- Test 2: commands preserve existing command ranking and error behavior.
- Test 3: command execution updates sidebar state without disrupting active playback/recording indicators.
  </behavior>
  <action>Extend src/renderer/lib/commandPalette.ts with concrete commands: `sidebar.toggle`, `sidebar.open.library`, and `sidebar.open.history`. Add corresponding dependencies in createBrowserCommands options and implement handlers in src/renderer/App.tsx that update sidebar collapse and active-section state. Ensure these commands operate even when sidebar is currently collapsed or in overlay mode.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- Command registry contains the three sidebar commands with descriptions and keywords.
- App command handlers update sidebar state deterministically.
- Existing command palette commands continue to compile and execute.
  </acceptance_criteria>
  <done>Sidebar remains keyboard-first and consistent with product command model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Source Home recent automations from history-backed projection and live refresh</name>
  <read_first>
- src/main/main.ts
- src/renderer/components/HomeStarterPage.tsx
- src/main/homeStore.ts
- src/shared/browser.ts
  </read_first>
  <files>src/main/main.ts, src/renderer/components/HomeStarterPage.tsx, src/renderer/App.tsx</files>
  <behavior>
- Test 1: `home:listRecentAutomations` returns latest recent runs derived from history records.
- Test 2: Home recent automations cards update after run completion/failure and after history remove/clear actions.
- Test 3: deleted workflow history entries render as deleted marker and are excluded from launch actions.
  </behavior>
  <action>In src/main/main.ts, update `home:listRecentAutomations` handler to derive recent entries from automationHistoryStore and automationLibraryStore projections instead of homeStore placeholder array. In src/renderer/components/HomeStarterPage.tsx, consume this projection with status/time display and disable launch action for deleted workflows. In src/renderer/App.tsx, trigger recent-automation refresh after playback status transitions and history mutation actions (remove/clear/rerun) so home and sidebar stay synchronized.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- Home recent automations no longer relies on placeholder-only source.
- Recent cards reflect latest history-backed results after state changes.
- Deleted workflows do not provide actionable run buttons from Home cards.
  </acceptance_criteria>
  <done>Cross-surface history projection is consistent between Home and sidebar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Final integration hardening for badges, confirmations, and accessibility regressions</name>
  <read_first>
- src/renderer/App.tsx
- src/renderer/styles/global.css
- src/renderer/components/AutomationSidebar.tsx
- src/renderer/components/AutomationSidebarHistory.tsx
  </read_first>
  <files>src/renderer/App.tsx, src/renderer/styles/global.css</files>
  <behavior>
- Test 1: section badges update immediately for failures and running entries in expanded and collapsed modes.
- Test 2: destructive actions (delete, clear-all) remain confirmation-gated and keyboard reachable.
- Test 3: narrow viewport, overlay mode, and keyboard navigation remain functional after integration changes.
  </behavior>
  <action>In src/renderer/App.tsx, compute sidebar badge counts from current history state (failed/running) and pass them to sidebar shell for icon-rail and expanded views. Ensure remove/clear-all flows always pass through confirmation state before invoking typed APIs. In src/renderer/styles/global.css, add/fix focus-visible styles and badge state styles for accessibility and deterministic visual feedback across docked/collapsed/overlay modes.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- Sidebar badges reflect current failure/running counts immediately.
- Delete and clear-all actions cannot bypass confirmation path.
- Keyboard focus outlines are visible for sidebar interactive controls.
- Full lint/typecheck/build suite passes.
  </acceptance_criteria>
  <done>Phase-9 cross-surface UX is coherent, accessible, and command-first.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command palette to sidebar state | Keyboard-triggered state changes could bypass expected UI safety flows |
| history/home projection rendering | Mixed-source data could produce stale or misleading automation launch states |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-15 | R | src/renderer/App.tsx | mitigate | Keep deterministic badge/count derivation from canonical history state |
| T-09-16 | T | src/renderer/lib/commandPalette.ts | mitigate | Limit sidebar commands to explicit safe state transitions only |
| T-09-17 | I | src/main/main.ts | mitigate | Exclude secret/variable values from home projection payloads |
| T-09-18 | E | src/renderer/App.tsx | mitigate | Enforce confirmation gate for destructive actions before API invocation |
| T-09-19 | D | src/renderer/styles/global.css | mitigate | Preserve responsive/keyboard accessibility so control paths remain available under narrow layouts |
</threat_model>

<verification>
Run `npm run typecheck` after Tasks 1 and 2, then `npm run lint; npm run typecheck; npm run build` after Task 3.
</verification>

<success_criteria>
1. Command palette and sidebar controls are integrated and deterministic.
2. Home recent automations and sidebar history remain synchronized.
3. Final accessibility and destructive-action safeguards are in place.
</success_criteria>

<output>
After completion, create `.planning/phases/09-sidebar-and-history/09-04-SUMMARY.md`
</output>
