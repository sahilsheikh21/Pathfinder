---
phase: 09-sidebar-and-history
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - src/renderer/components/AutomationSidebar.tsx
  - src/renderer/components/AutomationSidebarLibrary.tsx
  - src/renderer/components/AutomationSidebarHistory.tsx
  - src/renderer/App.tsx
  - src/renderer/styles/global.css
autonomous: true
requirements:
  - AUTO-04
  - AUTO-05
  - SIDE-01
must_haves:
  truths:
    - Sidebar presents Saved Automations, History, and AI Chat sections with persisted collapse and section state.
    - Library UI supports create/import/rename/delete/tag/filter/run behavior.
    - History UI supports status/duration/failure display, filters, details, rerun, and deletion semantics.
  artifacts:
    - path: src/renderer/components/AutomationSidebar.tsx
      provides: Sidebar shell, section routing, collapse/expand/icon rail, responsive drawer fallback.
    - path: src/renderer/components/AutomationSidebarLibrary.tsx
      provides: Library CRUD/tag/filter/run UI interactions.
    - path: src/renderer/components/AutomationSidebarHistory.tsx
      provides: History list/details/filter/rerun/remove/clear behaviors with virtualization.
    - path: src/renderer/App.tsx
      provides: State orchestration and preload API wiring.
    - path: src/renderer/styles/global.css
      provides: Sidebar layout and interaction styling.
  key_links:
    - from: src/renderer/App.tsx
      to: src/preload/index.ts
      via: renderer calls typed library/history/sidebar-preference methods.
      pattern: automation sidebar
    - from: src/renderer/components/AutomationSidebarHistory.tsx
      to: src/main/automationHistoryStore.ts
      via: list/filter/rerun/remove/clear handlers through preload API.
      pattern: history lifecycle
---

<objective>
Build phase-9 sidebar renderer surfaces and integrate them into the app shell.

Purpose: Deliver SIDE-01 visual/interaction behavior and complete AUTO-04/AUTO-05 user-facing operations.
Output: Collapsible sidebar, library section, history section, AI placeholder section, and responsive behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-sidebar-and-history/09-CONTEXT.md
@.planning/phases/09-sidebar-and-history/09-RESEARCH.md
@.planning/phases/09-sidebar-and-history/09-VALIDATION.md
@src/renderer/App.tsx
@src/renderer/components/HomeStarterPage.tsx
@src/renderer/lib/commandPalette.ts
@src/renderer/styles/global.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create sidebar shell with persisted collapse/section state and responsive drawer fallback</name>
  <read_first>
- src/renderer/App.tsx
- src/renderer/styles/global.css
- .planning/phases/09-sidebar-and-history/09-CONTEXT.md
  </read_first>
  <files>src/renderer/components/AutomationSidebar.tsx, src/renderer/App.tsx, src/renderer/styles/global.css</files>
  <behavior>
- Test 1: sidebar defaults left-docked and supports expanded/collapsed icon-rail modes.
- Test 2: collapse state, width, active section, and filter/search state are restored after app relaunch.
- Test 3: narrow viewport switches to overlay drawer behavior below configured threshold.
  </behavior>
  <action>Create src/renderer/components/AutomationSidebar.tsx with section tabs (`library`, `history`, `ai-chat`), collapse/expand toggle, icon rail mode, draggable width clamp (min 280 max 520), and responsive drawer fallback under 980px viewport width. In src/renderer/App.tsx, load/save sidebar preferences through typed preload methods, preserving active section, collapse state, width, and section-local UI state payload. In src/renderer/styles/global.css, add explicit classes for docked sidebar, collapsed icon rail, responsive overlay drawer, badges, and 150ms transition timing.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- Sidebar shell component exists and mounts in App layout.
- Preferences round-trip through preload APIs and rehydrate on startup.
- CSS includes deterministic classes for docked, collapsed, and overlay modes.
- Typecheck passes with new sidebar shell wiring.
  </acceptance_criteria>
  <done>Core sidebar structure and persistence behaviors satisfy SIDE-01 baseline decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement library section UI for CRUD, tags/filtering, and run actions</name>
  <read_first>
- src/renderer/components/AutomationSidebar.tsx
- src/renderer/App.tsx
- src/shared/browser.ts
- .planning/phases/09-sidebar-and-history/09-CONTEXT.md
  </read_first>
  <files>src/renderer/components/AutomationSidebarLibrary.tsx, src/renderer/App.tsx</files>
  <behavior>
- Test 1: library section lists items sorted by lastRunAt then updatedAt and supports text+tag OR filtering.
- Test 2: create/import/rename/delete flows enforce non-empty naming and explicit delete confirmation.
- Test 3: run action triggers existing playback flow and surfaces missing-variable prompt/errors consistently.
  </behavior>
  <action>Create src/renderer/components/AutomationSidebarLibrary.tsx with library list, filter controls, tag chip editor, create/import actions, rename, delete confirmation, and run button. In App state orchestration, call `automationLibraryList`, `automationLibraryUpsert`, `automationLibraryDelete`, and `automationLibraryRun` preload methods; keep run action connected to existing playback start/prompt/error handlers already used by command palette. Persist section-local filter/search state in sidebar preferences payload so collapse/restore retains context.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- Library section component exists and integrates with App sidebar shell.
- UI supports create/import/rename/delete/tag/filter operations via typed APIs.
- Delete action requires explicit confirmation path.
- Run action uses existing playback flow (including variable prompt behavior).
  </acceptance_criteria>
  <done>Library UI behavior satisfies AUTO-04 and phase context constraints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement history section UI with virtualization, details, and rerun/remove/clear controls</name>
  <read_first>
- src/renderer/components/AutomationSidebar.tsx
- src/renderer/App.tsx
- src/renderer/styles/global.css
- src/shared/browser.ts
  </read_first>
  <files>src/renderer/components/AutomationSidebarHistory.tsx, src/renderer/App.tsx, src/renderer/styles/global.css</files>
  <behavior>
- Test 1: history list shows running/success/failed/cancelled entries with status badge, duration, time, and failure snippet.
- Test 2: history supports status filter + text search and keeps running items pinned above completed entries.
- Test 3: row actions support view details, rerun, remove, and clear-all with confirmation while preserving active running entries.
  </behavior>
  <action>Create src/renderer/components/AutomationSidebarHistory.tsx with virtualized list rendering (for example windowed list implementation), status/text filters, row badges, relative time display, failure snippet ellipsis, expandable detail panel, rerun/remove actions, and clear-all confirmation flow. In App orchestration, call history list/remove/clear/rerun preload methods and refresh on playback status changes. Add CSS for history row states, failure highlight, details pane, and accessibility focus styles. Ensure AI Chat section in sidebar renders a structured "coming soon" panel and no executable AI actions.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- History section component exists with required fields and row actions.
- Running entries remain visible/pinned until terminal status update.
- Clear-all preserves active running entries and confirms destructive action.
- Full lint/typecheck/build suite passes.
  </acceptance_criteria>
  <done>History and AI placeholder UI behaviors satisfy AUTO-05 and SIDE-01 decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer sidebar interactions | User-driven CRUD and destructive actions must remain constrained to typed APIs |
| UI rendering of failure metadata | Failure messages may contain sensitive fragments and must be displayed safely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | T | src/renderer/App.tsx | mitigate | Use typed API methods only; reject direct mutation paths and validate inputs before submit |
| T-09-11 | I | src/renderer/components/AutomationSidebarHistory.tsx | mitigate | Render failure details as plain text, clamp snippets, and avoid injecting raw HTML |
| T-09-12 | D | src/renderer/components/AutomationSidebarHistory.tsx | mitigate | Use virtualized rendering and bounded list queries for large history sets |
| T-09-13 | R | src/renderer/components/AutomationSidebarLibrary.tsx | mitigate | Require confirmation for delete/clear actions and keep deterministic action logs through history entries |
| T-09-14 | E | src/renderer/components/AutomationSidebar.tsx | mitigate | Keep AI section non-executable in phase 9 with explicit placeholder state |
</threat_model>

<verification>
Run `npm run typecheck` after Tasks 1 and 2, then `npm run lint; npm run typecheck; npm run build` after Task 3.
</verification>

<success_criteria>
1. Sidebar shell and responsive behavior meet persistence/collapse requirements.
2. Library and history sections provide complete operational controls.
3. UI performance and security constraints are maintained.
</success_criteria>

<output>
After completion, create `.planning/phases/09-sidebar-and-history/09-03-SUMMARY.md`
</output>
