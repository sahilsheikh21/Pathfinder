---
phase: 09-sidebar-and-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/browser.ts
  - src/shared/ipc.ts
  - src/preload/index.ts
autonomous: true
requirements:
  - AUTO-04
  - AUTO-05
  - SIDE-01
must_haves:
  truths:
    - Shared contracts define automation library entities, history entities, and sidebar preference state with explicit redaction-safe fields.
    - Renderer can query and mutate library/history/sidebar state only through typed preload APIs.
    - Channel names and API signatures remain deterministic and backward-compatible for existing browser and automation operations.
  artifacts:
    - path: src/shared/browser.ts
      provides: Typed contracts for library CRUD, tags/filtering, history records, and sidebar preferences.
    - path: src/shared/ipc.ts
      provides: IPC channel constants and Pathfinder API signatures for new phase-9 operations.
    - path: src/preload/index.ts
      provides: Secure renderer invoke methods for phase-9 channels.
  key_links:
    - from: src/preload/index.ts
      to: src/shared/ipc.ts
      via: `ipcRenderer.invoke` uses new library/history/sidebar channel constants.
      pattern: automationLibrary
---

<objective>
Define phase-9 shared contracts and typed IPC/preload APIs for automation library, execution history, and sidebar preferences.

Purpose: Establish compile-safe boundaries before main-process/runtime and renderer implementation.
Output: New shared type system and secure renderer-facing methods for AUTO-04, AUTO-05, SIDE-01.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-sidebar-and-history/09-CONTEXT.md
@.planning/phases/09-sidebar-and-history/09-RESEARCH.md
@.planning/phases/09-sidebar-and-history/09-VALIDATION.md
@src/shared/browser.ts
@src/shared/ipc.ts
@src/preload/index.ts
@copilot-instructions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define typed automation library, history, and sidebar preference contracts</name>
  <read_first>
- src/shared/browser.ts
- .planning/phases/09-sidebar-and-history/09-CONTEXT.md
- .planning/phases/09-sidebar-and-history/09-RESEARCH.md
  </read_first>
  <files>src/shared/browser.ts</files>
  <behavior>
- Test 1: library types represent create/rename/delete/tag/filter operations with explicit request/response payloads.
- Test 2: history types represent running/completed/failed/cancelled entries with duration and failure snippet fields.
- Test 3: sidebar preference type includes collapse state, width, active section, and persisted filter/search state.
  </behavior>
  <action>In src/shared/browser.ts, add concrete contracts: `AutomationLibraryItem`, `AutomationLibraryFilter`, `AutomationLibraryListRequest`, `AutomationLibraryUpsertRequest`, `AutomationLibraryDeleteRequest`, `AutomationLibraryRunRequest`, `AutomationLibraryResult`; history contracts `AutomationRunSourceLabel`, `AutomationHistoryStatus`, `AutomationHistoryEntry`, `AutomationHistoryListRequest`, `AutomationHistoryListResult`, `AutomationHistoryRemoveRequest`, `AutomationHistoryClearRequest`, `AutomationHistoryRerunRequest`; and sidebar preference contracts `AutomationSidebarSection`, `AutomationSidebarPreferences`, `AutomationSidebarPreferencesUpdateRequest`. Include required fields for AUTO-05: status, durationMs, finishedAt, failureSnippet, failureDetail, sourceLabel. Exclude secret/variable-value fields from persisted history contract types.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/shared/browser.ts exports all new library/history/sidebar types listed in action text.
- `AutomationHistoryStatus` union includes `running`, `success`, `failed`, and `cancelled`.
- `AutomationHistoryEntry` includes `durationMs`, `finishedAt`, and failure context fields.
- No contract field stores raw variable values or secret payloads.
  </acceptance_criteria>
  <done>Shared contract layer is ready for main process stores and renderer UI integration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add typed IPC channels and preload methods for phase-9 operations</name>
  <read_first>
- src/shared/ipc.ts
- src/preload/index.ts
- src/shared/browser.ts
  </read_first>
  <files>src/shared/ipc.ts, src/preload/index.ts</files>
  <behavior>
- Test 1: IPC channels include library CRUD/list/run and history list/remove/clear/rerun operations.
- Test 2: IPC channels include sidebar preference get/save operations.
- Test 3: Pathfinder preload API exposes typed invoke wrappers for all new channels.
  </behavior>
  <action>In src/shared/ipc.ts, add explicit channel constants: `automation:library:list`, `automation:library:upsert`, `automation:library:delete`, `automation:library:run`, `automation:history:list`, `automation:history:remove`, `automation:history:clear`, `automation:history:rerun`, `automation:sidebar:getPreferences`, `automation:sidebar:savePreferences`. Extend `PathfinderApi` with typed methods mapped to these channels. In src/preload/index.ts, implement matching methods via `ipcRenderer.invoke`, preserving existing method signatures and behavior for all non-phase-9 APIs.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- `IPC_CHANNELS` contains the exact new channel names listed above.
- `PathfinderApi` includes typed methods for library, history, and sidebar preference operations.
- `src/preload/index.ts` includes invoke wrappers for each new channel.
- Existing tab/navigation/quick-search/recording/playback/home methods continue to compile unchanged.
  </acceptance_criteria>
  <done>Renderer can call phase-9 operations through secure typed preload bridges.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer to preload | Untrusted UI input crosses into privileged operations for library/history mutation |
| preload to main IPC | Channel misuse could attempt unauthorized destructive actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T | src/shared/browser.ts | mitigate | Use strict request/response interfaces and discriminated unions for status/section fields |
| T-09-02 | I | src/shared/browser.ts | mitigate | Exclude secret/variable-value fields from persisted history contracts |
| T-09-03 | S | src/shared/ipc.ts | mitigate | Add explicit channels only for required operations; no wildcard or generic command channel |
| T-09-04 | E | src/preload/index.ts | mitigate | Expose minimal typed methods and avoid exposing raw Electron/Node primitives |
</threat_model>

<verification>
Run `npm run typecheck` after each task.
</verification>

<success_criteria>
1. Shared contracts encode AUTO-04/AUTO-05/SIDE-01 data requirements precisely.
2. IPC channel names and preload signatures are explicit and type-safe.
3. Existing APIs remain backward-compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/09-sidebar-and-history/09-01-SUMMARY.md`
</output>
