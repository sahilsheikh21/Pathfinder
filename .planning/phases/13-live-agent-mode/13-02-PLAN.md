---
phase: 13-live-agent-mode
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/main/liveAgentAuditStore.ts
  - src/main/liveAgentOrchestrator.ts
  - src/main/automationHistoryStore.ts
  - src/main/main.ts
autonomous: true
requirements:
  - AI-04
must_haves:
  truths:
    - Every live-agent step records planned action, risk tier, approval decision, observed result, and next-step rationale.
    - High-impact actions remain blocked unless the corresponding batch approval exists.
    - Pause waits for a safe boundary, cancel is immediate hard stop, and resume is context-guarded.
  artifacts:
    - path: src/main/liveAgentAuditStore.ts
      provides: Persistent append-only audit events by run id.
    - path: src/main/liveAgentOrchestrator.ts
      provides: Batch gate enforcement plus pause/cancel/resume behavior.
    - path: src/main/main.ts
      provides: IPC handlers for audit retrieval and run-state controls.
  key_links:
    - from: src/main/liveAgentOrchestrator.ts
      to: src/main/liveAgentAuditStore.ts
      via: Step lifecycle emits structured audit events.
      pattern: audit
    - from: src/main/liveAgentOrchestrator.ts
      to: src/main/automationPlayback.ts
      via: Approved executable steps are dispatched through playback engine capabilities.
      pattern: playback
---

<objective>
Implement the live-agent enforcement core: audit persistence, batch approval execution gating, and deterministic run-control semantics.

Purpose: Fulfill AI-04 safety and observability guarantees in main process before UI finalization.
Output: Audit store, orchestrator enforcement behaviors, and IPC exposure for timeline retrieval.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-live-agent-mode/13-CONTEXT.md
@.planning/phases/13-live-agent-mode/13-RESEARCH.md
@.planning/phases/13-live-agent-mode/13-VALIDATION.md
@.planning/phases/13-live-agent-mode/13-01-PLAN.md
@src/main/liveAgentOrchestrator.ts
@src/main/automationPlayback.ts
@src/main/automationHistoryStore.ts
@src/main/main.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add persistent redacted step-audit storage</name>
  <read_first>
- src/main/automationHistoryStore.ts
- src/main/sessionStore.ts
- src/shared/browser.ts
- .planning/phases/13-live-agent-mode/13-CONTEXT.md
  </read_first>
  <files>src/main/liveAgentAuditStore.ts</files>
  <behavior>
- Test 1: audit store can append ordered step events grouped by run id.
- Test 2: audit events include required fields: planned action, risk tier, approval decision, observed result, next rationale.
- Test 3: sensitive values are redacted before persistence.
  </behavior>
  <action>Create src/main/liveAgentAuditStore.ts using existing local-store patterns from automation history persistence. Implement APIs appendEvent(runId, event), listEvents(runId), and clearRun(runId). Event schema must include stepIndex, actionSummary, riskTier, approvalDecision, observedResult, nextStepRationale, and timestamp. Redact known secret fields and variable payload values before writing.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/main/liveAgentAuditStore.ts exists and exports append/list APIs.
- file contains fields for action summary, risk tier, approval decision, observed result, and next rationale.
- redaction helper exists and is applied before persistence writes.
- `npm run typecheck` exits successfully.
  </acceptance_criteria>
  <done>Per-step audit events are persisted in a structured, redacted, queryable format.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce batch approval gate and pause/cancel/resume semantics in orchestrator</name>
  <read_first>
- src/main/liveAgentOrchestrator.ts
- src/main/liveAgentRiskPolicy.ts
- src/main/automationPlayback.ts
- src/main/liveAgentAuditStore.ts
  </read_first>
  <files>src/main/liveAgentOrchestrator.ts</files>
  <behavior>
- Test 1: high-impact steps cannot execute without explicit batch approval token/state.
- Test 2: pause transitions at safe boundaries only and does not execute subsequent post-boundary steps.
- Test 3: cancel hard-stops active run; resume validates context before continuing from last unexecuted step.
  </behavior>
  <action>Extend orchestrator execution loop to partition planned steps into approval batches, entering waiting-approval state before each high-impact batch. Require explicit approveBatch call to release execution. Emit audit events before and after each step through liveAgentAuditStore. Implement pause boundary logic between steps, immediate cancel guard that halts further dispatch, and resume path that validates target context (tab/url identity) before continuing; on mismatch, return typed failure requiring restart/re-approval.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- orchestrator code contains explicit check for approved batch state before high-impact step dispatch.
- orchestrator emits step audit events for both planned and observed outcomes.
- pause logic is evaluated at step boundaries and cancel logic short-circuits dispatch immediately.
- resume path contains context-validity check and mismatch failure return path.
- `npm run lint` and `npm run typecheck` exit successfully.
  </acceptance_criteria>
  <done>Core safety guarantees for approval gating and run controls are enforced in main-process execution loop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire audit retrieval handlers and integrate run records</name>
  <read_first>
- src/main/main.ts
- src/main/automationHistoryStore.ts
- src/main/liveAgentAuditStore.ts
- src/shared/ipc.ts
  </read_first>
  <files>src/main/main.ts, src/main/automationHistoryStore.ts</files>
  <behavior>
- Test 1: main process exposes audit retrieval endpoint by run id.
- Test 2: run terminal states are recorded with consistent status mappings for completed/failed/cancelled.
- Test 3: audit retrieval returns deterministic ordered events.
  </behavior>
  <action>Add IPC handlers in src/main/main.ts for live-agent audit retrieval (for example, liveAgentGetAuditTrail). Ensure orchestrator completion callbacks write final run summaries into existing automation history semantics using mapped status values. Keep event ordering deterministic by persisted stepIndex and timestamp. Update shared status mapping helpers if required so cancelled/failed/completed remain consistent with existing UI filters.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/main/main.ts contains handler for audit retrieval channel.
- automation history integration records terminal run status for live-agent runs.
- audit retrieval returns events in stable ordered sequence.
- `npm run lint` and `npm run typecheck` exit successfully.
  </acceptance_criteria>
  <done>Live-agent audit and run status data are available for renderer timeline and history surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator execution loop -> browser actions | Approved plans cross from decision layer into state-mutating browser operations |
| audit persistence -> renderer timeline | Stored run/audit data crosses process boundary for user display |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | T | src/main/liveAgentOrchestrator.ts | mitigate | Block high-impact step dispatch until explicit batch approval state is present |
| T-13-06 | R | src/main/liveAgentAuditStore.ts | mitigate | Append immutable ordered step events with timestamps and run-id correlation |
| T-13-07 | I | src/main/liveAgentAuditStore.ts | mitigate | Redact secrets and variable payload values before audit writes |
| T-13-08 | D | src/main/liveAgentOrchestrator.ts | mitigate | Immediate cancel hard-stop and bounded resume checks prevent uncontrolled loops |
</threat_model>

<verification>
Run `npm run typecheck` after Task 1 and `npm run lint; npm run typecheck` after Tasks 2-3.
</verification>

<success_criteria>
1. High-impact steps cannot run without explicit approved batch state.
2. Step-level audit events persist required rationale and result fields with redaction.
3. Pause/cancel/resume semantics match locked phase decisions and are queryable via IPC.
</success_criteria>

<output>
After completion, create `.planning/phases/13-live-agent-mode/13-02-SUMMARY.md`
</output>
