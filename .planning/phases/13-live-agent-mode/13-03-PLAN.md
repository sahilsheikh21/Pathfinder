---
phase: 13-live-agent-mode
plan: 03
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
files_modified:
  - src/renderer/App.tsx
  - src/renderer/lib/commandPalette.ts
  - src/renderer/components/AutomationSidebar.tsx
  - src/renderer/styles/global.css
autonomous: true
requirements:
  - AI-04
must_haves:
  truths:
    - Users can start live-agent runs and explicitly approve the next N-step batch before high-impact execution.
    - Users can pause, resume, and cancel from both AI sidebar and command palette controls.
    - Users can view per-step live timeline entries including action, risk tier, approval decision, observed result, and rationale.
  artifacts:
    - path: src/renderer/App.tsx
      provides: Live-agent control state, approval UI, and timeline rendering.
    - path: src/renderer/lib/commandPalette.ts
      provides: Command-first controls for start/pause/resume/cancel.
    - path: src/renderer/styles/global.css
      provides: Visual styles for batch approval cards, status badges, and audit timeline.
  key_links:
    - from: src/renderer/App.tsx
      to: src/preload/index.ts
      via: Live-agent start/status/approve/pause/resume/cancel and audit retrieval APIs.
      pattern: liveAgent
    - from: src/renderer/lib/commandPalette.ts
      to: src/renderer/App.tsx
      via: Command dependencies focus ai-chat and dispatch live-agent controls.
      pattern: ai.agent
---

<objective>
Deliver the user-facing live-agent controls and timeline visibility required to operate Phase 13 safely.

Purpose: Complete AI-04 UX by exposing explicit approvals, run controls, and explainable step logs in command-first and sidebar surfaces.
Output: Renderer state/UI for batch approvals and audit timeline plus command integration.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-live-agent-mode/13-CONTEXT.md
@.planning/phases/13-live-agent-mode/13-RESEARCH.md
@.planning/phases/13-live-agent-mode/13-VALIDATION.md
@.planning/phases/13-live-agent-mode/13-01-PLAN.md
@.planning/phases/13-live-agent-mode/13-02-PLAN.md
@src/renderer/App.tsx
@src/renderer/lib/commandPalette.ts
@src/renderer/components/AutomationSidebar.tsx
@src/renderer/styles/global.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build AI sidebar live-agent panel with batch approval and run controls</name>
  <read_first>
- src/renderer/App.tsx
- src/renderer/components/AutomationSidebar.tsx
- src/renderer/styles/global.css
- .planning/phases/13-live-agent-mode/13-CONTEXT.md
  </read_first>
  <files>src/renderer/App.tsx, src/renderer/components/AutomationSidebar.tsx, src/renderer/styles/global.css</files>
  <behavior>
- Test 1: panel can start a live-agent run and shows deterministic state transitions.
- Test 2: waiting-approval state renders next N-step batch with risk tags and explicit Approve/Reject controls.
- Test 3: panel exposes pause/resume/cancel controls mapped to live-agent API.
  </behavior>
  <action>In App.tsx add live-agent state slice for run status, batch approval payload, selected batch size, and control errors. Render a dedicated live-agent section in AI sidebar showing start input, current state badge, and control buttons (`Approve Batch`, `Reject Batch`, `Pause`, `Resume`, `Cancel`). Batch card must list each step intent, target context, risk tier, and expected side effect before approval. Style new controls, risk chips, and waiting-approval card states in global.css; wire through existing AutomationSidebar ai-content slot.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- App.tsx contains live-agent state handling for `waiting-approval`, `running`, `paused`, `cancelled`, `failed`, and `completed`.
- UI includes explicit `Approve Batch` and `Reject Batch` controls in waiting-approval state.
- UI includes `Pause`, `Resume`, and `Cancel` controls wired to live-agent handlers.
- `npm run lint` and `npm run typecheck` exit successfully.
  </acceptance_criteria>
  <done>Users can control live-agent execution and batch approvals directly from AI sidebar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render step audit timeline with required explainability fields</name>
  <read_first>
- src/renderer/App.tsx
- src/renderer/components/AutomationSidebarHistory.tsx
- src/renderer/styles/global.css
- src/shared/browser.ts
  </read_first>
  <files>src/renderer/App.tsx, src/renderer/styles/global.css</files>
  <behavior>
- Test 1: timeline fetches and renders audit events for active live-agent run id.
- Test 2: each entry displays planned action, risk tier, approval decision, observed result, and next rationale.
- Test 3: cancelled and failed runs retain visible partial timeline with terminal state marker.
  </behavior>
  <action>Add renderer fetch path for liveAgentGetAuditTrail using current run id and refresh on status changes. Implement timeline list component in App.tsx showing required fields in fixed order with timestamp and status tone. Add styles for timeline rows, risk badges, approval markers, terminal-state separators, and redacted payload display formatting. Keep long text truncated with expand control when needed.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- App.tsx calls live-agent audit retrieval API and binds results to timeline render state.
- Timeline row template includes action, risk tier, approval decision, observed result, and next-step rationale labels.
- Timeline remains visible after cancelled/failed terminal states with partial history preserved.
- `npm run lint` and `npm run typecheck` exit successfully.
  </acceptance_criteria>
  <done>Live-agent timeline provides explainable step-by-step visibility for trust and debugging.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add command-first live-agent controls and AI-section focus behavior</name>
  <read_first>
- src/renderer/lib/commandPalette.ts
- src/renderer/App.tsx
- .planning/phases/04-command-palette/04-CONTEXT.md
  </read_first>
  <files>src/renderer/lib/commandPalette.ts, src/renderer/App.tsx</files>
  <behavior>
- Test 1: command palette includes ai.agent.start, ai.agent.pause, ai.agent.resume, ai.agent.cancel.
- Test 2: command execution focuses ai-chat section before dispatching live-agent action.
- Test 3: command errors surface actionable guidance without breaking palette behavior.
  </behavior>
  <action>Extend BrowserCommandDeps and command registry in commandPalette.ts with live-agent dependencies and command ids `ai.agent.start`, `ai.agent.pause`, `ai.agent.resume`, and `ai.agent.cancel`. Wire handlers in App.tsx to open ai-chat section first, then invoke corresponding live-agent API calls. Ensure start command accepts optional prompt argument, falling back to panel input if absent, and report control-state errors using existing command error pattern.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- commandPalette.ts contains all four `ai.agent.*` command ids.
- App command handlers focus ai-chat section before action dispatch.
- start command supports optional inline argument with panel fallback behavior.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit successfully.
  </acceptance_criteria>
  <done>Command-first live-agent controls are fully integrated with sidebar operations and execution state.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main audit/control responses -> renderer timeline and controls | Privileged run state and audit data cross into user-facing UI |
| command palette input -> live-agent action dispatch | User command input can trigger high-impact control transitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | S | src/renderer/lib/commandPalette.ts | mitigate | Restrict dispatch to explicit `ai.agent.*` command ids with typed dependency functions |
| T-13-10 | I | src/renderer/App.tsx timeline rendering | mitigate | Render audit fields as plain text and preserve redaction markers from backend |
| T-13-11 | R | src/renderer/App.tsx control actions | mitigate | Show approval/control decisions in timeline and state badges with stable run-id context |
| T-13-12 | D | src/renderer/App.tsx live controls | mitigate | Disable incompatible controls per state and surface actionable error/retry guidance |
</threat_model>

<verification>
Run `npm run lint; npm run typecheck` after Tasks 1-2 and full `npm run lint; npm run typecheck; npm run build` after Task 3.
</verification>

<success_criteria>
1. Users can perform explicit batch approvals and pause/resume/cancel controls through sidebar and commands.
2. Timeline displays per-step explainability fields required by AI-04.
3. Command-first flow remains consistent with existing palette UX and error behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/13-live-agent-mode/13-03-SUMMARY.md`
</output>
