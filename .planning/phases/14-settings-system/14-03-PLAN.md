---
phase: 14-settings-system
plan: 03
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
files_modified:
  - src/renderer/components/SettingsPanel.tsx
  - src/renderer/App.tsx
  - src/renderer/lib/commandPalette.ts
  - src/renderer/styles/global.css
autonomous: true
requirements:
  - SET-01
  - SET-03
must_haves:
  truths:
    - Users can view and update general settings from one dedicated settings surface.
    - Users can configure cookie mode and run explicit bucketed clear-data flows from Privacy settings.
    - Users receive non-blocking repair/reset notice when settings were auto-recovered.
  artifacts:
    - path: src/renderer/components/SettingsPanel.tsx
      provides: Dedicated settings UI with General/Privacy section workflows.
    - path: src/renderer/App.tsx
      provides: Settings panel lifecycle, API calls, state hydration, and result notifications.
    - path: src/renderer/styles/global.css
      provides: Settings panel styles consistent with existing shell language.
  key_links:
    - from: src/renderer/components/SettingsPanel.tsx
      to: src/preload/index.ts
      via: UI actions call typed window.pathfinder settings APIs.
      pattern: settings
    - from: src/renderer/lib/commandPalette.ts
      to: src/renderer/App.tsx
      via: Command opens settings surface from command-first workflow.
      pattern: settings
---

<objective>
Deliver the user-facing Settings surface and wire it to the new typed settings/privacy APIs.

Purpose: Fulfill Phase 14 UX outcomes for configurable general/privacy behavior with safe destructive flows.
Output: Dedicated settings UI, command entry, and integrated repair/confirmation/result messaging.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-settings-system/14-CONTEXT.md
@.planning/phases/14-settings-system/14-RESEARCH.md
@.planning/phases/14-settings-system/14-VALIDATION.md
@.planning/phases/14-settings-system/14-01-SUMMARY.md
@.planning/phases/14-settings-system/14-02-SUMMARY.md
@src/renderer/App.tsx
@src/renderer/lib/commandPalette.ts
@src/renderer/styles/global.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build dedicated Settings panel component with General and Privacy sections</name>
  <read_first>
- src/renderer/App.tsx
- src/renderer/components/AutomationSidebar.tsx
- src/renderer/styles/global.css
- .planning/phases/14-settings-system/14-CONTEXT.md
  </read_first>
  <files>src/renderer/components/SettingsPanel.tsx, src/renderer/styles/global.css</files>
  <behavior>
- Test 1: General section contains startup mode, homepage mode/value, and downloads behavior/path controls.
- Test 2: Privacy section contains cookie mode selector and bucketed clear-data selection controls.
- Test 3: component emits typed callbacks for save-general, save-privacy, and clear-data actions.
  </behavior>
  <action>Create src/renderer/components/SettingsPanel.tsx implementing sectioned navigation (`General`, `Privacy`, `AI`, `Advanced` with AI/Advanced as non-destructive placeholders for now) and concrete form controls for D-04..D-10. Add checkbox/multi-select UI for clear-data buckets and explicit confirmation step before submit. Add accompanying styles in src/renderer/styles/global.css under a dedicated `.settings-panel*` namespace and reuse existing tokenized shell aesthetics.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/renderer/components/SettingsPanel.tsx exists and exports SettingsPanel component.
- component contains controls for startup mode, homepage mode, downloads mode/path, cookie mode, and clear-data buckets.
- component includes explicit confirmation UX for clear-data execution.
- global.css contains `.settings-panel` style namespace entries.
- `npm run typecheck` exits successfully.
  </acceptance_criteria>
  <done>Settings UI structure is complete and ready for API wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate Settings panel lifecycle and typed API interactions in App shell</name>
  <read_first>
- src/renderer/App.tsx
- src/preload/index.ts
- src/shared/browser.ts
- src/renderer/components/SettingsPanel.tsx
  </read_first>
  <files>src/renderer/App.tsx</files>
  <behavior>
- Test 1: App loads settings snapshot on open and hydrates Settings panel controls.
- Test 2: save-general/save-privacy actions call typed APIs and display deterministic success/error feedback.
- Test 3: clear-data action submits selected buckets and renders per-bucket result summary.
  </behavior>
  <action>In src/renderer/App.tsx add state and handlers for opening/closing Settings panel, fetching settings snapshot, saving general/privacy forms, and executing clear-data requests. Surface typed validation errors inline per field and show non-blocking repair notice when `repairNotice` exists. Ensure save operations do not block unrelated browser interactions and update local UI state from returned canonical snapshots.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- App.tsx invokes settings snapshot API when settings surface is opened.
- App.tsx calls save general/privacy APIs and updates local state from response snapshot.
- App.tsx renders per-bucket clear-data results after clear operation.
- repair notice from backend is displayed as non-blocking informational UI.
- `npm run lint` and `npm run typecheck` exit successfully.
  </acceptance_criteria>
  <done>Settings flows are fully wired to backend APIs and provide deterministic user feedback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add command entrypoint for settings and finalize UX polish states</name>
  <read_first>
- src/renderer/lib/commandPalette.ts
- src/renderer/App.tsx
- src/renderer/components/SettingsPanel.tsx
- src/renderer/styles/global.css
  </read_first>
  <files>src/renderer/lib/commandPalette.ts, src/renderer/App.tsx, src/renderer/styles/global.css</files>
  <behavior>
- Test 1: command palette exposes a settings command that opens the dedicated settings surface.
- Test 2: settings UI has stable loading/empty/error states for snapshot and action responses.
- Test 3: keyboard navigation remains functional for settings panel controls and command invocation.
  </behavior>
  <action>Add command palette entry (for example `settings.open`) in src/renderer/lib/commandPalette.ts and wire it in App command deps to open the settings surface. In App/styles, implement explicit loading and error state visuals for settings fetch/save/clear operations, and ensure focus management returns to prior shell context when panel closes.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- commandPalette.ts includes a settings-open command id and run handler.
- App.tsx wires command handler to open settings panel.
- settings UI renders loading and error states for fetch/save/clear actions.
- panel close restores focus without trapping keyboard input in hidden controls.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit successfully.
  </acceptance_criteria>
  <done>Settings surface is command-accessible, resilient, and production-build ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input forms -> settings/preload APIs | Potentially invalid or malicious payload values enter privileged handlers |
| destructive clear-data interaction -> persisted browser data | User-triggered destructive action can impact sensitive/local browsing state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-09 | S | src/renderer/App.tsx settings action handlers | mitigate | Bind all writes to typed API methods and reject unknown fields/client-side before submit |
| T-14-10 | T | src/renderer/components/SettingsPanel.tsx | mitigate | Validate required fields client-side and display explicit field-level errors for invalid payloads |
| T-14-11 | R | src/renderer/App.tsx clear-data result UI | mitigate | Show per-bucket result summary to preserve user-visible audit trail of destructive actions |
| T-14-12 | D | src/renderer/styles/global.css settings states | accept | UI-only degradation risk is low-impact; automated lint/typecheck/build checks catch major regressions |
</threat_model>

<verification>
Run `npm run typecheck` after Task 1, `npm run lint; npm run typecheck` after Task 2, and full suite `npm run lint; npm run typecheck; npm run build` after Task 3.
</verification>

<success_criteria>
1. Dedicated settings surface allows users to configure Phase 14 general/privacy settings.
2. Privacy clear-data and cookie mode workflows are explicit, typed, and user-confirmed.
3. Settings repair notices and validation errors are surfaced non-blockingly with stable UX states.
</success_criteria>

<output>
After completion, create `.planning/phases/14-settings-system/14-03-SUMMARY.md`
</output>