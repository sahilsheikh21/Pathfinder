---
phase: 14-settings-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/browser.ts
  - src/shared/ipc.ts
  - src/preload/index.ts
  - src/main/settingsStore.ts
  - src/main/main.ts
autonomous: true
requirements:
  - SET-01
  - SET-03
must_haves:
  truths:
    - Settings state has one typed canonical snapshot owned by main process.
    - General and privacy settings payloads are validated before persistence.
    - Corrupted settings persistence auto-recovers safely without crashing startup.
  artifacts:
    - path: src/main/settingsStore.ts
      provides: Canonical settings persistence, validation, and corruption repair logic.
    - path: src/shared/browser.ts
      provides: Shared settings contracts, clear-data request/result envelopes, and repair notice model.
    - path: src/shared/ipc.ts
      provides: IPC channels and preload API signatures for settings operations.
  key_links:
    - from: src/main/main.ts
      to: src/main/settingsStore.ts
      via: Main process initializes settings store and routes settings handlers through it.
      pattern: settingsStore
    - from: src/preload/index.ts
      to: src/shared/ipc.ts
      via: Preload wrappers invoke typed settings channel constants.
      pattern: IPC_CHANNELS
---

<objective>
Establish the Phase 14 settings foundation: typed contracts, canonical store, and secure main/preload handler wiring.

Purpose: Remove fragmented settings behavior by introducing one validated settings domain backbone.
Output: Shared settings types/channels, settings store with corruption recovery, and operational IPC/preload settings endpoints.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-settings-system/14-CONTEXT.md
@.planning/phases/14-settings-system/14-RESEARCH.md
@.planning/phases/14-settings-system/14-VALIDATION.md
@src/shared/browser.ts
@src/shared/ipc.ts
@src/preload/index.ts
@src/main/main.ts
@src/main/homeStore.ts
@src/main/llm/providerConfigStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared settings contracts and channel surface</name>
  <read_first>
- src/shared/browser.ts
- src/shared/ipc.ts
- .planning/phases/14-settings-system/14-CONTEXT.md
  </read_first>
  <files>src/shared/browser.ts, src/shared/ipc.ts</files>
  <behavior>
- Test 1: shared types define general settings, privacy settings, and canonical snapshot envelopes.
- Test 2: clear-data request and per-bucket result types are explicit and typed.
- Test 3: IPC channel constants and PathfinderApi include typed settings get/save/clear operations.
  </behavior>
  <action>Add concrete settings domain types in src/shared/browser.ts: startup mode (`restore-last-session` | `open-home` | `open-urls`), homepage mode (`home-starter` | `custom-url`), downloads mode (`ask-every-time` | `fixed-path`), cookie mode (`allow-all` | `block-third-party` | `block-all`), clear-data buckets (`history-downloads`, `cookies-site-data`, `cache-storage`, `app-settings-subset`), canonical `BrowserSettingsSnapshot`, `BrowserSettingsValidationError`, and `BrowserSettingsRepairNotice`. Extend src/shared/ipc.ts with channels and PathfinderApi methods for `settings:getSnapshot`, `settings:saveGeneral`, `settings:savePrivacy`, `settings:clearData`, and `settings:getRepairNotice`.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/shared/browser.ts contains `type BrowserCookieMode` with all three global modes.
- src/shared/browser.ts contains `interface BrowserSettingsSnapshot`.
- src/shared/browser.ts contains clear-data bucket request/result types.
- src/shared/ipc.ts contains channel constants for getSnapshot/saveGeneral/savePrivacy/clearData/getRepairNotice.
- src/shared/ipc.ts PathfinderApi includes methods with the same settings names.
- `npm run typecheck` exits successfully.
  </acceptance_criteria>
  <done>Shared settings and privacy contract surfaces are fully typed and compile-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement canonical settings store with validation and corruption recovery</name>
  <read_first>
- src/main/homeStore.ts
- src/main/llm/providerConfigStore.ts
- src/shared/browser.ts
- .planning/phases/14-settings-system/14-CONTEXT.md
  </read_first>
  <files>src/main/settingsStore.ts</files>
  <behavior>
- Test 1: valid general/privacy updates persist and return normalized snapshots.
- Test 2: invalid payloads produce typed validation errors and do not persist bad state.
- Test 3: corrupted persisted settings file is repaired to safe defaults with repair notice metadata.
  </behavior>
  <action>Create src/main/settingsStore.ts using userData JSON persistence pattern. Include deterministic defaults per D-04..D-07 and D-10 (`restore-last-session`, `home-starter`, `ask-every-time`, `allow-all`). Implement validators for custom homepage URL format, startup URL list (when mode is `open-urls`), and fixed download path non-empty checks. Persist `updatedAt` and `repairNotice` metadata. On read failure or invalid persisted shape, reset to safe defaults, set repair notice (`reason`, `repairedAt`), and continue operation.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/main/settingsStore.ts exists and exports `createSettingsStore`.
- settings defaults include restore-session startup and ask-every-time download behavior.
- store read path contains corruption fallback branch that writes repaired defaults.
- store update path returns typed validation errors for invalid URL/path payloads.
- `npm run typecheck` exits successfully.
  </acceptance_criteria>
  <done>Settings persistence is canonical, validated, and corruption-resilient.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire settings handlers in main and preload bridge</name>
  <read_first>
- src/main/main.ts
- src/preload/index.ts
- src/shared/ipc.ts
- src/main/settingsStore.ts
  </read_first>
  <files>src/main/main.ts, src/preload/index.ts</files>
  <behavior>
- Test 1: main process registers typed handlers for settings get/save/repair retrieval.
- Test 2: preload exposes matching wrappers on window.pathfinder.
- Test 3: unavailable store/failure cases return typed redacted failures without unhandled throws.
  </behavior>
  <action>Instantiate settings store in createWindow lifecycle in src/main/main.ts and register handlers for get snapshot, save general, save privacy, and get repair notice. Map thrown validation failures to typed `BrowserSettingsValidationError` envelopes and redact internal messages. In src/preload/index.ts add wrapper methods with signatures matching PathfinderApi settings methods from Task 1.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/main/main.ts contains `ipcMain.handle` registrations for all non-clear settings channels.
- src/main/main.ts initializes a settings store singleton in window bootstrap.
- src/preload/index.ts exposes settings wrappers for get/save/repair methods.
- handler failure branches return typed result envelopes instead of raw Error objects.
- `npm run lint` and `npm run typecheck` exit successfully.
  </acceptance_criteria>
  <done>Settings backend and preload bridge are operational through secure typed APIs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> main settings IPC | Untrusted settings payloads cross into privileged persistence and policy logic |
| persisted local settings file -> runtime config | Potentially corrupted/on-disk-tampered data enters startup configuration path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | T | src/main/settingsStore.ts | mitigate | Validate and normalize every update payload before persistence and reject invalid state transitions |
| T-14-02 | D | src/main/settingsStore.ts | mitigate | Corruption-safe read path repairs invalid JSON/shape and prevents startup failure |
| T-14-03 | I | src/main/main.ts settings handlers | mitigate | Redact internal errors and return typed validation envelopes over IPC |
| T-14-04 | E | src/preload/index.ts settings API | mitigate | Expose minimal allowlisted settings methods only through typed preload wrappers |
</threat_model>

<verification>
Run `npm run typecheck` after Tasks 1-2 and `npm run lint; npm run typecheck` after Task 3.
</verification>

<success_criteria>
1. Canonical settings contracts and IPC/preload APIs compile and are callable.
2. Settings persistence validates input and recovers automatically from corrupted data.
3. Main-process settings handlers provide typed success/failure envelopes for renderer consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/14-settings-system/14-01-SUMMARY.md`
</output>