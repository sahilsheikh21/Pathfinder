---
phase: 14-settings-system
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/main/privacyDataService.ts
  - src/main/settingsStore.ts
  - src/main/main.ts
  - src/shared/browser.ts
autonomous: true
requirements:
  - SET-03
  - SET-01
must_haves:
  truths:
    - Clear-data operations execute only explicit selected buckets and provide per-bucket result summaries.
    - Global cookie mode updates are persisted and applied through main-process policy wiring.
    - Privacy operations are confirmation-oriented and deterministic in success/failure reporting.
  artifacts:
    - path: src/main/privacyDataService.ts
      provides: Bucketed clear-data execution and result aggregation.
    - path: src/main/main.ts
      provides: Operational privacy handlers for clear-data and cookie policy updates.
    - path: src/main/settingsStore.ts
      provides: Persisted privacy settings and policy flags consumed by runtime handlers.
  key_links:
    - from: src/main/main.ts
      to: src/main/privacyDataService.ts
      via: IPC clear-data handler delegates selected buckets and returns per-bucket results.
      pattern: clearData
    - from: src/main/main.ts
      to: src/main/settingsStore.ts
      via: Cookie mode save path persists privacy mode before applying runtime policy.
      pattern: savePrivacy
---

<objective>
Implement executable privacy behavior: bucketed clear-data flows and global cookie mode policy handling.

Purpose: Deliver `SET-03` safely with explicit destructive-action handling and deterministic runtime outcomes.
Output: Privacy execution service, main-process privacy handlers, and persisted cookie-mode policy wiring.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-settings-system/14-CONTEXT.md
@.planning/phases/14-settings-system/14-RESEARCH.md
@.planning/phases/14-settings-system/14-VALIDATION.md
@.planning/phases/14-settings-system/14-01-SUMMARY.md
@src/main/main.ts
@src/main/settingsStore.ts
@src/shared/browser.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build bucketed privacy clear-data execution service</name>
  <read_first>
- src/main/main.ts
- src/main/settingsStore.ts
- src/shared/browser.ts
- .planning/phases/14-settings-system/14-CONTEXT.md
  </read_first>
  <files>src/main/privacyDataService.ts, src/shared/browser.ts</files>
  <behavior>
- Test 1: service accepts selected clear-data buckets and executes only those scopes.
- Test 2: service returns per-bucket status with message for success/failure.
- Test 3: service redacts internal errors in result payloads.
  </behavior>
  <action>Create src/main/privacyDataService.ts exposing `createPrivacyDataService` with `clearSelectedBuckets(request)` method. Implement bucket handlers for `history-downloads`, `cookies-site-data`, `cache-storage`, and `app-settings-subset` using Electron session clear APIs and app-store cleanup hooks. Return deterministic `ClearDataBucketResult[]` with `bucket`, `ok`, and `message` fields. Update shared result typing in src/shared/browser.ts if additional bucket-result fields are required.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/main/privacyDataService.ts exists and exports `createPrivacyDataService`.
- service implementation maps all four required clear-data buckets.
- clear-data response type includes per-bucket success/failure values.
- service code redacts error details before returning message payloads.
- `npm run typecheck` exits successfully.
  </acceptance_criteria>
  <done>Privacy clear-data execution can run safely and report bucketed outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate cookie-mode policy and clear-data handlers in main process</name>
  <read_first>
- src/main/main.ts
- src/main/privacyDataService.ts
- src/main/settingsStore.ts
- src/shared/ipc.ts
  </read_first>
  <files>src/main/main.ts, src/main/settingsStore.ts</files>
  <behavior>
- Test 1: save privacy handler persists cookie mode and returns updated snapshot.
- Test 2: clear-data handler requires selected buckets and returns per-bucket summary.
- Test 3: runtime cookie policy apply function handles allow-all/block-third-party/block-all deterministically.
  </behavior>
  <action>In src/main/main.ts instantiate privacy data service and register `settings:clearData` handler that rejects empty bucket selections with typed validation error. Add cookie-mode apply helper that maps `allow-all`, `block-third-party`, and `block-all` to Electron session cookie policy behavior (or explicit no-op with persisted policy if Electron cannot fully enforce third-party mode directly). Update src/main/settingsStore.ts privacy update path so cookie-mode save is validated and persisted before runtime apply.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/main/main.ts contains `ipcMain.handle` registration for settings clear-data channel.
- clear-data handler validates non-empty selected bucket list.
- src/main/main.ts contains explicit cookie-mode branching for all three global modes.
- src/main/settingsStore.ts persists and returns privacy cookie mode updates.
- `npm run typecheck` exits successfully.
  </acceptance_criteria>
  <done>Privacy operations and cookie mode policy are executable through main-process settings handlers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finalize typed response envelopes for privacy workflows</name>
  <read_first>
- src/shared/browser.ts
- src/main/main.ts
- src/preload/index.ts
- .planning/phases/14-settings-system/14-01-PLAN.md
  </read_first>
  <files>src/shared/browser.ts, src/main/main.ts, src/preload/index.ts</files>
  <behavior>
- Test 1: clear-data and privacy save responses carry typed success plus validation/error metadata.
- Test 2: preload wrapper signatures match updated PathfinderApi contracts.
- Test 3: handler failures return typed envelopes rather than raw exceptions.
  </behavior>
  <action>Ensure privacy operations use explicit result envelopes with `ok`, `snapshot`, `bucketResults`, and optional `validationError` fields in src/shared/browser.ts. Align main handler returns in src/main/main.ts to this envelope shape in both success and failure paths. Mirror any signature changes in src/preload/index.ts wrappers so renderer receives stable typed results.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- shared types define one canonical envelope for clear-data result and one for privacy-save result.
- main privacy handlers return those envelopes in success and failure branches.
- preload exposes wrapper methods matching the final privacy envelope signatures.
- `npm run lint` and `npm run typecheck` exit successfully.
  </acceptance_criteria>
  <done>Privacy execution APIs are stable, typed, and safe for renderer consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> main privacy operations | Untrusted requests can trigger destructive clear-data actions |
| settings policy -> Electron session behavior | Persisted cookie/privacy modes influence runtime browser data handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | T | src/main/privacyDataService.ts | mitigate | Execute only explicitly selected buckets; reject empty/unknown bucket requests |
| T-14-06 | D | src/main/main.ts clear-data handler | mitigate | Require confirmation token or explicit selected list and return per-bucket outcomes for traceability |
| T-14-07 | I | src/main/privacyDataService.ts | mitigate | Redact internal exception details and surface safe bucket-level messages only |
| T-14-08 | E | src/main/main.ts cookie policy apply | mitigate | Restrict cookie policy mutation to main process and clamp mode to allowlisted enum values |
</threat_model>

<verification>
Run `npm run typecheck` after Tasks 1-2 and `npm run lint; npm run typecheck` after Task 3.
</verification>

<success_criteria>
1. Bucketed clear-data operations execute selected scopes and return per-bucket summaries.
2. Global cookie mode save/apply logic supports allow-all, block-third-party, and block-all.
3. Privacy APIs expose deterministic typed result envelopes for renderer workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/14-settings-system/14-02-SUMMARY.md`
</output>