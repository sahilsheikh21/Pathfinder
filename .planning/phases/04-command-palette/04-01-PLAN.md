---
phase: 04-command-palette
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/lib/commandPalette.ts
autonomous: true
requirements:
  - CMD-02
must_haves:
  truths:
    - Command queries return deterministic fuzzy matches from command id/title/keywords.
    - Results include command metadata needed for palette rows and argument hints.
  artifacts:
    - path: src/renderer/lib/commandPalette.ts
      provides: Typed command model, deterministic scoring, and ranked command filtering helpers.
  key_links:
    - from: src/renderer/lib/commandPalette.ts
      to: src/renderer/lib/omnibox.ts
      via: goto/search command target resolution for consistent browser semantics.
      pattern: resolveOmniboxInput
---

<objective>
Create the command registry contract and deterministic fuzzy ranking engine that the palette UI and App integration will consume.

Purpose: Establish CMD-02 search behavior and extensible command metadata before UI wiring.
Output: Typed command utilities with predictable ranking and command factory helpers.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-command-palette/04-CONTEXT.md
@.planning/phases/04-command-palette/04-RESEARCH.md
@.planning/phases/04-command-palette/04-VALIDATION.md
@src/renderer/lib/omnibox.ts
@copilot-instructions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define command metadata contracts and deterministic fuzzy ranking</name>
  <read_first>
- src/renderer/lib/omnibox.ts
- src/shared/browser.ts
- .planning/phases/04-command-palette/04-CONTEXT.md
  </read_first>
  <files>src/renderer/lib/commandPalette.ts</files>
  <behavior>
- Test 1: Given query prefix match, command ranks above token and substring matches.
- Test 2: Given identical score tier, ranking falls back to alphabetical command title.
- Test 3: Empty query returns the full command list in stable order.
  </behavior>
  <action>Create `CommandPaletteCommand` and `CommandPaletteMatch` types with explicit fields: `id`, `title`, `description`, `argumentHint`, `keywords`, and `run`. Implement `rankCommands(commands, query)` that normalizes query to lowercase and scores each command in deterministic tiers: 0 for prefix match on `title` or `id`, 1 for token-word match in `title` or any `keywords`, 2 for substring match in `title`, `id`, or `keywords`. Return sorted matches by score asc then title asc. Export helpers so UI can render both command metadata and score.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/renderer/lib/commandPalette.ts contains exported `CommandPaletteCommand` type.
- src/renderer/lib/commandPalette.ts contains exported `rankCommands(` function.
- src/renderer/lib/commandPalette.ts contains deterministic sort logic using score tier and title fallback.
  </acceptance_criteria>
  <done>Deterministic fuzzy matching contract exists and is usable by palette UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build browser-core command factory with reusable action wiring</name>
  <read_first>
- src/renderer/lib/commandPalette.ts
- src/renderer/lib/omnibox.ts
- src/renderer/App.tsx
- .planning/phases/04-command-palette/04-CONTEXT.md
  </read_first>
  <files>src/renderer/lib/commandPalette.ts</files>
  <behavior>
- Test 1: Command factory returns executable commands for new-tab, close-tab, back, forward, reload, stop, goto, search.
- Test 2: `goto` and `search` command argument processing uses omnibox resolver semantics.
- Test 3: Returned command metadata includes descriptions and argument hints for palette rendering.
  </behavior>
  <action>Add `createBrowserCommands(deps)` in `src/renderer/lib/commandPalette.ts` that accepts concrete callback dependencies (`createTab`, `closeActiveTab`, `navigateTarget`, `back`, `forward`, `reload`, `stop`, `activeTabId`). Define command entries for `tab new`, `tab close`, `back`, `forward`, `reload`, `stop`, `goto`, and `search`. For `goto` and `search`, call `resolveOmniboxInput` with `https://duckduckgo.com/?q={query}` and route the resulting target to `navigateTarget` (for current tab). Ensure each command has explicit `description`, `argumentHint` where needed, and keyword aliases for fuzzy discovery.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/renderer/lib/commandPalette.ts exports `createBrowserCommands(`.
- src/renderer/lib/commandPalette.ts includes command ids for `tab.new`, `tab.close`, `nav.back`, `nav.forward`, `nav.reload`, `nav.stop`, `nav.goto`, and `nav.search`.
- src/renderer/lib/commandPalette.ts references `resolveOmniboxInput` for goto/search commands.
- Each command object in src/renderer/lib/commandPalette.ts includes `title`, `description`, and `keywords`.
  </acceptance_criteria>
  <done>Browser command registry is complete, typed, and reusable by component/App integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| palette input to command matcher | Untrusted user query text enters fuzzy matching and argument handling |
| renderer command registry to browser actions | Command callbacks can trigger navigation/tab lifecycle operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | src/renderer/lib/commandPalette.ts | mitigate | Restrict command set to explicit static registry; no dynamic command injection path |
| T-04-02 | D | src/renderer/lib/commandPalette.ts | mitigate | Use bounded deterministic ranking logic without regex backtracking or unbounded recursion |
| T-04-03 | E | src/renderer/lib/commandPalette.ts | mitigate | Route execution only through injected typed callbacks that map to existing safe APIs |
</threat_model>

<verification>
Run `npm run typecheck` after each task to keep utility contracts and command signatures valid.
</verification>

<success_criteria>
1. Deterministic fuzzy ranking supports CMD-02 command discovery behavior.
2. Browser command metadata and run handlers are defined with argument hints and aliases.
3. Utility module is ready for direct consumption by `CommandPalette` component and `App` integration.
</success_criteria>

<output>
After completion, create `.planning/phases/04-command-palette/04-01-SUMMARY.md`
</output>
