---
phase: 04-command-palette
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/renderer/App.tsx
autonomous: true
requirements:
  - CMD-01
  - CMD-02
must_haves:
  truths:
    - Ctrl+Shift+P and Ctrl+K open the palette while the app window is focused.
    - Executing a command runs existing browser actions in active-tab context and closes on success.
    - Failures keep the palette open and show inline error feedback for user recovery.
  artifacts:
    - path: src/renderer/App.tsx
      provides: Shortcut listener lifecycle, command execution bridge, palette integration state.
  key_links:
    - from: src/renderer/App.tsx
      to: src/renderer/components/CommandPalette.tsx
      via: controlled props for open state, query, execute, and error message.
      pattern: CommandPalette
    - from: src/renderer/App.tsx
      to: src/renderer/lib/commandPalette.ts
      via: command factory and active-tab callbacks.
      pattern: createBrowserCommands
---

<objective>
Integrate command palette into App shell with shortcut handling and active-tab command execution semantics.

Purpose: Complete CMD-01 and execution portions of CMD-02 by wiring UI and command actions into the live browser shell.
Output: End-to-end command palette flow from hotkey open to command run/error handling.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-command-palette/04-CONTEXT.md
@.planning/phases/04-command-palette/04-RESEARCH.md
@.planning/phases/04-command-palette/04-VALIDATION.md
@.planning/phases/04-command-palette/04-01-PLAN.md
@.planning/phases/04-command-palette/04-02-PLAN.md
@src/renderer/App.tsx
@copilot-instructions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire palette state and focused-window shortcut listeners in App shell</name>
  <read_first>
- src/renderer/App.tsx
- src/renderer/components/CommandPalette.tsx
- src/renderer/lib/commandPalette.ts
- .planning/phases/04-command-palette/04-CONTEXT.md
  </read_first>
  <files>src/renderer/App.tsx</files>
  <behavior>
- Test 1: Ctrl+Shift+P opens palette from focused app context.
- Test 2: Ctrl+K opens palette from focused app context.
- Test 3: Opening palette resets query to empty string and clears previous error.
  </behavior>
  <action>In `App.tsx`, add state fields `isCommandPaletteOpen`, `commandPaletteQuery`, and `commandPaletteError`. Register a window keydown listener in `useEffect` that checks `event.ctrlKey` and either `event.shiftKey && event.key.toLowerCase() === 'p'` or `event.key.toLowerCase() === 'k'`, then `preventDefault()` and open palette. Guard so shortcuts ignore editable focused targets (`INPUT`, `TEXTAREA`, `contentEditable`) to prevent accidental capture while typing. On open, set query to `''` and error to `''`. Render `<CommandPalette ... />` at shell level with controlled props.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/renderer/App.tsx contains state for `isCommandPaletteOpen`.
- src/renderer/App.tsx contains key handling for Ctrl+Shift+P and Ctrl+K.
- src/renderer/App.tsx renders `CommandPalette` component.
- src/renderer/App.tsx resets query and error values when palette opens.
  </acceptance_criteria>
  <done>Hotkey-driven palette open behavior is integrated with App lifecycle and controlled state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Connect command execution, close-on-success, and inline failure feedback</name>
  <read_first>
- src/renderer/App.tsx
- src/renderer/lib/commandPalette.ts
- src/renderer/components/CommandPalette.tsx
- src/renderer/lib/omnibox.ts
- .planning/phases/04-command-palette/04-CONTEXT.md
  </read_first>
  <files>src/renderer/App.tsx</files>
  <behavior>
- Test 1: Executing a successful command closes palette and applies browser action.
- Test 2: Failed command keeps palette open and sets inline error message.
- Test 3: Escape close path does not execute commands.
  </behavior>
  <action>Use `createBrowserCommands` from `src/renderer/lib/commandPalette.ts` to build command list in `App.tsx` with concrete callbacks mapped to existing handlers: `handleCreateTab`, close active tab via `handleCloseTab(activeTabId)`, `handleNavigate`, `handleBack`, `handleForward`, `handleReload`, and `handleStop`. Implement `handleExecuteCommand(command, query)` that awaits command run, then closes palette on success. Wrap execution in try/catch and on failure keep `isCommandPaletteOpen` true while setting `commandPaletteError` to `Command failed. Try again.`. Pass `errorMessage` and `onRequestClose` into `CommandPalette`. Ensure Escape close only toggles open state and does not call command run logic.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- src/renderer/App.tsx imports and uses `createBrowserCommands`.
- src/renderer/App.tsx contains async `handleExecuteCommand` function with try/catch.
- src/renderer/App.tsx sets error text `Command failed. Try again.` on execution error.
- src/renderer/App.tsx closes palette after successful command execution.
- src/renderer/App.tsx passes `errorMessage` prop to `CommandPalette`.
  </acceptance_criteria>
  <done>Command palette execution pipeline satisfies CMD-01/CMD-02 behavior including error recovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard shortcut input to app state | Global app key events can trigger command surface state transitions |
| command callback execution to typed API calls | Commands invoke browser mutations through renderer-to-main IPC bridge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | E | src/renderer/App.tsx | mitigate | Keep execution bound to static command registry and existing typed handlers only |
| T-04-08 | T | src/renderer/App.tsx | mitigate | Ignore shortcut capture in editable contexts to reduce unintended command invocation |
| T-04-09 | R | src/renderer/App.tsx | accept | Full command audit history is deferred to automation/history phases; inline error handling is sufficient for this phase |
</threat_model>

<verification>
Run `npm run typecheck` after shortcut wiring and full `npm run lint; npm run typecheck; npm run build` after execution integration.
</verification>

<success_criteria>
1. CMD-01 hotkey open/close behavior works in focused window for both configured shortcuts.
2. CMD-02 execution path runs browser commands with deterministic discovery and inline error recovery.
3. App integration preserves existing navigation/tab behavior while adding command-first interaction layer.
</success_criteria>

<output>
After completion, create `.planning/phases/04-command-palette/04-03-SUMMARY.md`
</output>
