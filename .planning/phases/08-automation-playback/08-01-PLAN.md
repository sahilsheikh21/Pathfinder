---
phase: 08-automation-playback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/browser.ts
  - src/shared/ipc.ts
  - src/preload/index.ts
autonomous: true
requirements:
  - AUTO-03
must_haves:
  truths:
    - Playback start requests include explicit source, policy, timeout, and variable payload semantics.
    - Renderer can start, inspect, and cancel playback only through typed preload APIs.
    - Playback failure and status contracts expose actionable context without raw secret leakage.
  artifacts:
    - path: src/shared/browser.ts
      provides: Typed playback request/result/status and step-level outcome contracts.
    - path: src/shared/ipc.ts
      provides: Playback IPC channel constants and Pathfinder API signatures.
    - path: src/preload/index.ts
      provides: Secure playback invoke methods for renderer.
  key_links:
    - from: src/preload/index.ts
      to: src/shared/ipc.ts
      via: ipcRenderer.invoke uses playback channel constants only.
      pattern: automationPlayback
---

<objective>
Define playback contracts and typed IPC/preload surface before implementing runtime execution.

Purpose: Lock compile-safe boundaries for AUTO-03 so runtime implementation can remain deterministic and secure.
Output: Shared playback type system plus typed renderer-facing playback APIs.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-automation-playback/08-CONTEXT.md
@.planning/phases/08-automation-playback/08-RESEARCH.md
@.planning/phases/08-automation-playback/08-VALIDATION.md
@src/shared/browser.ts
@src/shared/ipc.ts
@src/preload/index.ts
@copilot-instructions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define playback contract types in shared browser model</name>
  <read_first>
- src/shared/browser.ts
- .planning/phases/08-automation-playback/08-CONTEXT.md
- .planning/phases/08-automation-playback/08-RESEARCH.md
  </read_first>
  <files>src/shared/browser.ts</files>
  <behavior>
- Test 1: playback policy type supports only `stop-on-error` and `continue-on-error`.
- Test 2: playback start request can represent file-backed workflow source plus optional variable map.
- Test 3: playback failure result includes `stepId`, `seq`, `action`, `reason`, and `message` fields.
  </behavior>
  <action>In `src/shared/browser.ts`, add explicit playback contracts using concrete names and values: `AutomationPlaybackFailurePolicy = 'stop-on-error' | 'continue-on-error'`, `AutomationPlaybackSource` with `kind: 'file'` and `path`, `AutomationPlaybackStartRequest` (`source`, optional `tabId`, optional `variables`, optional `policy`, optional `defaultTimeoutMs`), `AutomationPlaybackVariablePrompt`, `AutomationPlaybackStepFailure`, `AutomationPlaybackRunSummary`, `AutomationPlaybackStartResult`, `AutomationPlaybackStatus`, and `AutomationPlaybackCancelRequest/Result`. Reuse existing recorder workflow contracts (`RecorderWorkflowDocument`, step action union) rather than introducing parallel step definitions.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- `src/shared/browser.ts` exports playback policy, request, status, and result interfaces.
- `AutomationPlaybackFailurePolicy` union contains only `stop-on-error` and `continue-on-error`.
- Playback result interfaces include actionable step context fields (`stepId`, `seq`, `action`, `reason`, `message`).
  </acceptance_criteria>
  <done>Playback contracts are compile-safe and ready for main/preload/renderer integration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add playback IPC channels and preload API methods</name>
  <read_first>
- src/shared/ipc.ts
- src/preload/index.ts
- src/shared/browser.ts
- .planning/phases/08-automation-playback/08-CONTEXT.md
  </read_first>
  <files>src/shared/ipc.ts, src/preload/index.ts</files>
  <behavior>
- Test 1: channel constants include playback start/status/cancel routes.
- Test 2: Pathfinder API exposes typed playback methods calling only those channels.
- Test 3: existing recorder and navigation channels are unchanged.
  </behavior>
  <action>Extend `src/shared/ipc.ts` with concrete playback channels `automation:playback:start`, `automation:playback:status`, and `automation:playback:cancel`. Add Pathfinder API method signatures `startAutomationPlayback(request)`, `getAutomationPlaybackStatus()`, and `cancelAutomationPlayback(request?)` using playback types from `src/shared/browser.ts`. In `src/preload/index.ts`, implement the same three methods with `ipcRenderer.invoke` and the new channel constants, preserving all existing method signatures and behavior.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- `IPC_CHANNELS` includes playback channel constants with exact names.
- `PathfinderApi` includes three typed playback methods.
- `src/preload/index.ts` invokes playback channels through typed methods.
- Existing APIs for tabs, quick search, automation bridge, and recorder still compile without signature changes.
  </acceptance_criteria>
  <done>Renderer can call playback start/status/cancel through secure typed preload APIs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer to preload playback API | Untrusted renderer input crosses into privileged playback controls |
| preload to main playback IPC | Malformed payloads may attempt policy/session spoofing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T | src/shared/browser.ts | mitigate | Use strict playback request/result unions and enumerated policy values to block tampered contract shapes |
| T-08-02 | S | src/shared/ipc.ts | mitigate | Restrict playback to explicit start/status/cancel channels and typed payloads |
| T-08-03 | E | src/preload/index.ts | mitigate | Expose minimal playback surface only; do not expose raw CDP/browser internals to renderer |
</threat_model>

<verification>
Run `npm run typecheck` after each task and confirm shared and preload imports resolve with no playback contract type errors.
</verification>

<success_criteria>
1. Shared playback contracts encode source, policy, variable prompts, and failure context.
2. Playback IPC channels and preload methods are typed and deterministic.
3. Existing non-playback renderer API surface remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/08-automation-playback/08-01-SUMMARY.md`
</output>
