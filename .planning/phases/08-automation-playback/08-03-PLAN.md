---
phase: 08-automation-playback
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - src/renderer/lib/commandPalette.ts
  - src/renderer/App.tsx
  - src/renderer/components/AutomationPlaybackPrompt.tsx
  - src/renderer/styles/global.css
autonomous: true
requirements:
  - AUTO-03
must_haves:
  truths:
    - Playback can be started and cancelled through command palette actions.
    - Required variables are collected before execution using safe prompt inputs.
    - Playback status and failure context are visible to users in command-first flow.
  artifacts:
    - path: src/renderer/lib/commandPalette.ts
      provides: Playback run/cancel commands and command wiring.
    - path: src/renderer/App.tsx
      provides: Playback request orchestration, variable-prompt lifecycle, and status synchronization.
    - path: src/renderer/components/AutomationPlaybackPrompt.tsx
      provides: Pre-run variable prompt UI with secret masking.
    - path: src/renderer/styles/global.css
      provides: Prompt and playback status styling consistent with existing shell.
  key_links:
    - from: src/renderer/App.tsx
      to: src/preload/index.ts
      via: app invokes typed playback start/status/cancel preload APIs.
      pattern: playback
    - from: src/renderer/lib/commandPalette.ts
      to: src/renderer/App.tsx
      via: command handlers dispatch playback run/cancel with typed error feedback.
      pattern: automation.playback
---

<objective>
Integrate command-first playback UX with pre-run variable prompt flow and actionable playback status feedback.

Purpose: Complete AUTO-03 user-facing behavior without introducing full automation library/sidebar scope.
Output: Playback commands, variable prompt component, and renderer playback orchestration.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-automation-playback/08-CONTEXT.md
@.planning/phases/08-automation-playback/08-RESEARCH.md
@.planning/phases/08-automation-playback/08-VALIDATION.md
@.planning/phases/07-action-recording/07-03-SUMMARY.md
@src/renderer/lib/commandPalette.ts
@src/renderer/components/CommandPalette.tsx
@src/renderer/App.tsx
@src/renderer/styles/global.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add command palette playback run and cancel actions</name>
  <read_first>
- src/renderer/lib/commandPalette.ts
- src/renderer/components/CommandPalette.tsx
- src/shared/ipc.ts
- .planning/phases/08-automation-playback/08-CONTEXT.md
  </read_first>
  <files>src/renderer/lib/commandPalette.ts</files>
  <behavior>
- Test 1: command registry includes `automation.playback.run` and `automation.playback.cancel` entries.
- Test 2: run command enforces non-empty workflow JSON file path input.
- Test 3: cancel command calls typed cancel API and preserves existing command behavior.
  </behavior>
  <action>Extend `src/renderer/lib/commandPalette.ts` with concrete commands `automation.playback.run` (argument hint `<workflow-json-path>`) and `automation.playback.cancel`. `automation.playback.run` must parse and trim command input, throw a clear error when empty, and call a renderer-provided playback-run dependency with the path value. `automation.playback.cancel` must call renderer-provided cancel dependency and report typed cancellation failures through existing command error flow.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- New playback commands are discoverable by rank/search logic.
- Run command requires explicit workflow path input and returns clear errors for invalid input.
- Existing navigation/recording command semantics remain unchanged.
  </acceptance_criteria>
  <done>Command palette can initiate and cancel playback as primary user entry points.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement playback variable prompt orchestration in App shell</name>
  <read_first>
- src/renderer/App.tsx
- src/preload/index.ts
- src/shared/browser.ts
- .planning/phases/08-automation-playback/08-CONTEXT.md
  </read_first>
  <files>src/renderer/App.tsx</files>
  <behavior>
- Test 1: playback start without variables handles `missing-variables` response by opening prompt state.
- Test 2: submitting prompt values retries playback start with collected variables.
- Test 3: playback status polling reflects running/completed/failed states in UI feedback.
  </behavior>
  <action>In `src/renderer/App.tsx`, add playback state and handlers wired to typed preload methods `startAutomationPlayback`, `getAutomationPlaybackStatus`, and `cancelAutomationPlayback`. Implement two-step flow: first start attempt with source path and no variables; if response reason is `missing-variables`, store returned variable prompt list and show prompt UI; on submit, retry start with collected values. Add lightweight status refresh loop (matching recorder status pattern) and map typed playback failures to user-visible command feedback strings including step/action context when provided.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- App state includes playback status and pending variable prompt models.
- Missing-variable response triggers prompt flow before execution starts.
- Retried start call includes submitted variable map and updates playback state.
- Typed playback failures render actionable feedback without raw stack traces.
  </acceptance_criteria>
  <done>App shell orchestrates safe pre-run variable collection and playback lifecycle status.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build variable prompt component and playback UI styling</name>
  <read_first>
- src/renderer/components/CommandPalette.tsx
- src/renderer/styles/global.css
- src/renderer/App.tsx
  </read_first>
  <files>src/renderer/components/AutomationPlaybackPrompt.tsx, src/renderer/styles/global.css</files>
  <behavior>
- Test 1: prompt renders all required variables with text vs secret input type handling.
- Test 2: secret fields are masked (`type=password`) and never echoed in status labels.
- Test 3: prompt supports submit/cancel actions and is keyboard accessible.
  </behavior>
  <action>Create `src/renderer/components/AutomationPlaybackPrompt.tsx` as a focused modal/panel component that accepts required variable definitions (`name`, `prompt`, `secret`) and emits `onSubmit(values)`/`onCancel()`. Render secret variables with `input type='password'`, text variables with `input type='text'`, and enforce required non-empty submission for each variable before submit. Add corresponding CSS blocks in `src/renderer/styles/global.css` for backdrop, panel, field rows, and action buttons using existing token variables; include a compact playback status pill style for running/failed/completed states in browser chrome.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- New prompt component exists and is integrated into App flow.
- Secret variables are visually masked and not rendered in plain text labels.
- Submit and cancel controls are functional and keyboard reachable.
- Lint, typecheck, and build pass with the new component/style additions.
  </acceptance_criteria>
  <done>User can safely provide required variables and observe playback progress/failure feedback in command-first flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command palette input to playback start | User-provided path and values are untrusted renderer input |
| variable prompt UI to preload request | Secret/text values cross renderer-to-main execution boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | T | src/renderer/App.tsx | mitigate | Route all playback requests through typed APIs and display typed validation errors for malformed input |
| T-08-09 | I | src/renderer/components/AutomationPlaybackPrompt.tsx | mitigate | Render secret fields with masked input and avoid echoing entered values in labels/messages |
| T-08-10 | R | src/renderer/lib/commandPalette.ts | mitigate | Keep command-triggered playback actions explicit and map results to deterministic feedback strings |
</threat_model>

<verification>
Run `npm run typecheck` after Tasks 1 and 2, then `npm run lint; npm run typecheck; npm run build` after Task 3.
</verification>

<success_criteria>
1. Playback run/cancel commands are available in command palette.
2. Required variables are collected before playback using safe prompt UX.
3. Playback status and failures are surfaced with actionable, non-sensitive feedback.
</success_criteria>

<output>
After completion, create `.planning/phases/08-automation-playback/08-03-SUMMARY.md`
</output>
