---
phase: 08-automation-playback
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/main/automationPlayback.ts
  - src/main/cdpBridge.ts
  - src/main/main.ts
autonomous: true
requirements:
  - AUTO-03
must_haves:
  truths:
    - Playback performs preflight validation before any browser step runs.
    - Step execution is deterministic by `seq` with explicit timeout and failure policy behavior.
    - Playback runtime acquires and releases the automation bridge lock safely.
  artifacts:
    - path: src/main/automationPlayback.ts
      provides: Main-process playback manager with validation, run lifecycle, and policy handling.
    - path: src/main/cdpBridge.ts
      provides: Bridge helper for executing actions on a bound page under session lock.
    - path: src/main/main.ts
      provides: Playback manager instantiation, IPC handlers, and lifecycle cleanup wiring.
  key_links:
    - from: src/main/main.ts
      to: src/main/automationPlayback.ts
      via: ipcMain handlers route playback start/status/cancel to manager methods.
      pattern: playback
    - from: src/main/automationPlayback.ts
      to: src/main/cdpBridge.ts
      via: playback start acquires owner lock and executes actions through bound page helper.
      pattern: automation-engine
---

<objective>
Implement the main-process playback engine with preflight validation, timeout controls, and stop/continue failure policies.

Purpose: Deliver AUTO-03 execution reliability on top of existing Phase 6 bridge and Phase 7 workflow schema.
Output: Playback runner module, bridge execution helper, and fully wired playback IPC handlers.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-automation-playback/08-CONTEXT.md
@.planning/phases/08-automation-playback/08-RESEARCH.md
@.planning/phases/08-automation-playback/08-VALIDATION.md
@.planning/phases/07-action-recording/07-02-SUMMARY.md
@src/main/main.ts
@src/main/cdpBridge.ts
@src/main/browserRuntime.ts
@src/main/actionRecorder.ts
@src/shared/browser.ts
@src/shared/ipc.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement playback runner preflight and deterministic step execution</name>
  <read_first>
- src/main/actionRecorder.ts
- src/main/cdpBridge.ts
- src/shared/browser.ts
- .planning/phases/08-automation-playback/08-CONTEXT.md
  </read_first>
  <files>src/main/automationPlayback.ts</files>
  <behavior>
- Test 1: malformed workflow source or non-monotonic `seq` returns preflight failure and does not start run.
- Test 2: missing required variables returns prompt payload before any step executes.
- Test 3: stop policy halts on first failing step; continue policy executes remaining steps and reports partial failure summary.
  </behavior>
  <action>Create `src/main/automationPlayback.ts` with a playback manager exposing concrete methods `start(request)`, `getStatus()`, and `cancel(request?)`. Implement preflight checks for `version === 1`, supported actions (`navigate`, `click`, `type`, `wait`), and strict ascending `seq`. Resolve variable placeholders from `request.variables`; when required keys are missing, return `ok: false` with `reason: 'missing-variables'` and a `requiredVariables` list. Execute steps in `seq` order with deterministic timeout policy: default timeout `15000ms`, min clamp `250ms`, max clamp `120000ms`, and per-step `timeoutMs` override within clamp bounds. Map actions to explicit Playwright operations: `navigate -> page.goto(url, { waitUntil: 'domcontentloaded', timeout })`, `click -> page.click(selector, { timeout })`, `type -> page.fill(selector, value, { timeout })`, `wait/navigation -> page.waitForLoadState('domcontentloaded', { timeout })`, `wait/selector -> page.waitForSelector(selector, { timeout })`.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- `src/main/automationPlayback.ts` exists and exports playback manager factory/types used by main process.
- Preflight rejects invalid version/action/seq and returns typed failure reason.
- Missing variable path returns prompt metadata and does not mark run as started.
- Stop/continue policy outcomes are distinguishable in returned run summary.
  </acceptance_criteria>
  <done>Playback engine can validate and execute workflow steps deterministically with typed policy outcomes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend CDP bridge with bound-page execution helper for playback</name>
  <read_first>
- src/main/cdpBridge.ts
- src/main/automationPlayback.ts
- .planning/phases/06-cdp-integration/06-CONTEXT.md
  </read_first>
  <files>src/main/cdpBridge.ts</files>
  <behavior>
- Test 1: playback acquires bridge lock with owner `automation-engine` and receives busy response if lock is occupied.
- Test 2: playback can execute callback against session-bound page only.
- Test 3: disconnect/shutdown clears helper access and prevents stale execution.
  </behavior>
  <action>Update `src/main/cdpBridge.ts` by adding a concrete helper method for bound-page execution (for example `withConnectedPage(sessionId, callback)`), keeping main-process-only access. The helper must verify session ownership, resolve the page corresponding to bound `tabId`, and run the callback under current session lock. Return typed `invalid-session` or `missing-target` failures when the session/page is unavailable. Preserve existing `connect`, `disconnect`, `getStatus`, and `shutdown` behavior for recorder/manual bridge consumers.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- Bridge exposes a typed bound-page execution helper callable from playback manager.
- Helper enforces current session ownership and bound tab targeting.
- Existing bridge methods still compile and keep prior semantics.
  </acceptance_criteria>
  <done>Playback manager can execute browser actions through Phase 6 lock semantics safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire playback manager and IPC handlers into application lifecycle</name>
  <read_first>
- src/main/main.ts
- src/main/automationPlayback.ts
- src/main/cdpBridge.ts
- src/shared/ipc.ts
  </read_first>
  <files>src/main/main.ts</files>
  <behavior>
- Test 1: IPC handlers respond for playback start/status/cancel channels with typed payloads.
- Test 2: tab close and app shutdown cleanly stop active playback.
- Test 3: playback start with busy bridge lock returns typed busy reason.
  </behavior>
  <action>In `src/main/main.ts`, instantiate playback manager after bridge/runtime creation, passing concrete dependencies (`cdpBridge`, `browserRuntime` target resolver, and optional tab-close hook). Register handlers for `automation:playback:start`, `automation:playback:status`, and `automation:playback:cancel` channels and return typed responses from shared contracts. Wire lifecycle cleanup by cancelling/stopping playback in `before-quit` and on bound-tab close events. Ensure playback handlers do not throw uncaught errors; convert runtime exceptions into typed failure payloads with `stepId/seq/action` context when applicable.</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- `src/main/main.ts` contains playback manager initialization and three playback IPC handlers.
- Playback lifecycle cleanup is present in app shutdown and target-loss paths.
- Lint, typecheck, and build pass after wiring changes.
  </acceptance_criteria>
  <done>Main-process playback runtime is reachable, typed, and lifecycle-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer playback request to main runner | Untrusted workflow source/variables cross into privileged automation execution |
| playback runner to CDP bridge | Execution depends on lock/session boundary and target-page resolution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | T | src/main/automationPlayback.ts | mitigate | Preflight-validate version/actions/seq and reject malformed workflow payloads before execution |
| T-08-05 | D | src/main/automationPlayback.ts | mitigate | Enforce timeout clamps and deterministic policy handling to prevent unbounded waits |
| T-08-06 | S | src/main/cdpBridge.ts | mitigate | Verify session ID and bound tab ownership in bound-page helper before action execution |
| T-08-07 | I | src/main/automationPlayback.ts | mitigate | Redact secret variable values from errors/log payloads and return only safe failure context |
</threat_model>

<verification>
Run `npm run typecheck` after Tasks 1 and 2, then `npm run lint; npm run typecheck; npm run build` after Task 3.
</verification>

<success_criteria>
1. Playback preflight enforces schema, variable, and ordering guardrails before run start.
2. Step execution honors deterministic timeout controls and stop/continue policy.
3. Playback runtime is fully wired through typed IPC with safe bridge ownership behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/08-automation-playback/08-02-SUMMARY.md`
</output>
