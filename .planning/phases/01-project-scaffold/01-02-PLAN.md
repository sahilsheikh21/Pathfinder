---
phase: 01-project-scaffold
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/renderer/styles/tokens.css
  - src/renderer/styles/global.css
  - src/renderer/components/FrostedSurface.tsx
  - src/renderer/App.tsx
  - src/renderer/theme.ts
  - eslint.config.js
  - electron-builder.yml
  - .github/workflows/ci.yml
  - resources/icons/.gitkeep
autonomous: true
requirements: []
must_haves:
  truths:
    - Renderer shell uses centralized Apple-style design tokens rather than hardcoded style values.
    - Scaffold includes lint, typecheck, and build automation suitable for CI execution.
    - Packaging baseline is present for Windows installer work in later phases.
  artifacts:
    - path: src/renderer/styles/tokens.css
      provides: Theme token definitions for light, dark, and system fallback modes.
    - path: src/renderer/components/FrostedSurface.tsx
      provides: Frosted card primitive demonstrating blur, radius, and border tokens.
    - path: .github/workflows/ci.yml
      provides: Build quality workflow for lint, typecheck, and build commands.
    - path: electron-builder.yml
      provides: Packaging baseline for Windows target.
  key_links:
    - from: src/renderer/App.tsx
      to: src/renderer/theme.ts
      via: Theme mode state and class toggle wiring.
      pattern: import.*theme
    - from: .github/workflows/ci.yml
      to: package.json
      via: CI steps call npm scripts defined in scaffold.
      pattern: npm run (lint|typecheck|build)
---

<objective>
Apply visual and delivery foundations required to make phase 1 scaffold production-oriented.

Purpose: Ensure the initial shell already reflects Pathfinder design direction and verification discipline, reducing rework in later browser phases.
Output: Tokenized renderer foundation, reusable frosted UI primitive, CI checks, and packaging baseline config.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffold/01-01-PLAN.md
@.planning/phases/01-project-scaffold/01-UI-SPEC.md
@.planning/phases/01-project-scaffold/01-VALIDATION.md
@copilot-instructions.md

<interfaces>
From src/renderer/theme.ts (to be created in this plan):
- ThemeMode type with values light, dark, system.
- applyTheme helper that updates document root theme class.
- getInitialTheme helper reading stored preference with system fallback.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build tokenized Apple-style renderer shell baseline</name>
  <read_first>
- src/renderer/App.tsx
- .planning/phases/01-project-scaffold/01-UI-SPEC.md
- .planning/phases/01-project-scaffold/01-01-PLAN.md
  </read_first>
  <files>src/renderer/styles/tokens.css, src/renderer/styles/global.css, src/renderer/components/FrostedSurface.tsx, src/renderer/theme.ts, src/renderer/App.tsx</files>
  <behavior>
- Test 1: App renders using CSS variable tokens for color, spacing, radius, and blur.
- Test 2: Theme mode toggles between light, dark, and system classes without reload.
- Test 3: Frosted surface primitive renders with backdrop blur and tokenized border values.
  </behavior>
  <action>Create tokens.css defining CSS variables for background layers, text roles, accent roles, radius scale, spacing scale, and shadow levels for both light and dark themes. Create global.css applying baseline typography, root sizing, and focus-visible styles driven by tokens. Create FrostedSurface.tsx component that wraps children in a frosted card style using tokenized blur, border, and elevation classes. Create theme.ts with ThemeMode type, localStorage key pathfinder.themeMode, applyTheme(mode), and getInitialTheme() helpers with system fallback via matchMedia. Update App.tsx to import global.css, render a scaffold shell with FrostedSurface, and include a temporary theme mode selector wired to theme helpers.</action>
  <verify>
    <automated>npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- src/renderer/styles/tokens.css contains :root token declarations for color and radius variables.
- src/renderer/styles/tokens.css contains a dark theme selector with overridden token values.
- src/renderer/components/FrostedSurface.tsx contains backdrop-filter style usage.
- src/renderer/theme.ts exports ThemeMode, applyTheme, and getInitialTheme.
- src/renderer/App.tsx imports global.css and renders a theme selector control.
  </acceptance_criteria>
  <done>Renderer shell reflects Apple-style baseline tokens and runtime theme behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add scaffold quality and packaging automation baseline</name>
  <read_first>
- package.json
- .planning/phases/01-project-scaffold/01-VALIDATION.md
- .planning/ROADMAP.md
  </read_first>
  <files>eslint.config.js, .github/workflows/ci.yml, electron-builder.yml, resources/icons/.gitkeep</files>
  <action>Create eslint.config.js with TypeScript and React renderer lint rules, ignoring dist and release output folders. Create .github/workflows/ci.yml with a workflow on push and pull_request that uses Node LTS, installs dependencies with npm ci, then runs npm run lint, npm run typecheck, and npm run build in sequence. Create electron-builder.yml with appId com.pathfinder.browser, productName Pathfinder, files includes for dist bundles, and win target nsis. Create resources/icons directory with .gitkeep placeholder so packaging paths resolve in CI.</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- eslint.config.js exists and includes TypeScript parser configuration.
- .github/workflows/ci.yml includes lint, typecheck, and build steps.
- electron-builder.yml contains appId com.pathfinder.browser and win target nsis.
- resources/icons/.gitkeep exists.
  </acceptance_criteria>
  <done>Scaffold includes repeatable quality gates and packaging foundation for Windows distribution work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer style/runtime to user environment | User preferences and theme state influence rendered surface and stored values |
| CI workflow to repository | Automated workflow executes repository scripts on each change |
| packaging config to release artifacts | Build metadata controls what files are distributed in installers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | T | src/renderer/theme.ts | mitigate | Restrict stored theme values to light, dark, system and fallback on invalid values |
| T-01-06 | I | src/renderer/styles/tokens.css | accept | Token values are non-sensitive and do not expose secrets; visual disclosure risk is low |
| T-01-07 | E | .github/workflows/ci.yml | mitigate | Run lint and typecheck gates on every PR to reduce unsafe script drift |
| T-01-08 | T | electron-builder.yml | mitigate | Lock appId and include patterns explicitly to prevent accidental packaging of unintended files |
</threat_model>

<verification>
Run npm run lint, npm run typecheck, and npm run build after completing both tasks. Confirm CI workflow references the same script names and packaging config parses without syntax errors.
</verification>

<success_criteria>
1. Design token files and frosted primitive are present and used by renderer shell.
2. Theme mode logic supports light, dark, and system behavior through typed helper functions.
3. CI and packaging configuration files exist and align with scaffold scripts.
</success_criteria>

<output>
After completion, create .planning/phases/01-project-scaffold/01-02-SUMMARY.md
</output>
