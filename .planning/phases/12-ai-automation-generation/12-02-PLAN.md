---
phase: 12-ai-automation-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/renderer/App.tsx
  - src/renderer/lib/commandPalette.ts
  - src/renderer/components/AutomationSidebar.tsx
  - src/renderer/styles/global.css
autonomous: true
requirements:
  - AI-03
  - AI-05
must_haves:
  truths:
    - User can trigger automation draft generation and cancellation from command palette with visible progress states.
    - User can preview and edit generated draft workflow before approval.
    - No generated draft is saved or executed until user explicitly approves Save Draft or Save and Run.
  artifacts:
    - path: src/renderer/App.tsx
      provides: AI sidebar generation state machine, preview editor, and approval action wiring.
    - path: src/renderer/lib/commandPalette.ts
      provides: ai.automation.generate and ai.automation.cancel command entries.
    - path: src/renderer/styles/global.css
      provides: visual states for generation progress, draft preview, and approval controls.
  key_links:
    - from: src/renderer/App.tsx
      to: src/preload/index.ts
      via: window.pathfinder aiAutomationGenerate/Cancel/GetStatus methods.
      pattern: aiAutomation
    - from: src/renderer/App.tsx
      to: src/main/automationLibraryStore.ts
      via: approval actions call automationLibraryUpsert then optional automationLibraryRun.
      pattern: automationLibrary
---

<objective>
Deliver the user-facing Phase 12 workflow: command-first generation controls and AI sidebar preview/edit/approve experience.

Purpose: Complete AI-03 and AI-05 by making generation usable, cancellable, and explicitly user-approved before persistence/execution.
Output: Renderer state/UI, command palette entries, and approval routing to existing library/run paths.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-ai-automation-generation/12-CONTEXT.md
@.planning/phases/12-ai-automation-generation/12-RESEARCH.md
@.planning/phases/12-ai-automation-generation/12-01-PLAN.md
@src/renderer/App.tsx
@src/renderer/lib/commandPalette.ts
@src/renderer/components/AutomationSidebar.tsx
@src/renderer/components/AutomationPlaybackPrompt.tsx
@src/renderer/styles/global.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build AI sidebar generation panel with draft preview/edit and approval actions</name>
  <read_first>
- src/renderer/App.tsx
- src/renderer/components/AutomationSidebar.tsx
- src/renderer/components/AutomationPlaybackPrompt.tsx
- src/renderer/styles/global.css
- .planning/phases/12-ai-automation-generation/12-CONTEXT.md
  </read_first>
  <files>src/renderer/App.tsx, src/renderer/components/AutomationSidebar.tsx, src/renderer/styles/global.css</files>
  <behavior>
- Test 1: user can submit prompt + optional constraints and see deterministic generation states.
- Test 2: successful result renders structured step preview editor and optional raw JSON advanced view.
- Test 3: approval actions enforce explicit Save Draft / Save and Run / Discard semantics.
  </behavior>
  <action>In src/renderer/App.tsx add generation state and handlers: prompt text, optional constraints (`targetUrl`, `objective`, `variables`, `notes`), operation status, draft payload, validation warnings, and failure message. Render an AI sidebar section for generation controls with buttons for Generate and Cancel. Add draft preview region with step list editing (action-specific fields), JSON toggle for advanced edits, and explicit action buttons `Save Draft`, `Save and Run`, `Discard`. On `Save Draft`, call `window.pathfinder.automationLibraryUpsert` with imported origin and generated-workflow metadata. On `Save and Run`, perform save first then call `window.pathfinder.automationLibraryRun` for the saved id. `Discard` must clear in-memory draft state only.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/renderer/App.tsx contains generation statuses including `generating`, `validating`, `ready`, `failed`, and `cancelled`.
- App UI includes explicit buttons labeled `Save Draft`, `Save and Run`, and `Discard`.
- App calls `automationLibraryUpsert` before invoking run path in Save and Run handler.
- Discard flow clears draft state without invoking save or run APIs.
- `npm run lint` and `npm run typecheck` exit successfully.
  </acceptance_criteria>
  <done>AI sidebar provides full preview/edit/approve generation UX with explicit approval gating.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add command palette generation and cancel commands with AI section focus</name>
  <read_first>
- src/renderer/lib/commandPalette.ts
- src/renderer/App.tsx
- .planning/phases/04-command-palette/04-CONTEXT.md
  </read_first>
  <files>src/renderer/lib/commandPalette.ts, src/renderer/App.tsx</files>
  <behavior>
- Test 1: command palette registers ai.automation.generate and ai.automation.cancel command ids.
- Test 2: command execution always opens/focuses ai-chat section before running generation action.
- Test 3: generate command supports prompt argument fallback to panel prompt state.
  </behavior>
  <action>Extend BrowserCommandDeps and command registry in src/renderer/lib/commandPalette.ts with `generateAutomationFromAi` and `cancelAutomationGeneration` dependencies. Add command ids `ai.automation.generate` (argumentHint `[prompt]`) and `ai.automation.cancel`. In src/renderer/App.tsx wire command handlers that call `openSidebarSectionFromCommand('ai-chat')` first, then invoke generation/cancel functions. For empty inline prompt in command execution, reuse panel prompt value and focus panel input if still empty.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/renderer/lib/commandPalette.ts contains command ids `ai.automation.generate` and `ai.automation.cancel`.
- App command wiring opens ai-chat section before calling generation/cancel handlers.
- Generate command path handles optional inline prompt and panel fallback behavior.
- Command failures show actionable error without closing command palette unexpectedly.
- `npm run lint` and `npm run typecheck` exit successfully.
  </acceptance_criteria>
  <done>Command-first generation/cancel controls match AI-05 interaction requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Poll generation status and enforce revalidation before approval actions</name>
  <read_first>
- src/renderer/App.tsx
- src/shared/browser.ts
- .planning/phases/12-ai-automation-generation/12-VALIDATION.md
  </read_first>
  <files>src/renderer/App.tsx, src/renderer/styles/global.css</files>
  <behavior>
- Test 1: renderer polls aiAutomationGetStatus and updates UI state across lifecycle transitions.
- Test 2: approval actions revalidate edited draft payload before save/run.
- Test 3: cancel command and panel action both transition UI state to cancelled with retry-ready prompt.
  </behavior>
  <action>In src/renderer/App.tsx integrate `aiAutomationGetStatus` into existing periodic refresh loop and map backend state to panel status tone/message. Before Save Draft or Save and Run, run local draft validation guard (required fields, action whitelist, ascending seq) and block approval if invalid with clear inline guidance. Add retry path that preserves prompt/constraints and resets status to idle. Update src/renderer/styles/global.css with explicit styles for progress badges, validation warning blocks, and disabled/active approval action states.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- App refresh loop calls `aiAutomationGetStatus` and updates generation state display.
- Save actions perform validation checks before calling persistence/run APIs.
- Cancel flow sets `cancelled` status and leaves prompt inputs intact for retry.
- CSS includes dedicated generation status and warning/approval styles.
- `npm run lint`, `npm run typecheck`, and `npm run build` exit successfully.
  </acceptance_criteria>
  <done>Generation UX supports reliable status, cancellation, and approval-safe execution routing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user prompt/editor input -> generation API | Untrusted user content and edits are sent to privileged generation/persistence pathways |
| generated draft -> save/run actions | Draft becomes executable workflow after explicit approval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | T | src/renderer/App.tsx draft editor | mitigate | Revalidate edited draft shape/action whitelist before Save Draft or Save and Run |
| T-12-06 | S | src/renderer/lib/commandPalette.ts | mitigate | Restrict generation/cancel to explicit command ids and active ai-chat section focus flow |
| T-12-07 | R | src/renderer/App.tsx approval handlers | mitigate | Keep explicit approval actions and deterministic status messages for each transition |
| T-12-08 | D | src/renderer/App.tsx status/cancel UX | mitigate | Expose cancel in both panel and command surfaces with retry-ready state preservation |
</threat_model>

<verification>
Run `npm run lint; npm run typecheck` after Tasks 1-2 and `npm run lint; npm run typecheck; npm run build` after Task 3.
</verification>

<success_criteria>
1. Users can trigger generation and cancellation from command palette with visible deterministic progress.
2. Users can preview/edit generated drafts in AI sidebar and must explicitly approve before save/run.
3. Save and Run path always saves first and only executes validated approved drafts.
</success_criteria>

<output>
After completion, create `.planning/phases/12-ai-automation-generation/12-02-SUMMARY.md`
</output>
