---
phase: 12-ai-automation-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/browser.ts
  - src/shared/ipc.ts
  - src/main/llm/automationGenerationService.ts
  - src/main/main.ts
  - src/preload/index.ts
autonomous: true
requirements:
  - AI-03
must_haves:
  truths:
    - Natural-language generation requests produce a typed candidate workflow draft.
    - Candidate workflow drafts are schema-valid before they can be previewed in renderer.
    - Generation exposes deterministic status and cancellation through typed APIs.
  artifacts:
    - path: src/main/llm/automationGenerationService.ts
      provides: Main-process orchestration for generate/cancel/status and schema normalization.
    - path: src/shared/browser.ts
      provides: Shared request/result/status contracts for AI automation generation.
    - path: src/shared/ipc.ts
      provides: Typed IPC channels and PathfinderApi signatures for generation APIs.
    - path: src/preload/index.ts
      provides: Secure renderer bridge wrappers for generation APIs.
  key_links:
    - from: src/main/llm/automationGenerationService.ts
      to: src/main/llm/llmAdapterService.ts
      via: Provider-neutral LLM generation calls with typed failure mapping.
      pattern: llmGenerate
    - from: src/main/main.ts
      to: src/main/llm/automationGenerationService.ts
      via: ipcMain handlers for generate/cancel/status.
      pattern: aiAutomation
---

<objective>
Build the main-process and contract foundation for AI automation generation, including strict workflow normalization/validation before preview.

Purpose: Deliver a safe and typed generation backend that enforces AI-03 before renderer UX and command flows are added.
Output: Shared contracts, IPC/preload bridge, and generation orchestration service with status/cancel semantics.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-ai-automation-generation/12-CONTEXT.md
@.planning/phases/12-ai-automation-generation/12-RESEARCH.md
@.planning/phases/12-ai-automation-generation/12-VALIDATION.md
@src/shared/browser.ts
@src/shared/ipc.ts
@src/main/llm/llmAdapterService.ts
@src/main/automationPlayback.ts
@src/main/main.ts
@src/preload/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define shared generation contracts and IPC channel surface</name>
  <read_first>
- src/shared/browser.ts
- src/shared/ipc.ts
- .planning/phases/12-ai-automation-generation/12-CONTEXT.md
  </read_first>
  <files>src/shared/browser.ts, src/shared/ipc.ts</files>
  <behavior>
- Test 1: shared types represent generate request, normalized draft result, status states, and cancel payloads.
- Test 2: IPC channels include aiAutomationGenerate, aiAutomationCancel, and aiAutomationGetStatus.
- Test 3: PathfinderApi exposes typed methods for all generation channels with no optional-call ambiguity.
  </behavior>
  <action>Add concrete shared models in src/shared/browser.ts: `AIAutomationGenerateRequest`, `AIAutomationGenerateConstraints`, `AIAutomationGeneratedDraft`, `AIAutomationGenerateResult`, `AIAutomationGenerationState` (`idle|generating|validating|ready|failed|cancelled`), `AIAutomationStatusResult`, and `AIAutomationCancelResult`. Extend src/shared/ipc.ts with channel constants `aiAutomationGenerate`, `aiAutomationCancel`, `aiAutomationGetStatus` and required PathfinderApi signatures for these calls. Keep payload fields concrete: prompt text, optional targetUrl/objective/variables/notes constraints, normalized `RecorderWorkflowDocument` draft, warnings array, and typed failure reason/message/retryable/action.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/shared/browser.ts contains `interface AIAutomationGenerateRequest`.
- src/shared/browser.ts contains `type AIAutomationGenerationState = 'idle' | 'generating' | 'validating' | 'ready' | 'failed' | 'cancelled'`.
- src/shared/ipc.ts contains `aiAutomationGenerate`, `aiAutomationCancel`, and `aiAutomationGetStatus` channel constants.
- src/shared/ipc.ts PathfinderApi contains `aiAutomationGenerate`, `aiAutomationCancel`, and `aiAutomationGetStatus` methods.
- `npm run typecheck` exits successfully.
  </acceptance_criteria>
  <done>Contract and IPC surfaces are complete and ready for service wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement automation generation orchestration service with strict draft normalization</name>
  <read_first>
- src/main/llm/llmAdapterService.ts
- src/main/automationPlayback.ts
- src/main/actionRecorder.ts
- src/shared/browser.ts
  </read_first>
  <files>src/main/llm/automationGenerationService.ts</files>
  <behavior>
- Test 1: generate operation transforms model output into a normalized `RecorderWorkflowDocument` draft using supported actions only.
- Test 2: invalid or unsupported output returns typed failure (`invalid-draft` / `unsupported-intent`) before renderer preview.
- Test 3: service exposes `getStatus()` and `cancel()` with deterministic state transitions and operation ids.
  </behavior>
  <action>Create `src/main/llm/automationGenerationService.ts` exporting `createAutomationGenerationService`. Implement methods `generate(request)`, `cancel(request?)`, and `getStatus()`. In generate flow: set status to `generating`, call `llmAdapterService.generate`, parse model output into candidate JSON, normalize into `RecorderWorkflowDocument` with `version: 1`, monotonic `seq`, and allowed actions (`navigate`, `click`, `type`, `wait`) only, then validate before returning `ok: true`. Map malformed output and unsupported intents to typed failure payloads with actionable `userAction` hints. Implement one active operation guard and cancellation token flag that transitions status to `cancelled` and returns a typed cancel result.
</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- New file src/main/llm/automationGenerationService.ts exists and exports `createAutomationGenerationService`.
- File contains public methods for generate, cancel, and status retrieval.
- File enforces action whitelist (`navigate`, `click`, `type`, `wait`) in draft normalization logic.
- File returns typed failure for invalid draft output before success payload is returned.
- `npm run typecheck` exits successfully.
  </acceptance_criteria>
  <done>Main-process service can generate schema-valid candidate drafts with status/cancel support.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire main-process handlers and preload bridge for generation APIs</name>
  <read_first>
- src/main/main.ts
- src/preload/index.ts
- src/shared/ipc.ts
- src/main/llm/automationGenerationService.ts
  </read_first>
  <files>src/main/main.ts, src/preload/index.ts</files>
  <behavior>
- Test 1: main registers handlers for aiAutomationGenerate, aiAutomationCancel, and aiAutomationGetStatus.
- Test 2: preload exposes typed wrappers on window.pathfinder for those generation APIs.
- Test 3: unavailable-service fallback returns typed failure envelope instead of throwing renderer runtime errors.
  </behavior>
  <action>In src/main/main.ts instantiate automation generation service near existing LLM services and register ipcMain handlers for `IPC_CHANNELS.aiAutomationGenerate`, `IPC_CHANNELS.aiAutomationCancel`, and `IPC_CHANNELS.aiAutomationGetStatus`. Use typed fallback envelope if service cannot initialize. In src/preload/index.ts add invoke wrappers `aiAutomationGenerate`, `aiAutomationCancel`, and `aiAutomationGetStatus` on `window.pathfinder` API, matching shared signatures exactly.
</action>
  <verify>
    <automated>npm run lint; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/main/main.ts contains `ipcMain.handle` registrations for all three aiAutomation channels.
- src/preload/index.ts exposes `aiAutomationGenerate`, `aiAutomationCancel`, and `aiAutomationGetStatus` wrappers.
- Generation handler paths return typed objects for success and failure cases.
- `npm run lint` and `npm run typecheck` exit successfully.
  </acceptance_criteria>
  <done>Generation backend is reachable through typed IPC/preload APIs and ready for renderer integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> main generation IPC | Untrusted prompt text and constraints cross into privileged orchestration code |
| LLM output -> workflow draft | Untrusted model output is converted into executable workflow artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | T | src/main/llm/automationGenerationService.ts | mitigate | Enforce strict action whitelist and schema normalization before returning draft |
| T-12-02 | E | src/main/main.ts IPC handlers | mitigate | Keep generation execution in main process and expose typed minimal IPC methods only |
| T-12-03 | D | src/main/llm/automationGenerationService.ts | mitigate | Implement explicit cancel flow and single active operation guard to bound long-running calls |
| T-12-04 | I | src/preload/index.ts wrappers | mitigate | Return redacted typed failure envelopes; do not leak raw provider/internal stack details |
</threat_model>

<verification>
Run `npm run typecheck` after Tasks 1-2 and `npm run lint; npm run typecheck` after Task 3.
</verification>

<success_criteria>
1. Typed generation contracts and IPC/preload methods exist and compile.
2. Main service always returns schema-valid drafts or typed failures before renderer preview.
3. Generation status/cancel behavior is deterministic and reachable from renderer.
</success_criteria>

<output>
After completion, create `.planning/phases/12-ai-automation-generation/12-01-SUMMARY.md`
</output>
