---
phase: 10-llm-adapter-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/browser.ts
  - src/shared/ipc.ts
  - src/preload/index.ts
autonomous: true
requirements:
  - AI-01
must_haves:
  truths:
    - Shared contracts define provider-neutral config, capability, request, response, and error envelopes for one cloud and one local provider.
    - Renderer can invoke adapter config and validation operations only through typed preload APIs.
    - IPC channels for LLM adapter operations are explicit and deterministic.
  artifacts:
    - path: src/shared/browser.ts
      provides: Typed LLM adapter domain contracts and normalized error categories.
    - path: src/shared/ipc.ts
      provides: LLM IPC channel constants and Pathfinder API signatures.
    - path: src/preload/index.ts
      provides: Secure invoke wrappers for LLM adapter operations.
  key_links:
    - from: src/preload/index.ts
      to: src/shared/ipc.ts
      via: ipcRenderer.invoke uses LLM channel constants.
      pattern: llm
---

<objective>
Define provider-neutral shared contracts and typed IPC/preload APIs for the Phase 10 LLM adapter layer.

Purpose: Establish compile-safe process boundaries before main-process provider adapters and secure credential services are implemented.
Output: LLM contract types plus renderer-safe channel wrappers for config, validation, and generation entrypoints.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-llm-adapter-layer/10-CONTEXT.md
@.planning/phases/10-llm-adapter-layer/10-RESEARCH.md
@.planning/phases/10-llm-adapter-layer/10-VALIDATION.md
@src/shared/browser.ts
@src/shared/ipc.ts
@src/preload/index.ts
@copilot-instructions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define provider-neutral LLM domain contracts</name>
  <read_first>
- src/shared/browser.ts
- .planning/phases/10-llm-adapter-layer/10-CONTEXT.md
- .planning/phases/10-llm-adapter-layer/10-RESEARCH.md
  </read_first>
  <files>src/shared/browser.ts</files>
  <behavior>
- Test 1: shared contracts include provider ids for `openai` and `ollama` and an extensible capability map.
- Test 2: generation request and response contracts are provider-neutral and non-streaming.
- Test 3: normalized failure categories exist and do not expose provider-native raw payloads.
  </behavior>
  <action>Add explicit LLM contracts to src/shared/browser.ts: `LLMProviderId` (`openai` | `ollama`), `LLMProviderCapability`, `LLMProviderConfig`, `LLMProviderConfigPatch`, `LLMAdapterConfigState`, `LLMGenerateRequest`, `LLMGenerateResult`, and `LLMGenerateError` with reason union `invalid-config | auth | network | timeout | quota | provider-error | unsupported-capability`. Include fields for provider-scoped model id, optional endpoint/baseUrl, timeoutMs, and retryable flag. Keep response shape non-streaming (single text output plus metadata) and add redaction-safe error message field.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/shared/browser.ts exports all LLM types named in the action section.
- `LLMProviderId` includes exactly `openai` and `ollama`.
- `LLMGenerateError.reason` includes `unsupported-capability` and `provider-error`.
- No LLM contract field stores raw secret values.
  </acceptance_criteria>
  <done>Shared contract layer can represent adapter config, validation, and generate operations for AI-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add typed LLM IPC channels and preload methods</name>
  <read_first>
- src/shared/ipc.ts
- src/preload/index.ts
- src/shared/browser.ts
  </read_first>
  <files>src/shared/ipc.ts, src/preload/index.ts</files>
  <behavior>
- Test 1: IPC constants include LLM config get/save, config validation, and generation channels.
- Test 2: Pathfinder API includes typed methods for each new channel.
- Test 3: preload wrappers invoke only explicit LLM channels and preserve existing API behavior.
  </behavior>
  <action>In src/shared/ipc.ts, add channel constants `llm:getConfig`, `llm:saveConfig`, `llm:validateConfig`, and `llm:generate`. Extend `PathfinderApi` with methods `llmGetConfig()`, `llmSaveConfig(patch)`, `llmValidateConfig(request)`, and `llmGenerate(request)` using the LLM shared types. In src/preload/index.ts, add matching `ipcRenderer.invoke` wrappers and keep all pre-existing browser/automation/home methods unchanged.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- `IPC_CHANNELS` contains the exact `llm:*` channel names listed in action.
- `PathfinderApi` declares all four `llm*` methods.
- src/preload/index.ts implements invoke wrappers for all four `llm*` methods.
- Existing non-LLM API wrappers still compile with no signature regressions.
  </acceptance_criteria>
  <done>Renderer can call LLM adapter operations through secure typed preload APIs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer to preload | Untrusted input may attempt malformed provider config or generation requests |
| preload to main IPC | Channel abuse could invoke privileged provider operations without validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | T | src/shared/browser.ts | mitigate | Enforce explicit request/response schemas and strict reason unions for typed validation |
| T-10-02 | I | src/shared/browser.ts | mitigate | Exclude secret-bearing fields from shared renderer-visible contracts |
| T-10-03 | S | src/shared/ipc.ts | mitigate | Restrict LLM operations to explicit channels; no generic execute channel |
| T-10-04 | E | src/preload/index.ts | mitigate | Expose minimal typed wrappers only and keep Node/Electron primitives hidden |
</threat_model>

<verification>
Run `npm run typecheck` after each task.
</verification>

<success_criteria>
1. LLM contract and IPC/preload surfaces compile and reflect Phase 10 decisions.
2. Shared contracts remain provider-neutral and safe for future providers without breaking signatures.
3. Renderer access remains typed and least-privilege.
</success_criteria>

<output>
After completion, create `.planning/phases/10-llm-adapter-layer/10-01-SUMMARY.md`.
</output>
