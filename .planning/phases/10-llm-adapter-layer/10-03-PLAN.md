---
phase: 10-llm-adapter-layer
plan: 03
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
files_modified:
  - src/main/main.ts
  - src/renderer/components/AutomationSidebar.tsx
  - src/renderer/App.tsx
  - src/renderer/lib/commandPalette.ts
autonomous: true
requirements:
  - AI-01
must_haves:
  truths:
    - Main process exposes typed IPC handlers for LLM config retrieval/update, config validation, and generation via the adapter service.
    - Users have a minimal in-app surface to configure cloud/local provider settings without enabling chat execution behavior.
    - Command-first workflows can access adapter configuration and validation actions with clear status feedback.
  artifacts:
    - path: src/main/main.ts
      provides: Wired IPC handlers bridging renderer requests to llmAdapterService and stores.
    - path: src/renderer/App.tsx
      provides: App-level state/actions for LLM config save/validate invocations and result feedback.
    - path: src/renderer/components/AutomationSidebar.tsx
      provides: AI section config/testing surface while keeping chat execution disabled.
    - path: src/renderer/lib/commandPalette.ts
      provides: Command entries for opening AI config section and validating adapter configuration.
  key_links:
    - from: src/renderer/App.tsx
      to: src/main/main.ts
      via: Pathfinder LLM IPC methods for config and validation.
      pattern: llm
    - from: src/renderer/lib/commandPalette.ts
      to: src/renderer/App.tsx
      via: command deps invoke AI settings open/validate handlers.
      pattern: ai
---

<objective>
Wire the Phase 10 LLM adapter into main IPC and provide a minimal command-first configuration/testing surface in renderer.

Purpose: Satisfy AI-01 user-configurability and secure endpoint validation without expanding into full AI chat or agent execution behavior.
Output: End-to-end adapter config and validation flow accessible from sidebar AI section and command palette.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-llm-adapter-layer/10-CONTEXT.md
@.planning/phases/10-llm-adapter-layer/10-RESEARCH.md
@.planning/phases/10-llm-adapter-layer/10-VALIDATION.md
@.planning/phases/10-llm-adapter-layer/10-01-PLAN.md
@.planning/phases/10-llm-adapter-layer/10-02-PLAN.md
@src/main/main.ts
@src/renderer/components/AutomationSidebar.tsx
@src/renderer/App.tsx
@src/renderer/lib/commandPalette.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register LLM adapter IPC handlers in main process</name>
  <read_first>
- src/main/main.ts
- src/shared/ipc.ts
- src/main/llm/llmAdapterService.ts
  </read_first>
  <files>src/main/main.ts</files>
  <behavior>
- Test 1: main process handles `llm:getConfig`, `llm:saveConfig`, `llm:validateConfig`, and `llm:generate` IPC routes.
- Test 2: handlers call the adapter service and return normalized typed responses.
- Test 3: handler errors are redacted and mapped to typed failure envelopes.
  </behavior>
  <action>In src/main/main.ts, initialize providerConfigStore, secretStore, and llmAdapterService during startup and register `ipcMain.handle` routes for all LLM channels defined in plan 10-01. `llm:saveConfig` should persist non-secret settings and update secret values through safeStorage-backed service. `llm:validateConfig` should run provider-specific health/config checks. `llm:generate` should invoke service generate path and return provider-neutral envelope. Keep handler errors redacted and deterministic.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/main/main.ts contains `ipcMain.handle` blocks for all four `llm:*` channels.
- llm handler path initializes and uses llmAdapterService instance.
- No main-process error response includes raw API key values.
- `npm run typecheck` passes after wiring.
  </acceptance_criteria>
  <done>Main process exposes a complete typed LLM adapter runtime surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add minimal AI section configuration and validation UI flow</name>
  <read_first>
- src/renderer/components/AutomationSidebar.tsx
- src/renderer/App.tsx
- src/preload/index.ts
  </read_first>
  <files>src/renderer/components/AutomationSidebar.tsx, src/renderer/App.tsx</files>
  <behavior>
- Test 1: AI sidebar section allows selecting provider/model/endpoint and saving config.
- Test 2: AI sidebar section includes explicit "Validate Connection" action with typed success/failure feedback.
- Test 3: AI chat execution remains disabled; section remains config/testing only.
  </behavior>
  <action>Replace the current static AI placeholder card in src/renderer/components/AutomationSidebar.tsx with a minimal config/testing panel: provider selector (`openai`/`ollama`), model input, endpoint input (for local provider), masked key/token input, save action, and validate action. In src/renderer/App.tsx, add state/actions that call `window.pathfinder.llmGetConfig`, `llmSaveConfig`, and `llmValidateConfig`, then show deterministic feedback messages. Keep generation/chat action buttons absent or disabled with explicit "not in phase 10" labeling.</action>
  <verify>
    <automated>npm run lint; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- AutomationSidebar AI section renders provider/model/endpoint/key inputs and Save/Validate actions.
- App.tsx calls all three LLM config APIs (`llmGetConfig`, `llmSaveConfig`, `llmValidateConfig`).
- AI section has no runnable chat generation action enabled.
- lint and typecheck pass after renderer updates.
  </acceptance_criteria>
  <done>Users can configure and validate one cloud and one local provider from in-app surface without enabling chat execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend command palette with AI configuration utilities</name>
  <read_first>
- src/renderer/lib/commandPalette.ts
- src/renderer/App.tsx
- .planning/phases/10-llm-adapter-layer/10-CONTEXT.md
  </read_first>
  <files>src/renderer/lib/commandPalette.ts, src/renderer/App.tsx</files>
  <behavior>
- Test 1: command palette includes explicit command to open/focus AI config section.
- Test 2: command palette includes explicit command to run adapter config validation.
- Test 3: command feedback matches typed adapter validation status and keeps command-first UX.
  </behavior>
  <action>In src/renderer/lib/commandPalette.ts, add command entries `ai.config.open` and `ai.config.validate` with clear descriptions and keywords. In App command dependencies, wire these commands to sidebar AI section activation and `llmValidateConfig` execution with user-visible result messaging. Maintain existing command ranking and error handling conventions; do not add command for full AI chat or generation in this phase.</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- commandPalette registry contains `ai.config.open` and `ai.config.validate` entries.
- command execution paths call App handlers that target AI config section and validation flow.
- No new command id triggers unrestricted AI generation/chat behavior.
- Full lint/typecheck/build passes.
  </acceptance_criteria>
  <done>Phase 10 closes with command-first provider configuration and validation flows for AI-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer config forms to main IPC | User-entered provider config may be malformed or malicious |
| command palette triggers to adapter runtime | Command shortcuts can invoke privileged validation/generation paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | T | src/main/main.ts | mitigate | Validate/sanitize all LLM config payloads before persistence and adapter use |
| T-10-11 | I | src/renderer/App.tsx | mitigate | Display redacted diagnostics only; never render secret values in status messages |
| T-10-12 | S | src/renderer/lib/commandPalette.ts | mitigate | Restrict new commands to config open/validate only; no hidden privileged operations |
| T-10-13 | D | src/main/main.ts | mitigate | Enforce timeout clamps and fail-fast validation to prevent hanging command flows |
| T-10-14 | E | src/renderer/components/AutomationSidebar.tsx | mitigate | Keep AI section non-executable beyond config/validate actions in this phase |
</threat_model>

<verification>
Run `npm run typecheck` after Task 1 and full lint/typecheck/build after Tasks 2-3.
</verification>

<success_criteria>
1. Users can configure and validate OpenAI/Ollama adapter settings through typed in-app flows.
2. Main-process IPC handlers enforce secure typed boundaries and redacted errors.
3. Phase 10 ships no out-of-scope AI chat execution behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/10-llm-adapter-layer/10-03-SUMMARY.md`.
</output>
