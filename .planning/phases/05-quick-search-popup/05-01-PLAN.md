---
phase: 05-quick-search-popup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/ipc.ts
  - src/preload/index.ts
  - src/shared/browser.ts
  - src/renderer/App.tsx
autonomous: true
requirements:
  - QSR-01
must_haves:
  truths:
    - Quick-search toggle can be requested by renderer through typed preload APIs.
    - Ctrl+Shift+S triggers quick-search toggle without breaking existing command palette shortcuts.
  artifacts:
    - path: src/shared/ipc.ts
      provides: Quick-search IPC channel constants and Pathfinder API method signatures.
    - path: src/preload/index.ts
      provides: Renderer-safe quick-search method bridge implementation.
    - path: src/renderer/App.tsx
      provides: App-scoped Ctrl+Shift+S shortcut integration with editable-target guard.
  key_links:
    - from: src/renderer/App.tsx
      to: src/preload/index.ts
      via: window.pathfinder.quickSearchToggle invocation in keydown handler.
      pattern: quickSearchToggle
---

<objective>
Define typed quick-search contracts and integrate app-scoped keyboard trigger behavior for QSR-01.

Purpose: Establish safe renderer-to-main invocation paths before popup window lifecycle work.
Output: IPC channels, preload bridge methods, and hotkey wiring in the renderer shell.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-quick-search-popup/05-CONTEXT.md
@.planning/phases/05-quick-search-popup/05-RESEARCH.md
@.planning/phases/05-quick-search-popup/05-VALIDATION.md
@src/shared/ipc.ts
@src/preload/index.ts
@src/renderer/App.tsx
@copilot-instructions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend quick-search IPC contract and preload bridge</name>
  <read_first>
- src/shared/ipc.ts
- src/preload/index.ts
- src/shared/browser.ts
- .planning/phases/05-quick-search-popup/05-CONTEXT.md
  </read_first>
  <files>src/shared/ipc.ts, src/preload/index.ts, src/shared/browser.ts</files>
  <behavior>
- Test 1: PathfinderApi exposes quick-search methods for toggle, open, close, and submit.
- Test 2: IPC channel constants exist for each quick-search operation with stable names.
- Test 3: Preload methods invoke only those typed channel constants.
  </behavior>
  <action>In `src/shared/ipc.ts`, add channels `quickSearch:toggle`, `quickSearch:open`, `quickSearch:close`, and `quickSearch:submit` to `IPC_CHANNELS`. Add PathfinderApi methods: `quickSearchToggle(): Promise<void>`, `quickSearchOpen(request?: QuickSearchOpenRequest): Promise<void>`, `quickSearchClose(): Promise<void>`, and `quickSearchSubmit(request: QuickSearchSubmitRequest): Promise<void>`. In `src/shared/browser.ts`, add `QuickSearchOpenRequest` with optional `query` and `QuickSearchSubmitRequest` with required `query` and `target`. In `src/preload/index.ts`, implement each method with `ipcRenderer.invoke(IPC_CHANNELS.<channel>, payload)` and preserve existing subscribe/contextBridge patterns.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/shared/ipc.ts contains `quickSearchToggle`, `quickSearchOpen`, `quickSearchClose`, and `quickSearchSubmit` keys in `IPC_CHANNELS`.
- src/shared/ipc.ts PathfinderApi interface contains the same four method signatures.
- src/shared/browser.ts exports `QuickSearchOpenRequest` and `QuickSearchSubmitRequest`.
- src/preload/index.ts contains `quickSearchToggle`, `quickSearchOpen`, `quickSearchClose`, and `quickSearchSubmit` implementations that call `ipcRenderer.invoke`.
  </acceptance_criteria>
  <done>Typed quick-search IPC contract is available to renderer and main with compile-safe payloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add app-scoped Ctrl+Shift+S quick-search trigger in renderer shell</name>
  <read_first>
- src/renderer/App.tsx
- src/renderer/components/CommandPalette.tsx
- .planning/phases/05-quick-search-popup/05-CONTEXT.md
  </read_first>
  <files>src/renderer/App.tsx</files>
  <behavior>
- Test 1: Ctrl+Shift+S invokes quick-search toggle when focus is not in editable fields.
- Test 2: Existing Ctrl+K and Ctrl+Shift+P command palette shortcuts still work unchanged.
- Test 3: Editable input/textarea/contentEditable targets do not trigger quick-search toggle.
  </behavior>
  <action>Update the existing `window.addEventListener('keydown', ...)` logic in `src/renderer/App.tsx` to detect quick-search shortcut with `event.ctrlKey && event.shiftKey && event.key.toLowerCase() === 's'`. Keep the current editable-target guard logic and existing command palette shortcut handling. On quick-search match, call `event.preventDefault()` and `void window.pathfinder.quickSearchToggle()`. Ensure shortcut routing remains app-window scoped and does not register system-global handlers.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/renderer/App.tsx keydown handler checks for `Ctrl+Shift+S`.
- src/renderer/App.tsx calls `window.pathfinder.quickSearchToggle()` in the quick-search branch.
- src/renderer/App.tsx still contains existing command palette shortcut checks for `Ctrl+Shift+P` and `Ctrl+K`.
- src/renderer/App.tsx retains editable-target guard for inputs/textareas/contentEditable elements.
  </acceptance_criteria>
  <done>Renderer can request quick-search popup toggle with locked shortcut semantics (D-01, D-02, D-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer keyboard event to preload API | Untrusted key events trigger privileged IPC actions |
| renderer quick-search API call to main IPC handler | Renderer requests can attempt invalid quick-search operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | S | src/renderer/App.tsx | mitigate | Keep app-scoped shortcut listener and editable-target guard to reduce spoofed trigger paths |
| T-05-02 | T | src/shared/ipc.ts | mitigate | Use explicit typed channel names and payload types to prevent ad-hoc message mutation |
| T-05-03 | E | src/preload/index.ts | mitigate | Expose only narrow quick-search methods through contextBridge; no raw ipcRenderer access |
</threat_model>

<verification>
Run `npm run typecheck` after each task to validate typed channel and App handler signatures.
</verification>

<success_criteria>
1. Quick-search operations are represented by explicit typed IPC channels and preload methods.
2. App-level Ctrl+Shift+S trigger is wired with required scope and conflict behavior.
3. Existing command palette shortcut behavior remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/05-quick-search-popup/05-01-SUMMARY.md`
</output>
