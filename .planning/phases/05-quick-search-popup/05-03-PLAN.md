---
phase: 05-quick-search-popup
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/renderer/main.tsx
  - src/renderer/components/QuickSearchPopup.tsx
  - src/renderer/lib/quickSearch.ts
  - src/renderer/styles/global.css
autonomous: true
requirements:
  - QSR-01
  - QSR-02
must_haves:
  truths:
    - Quick-search popup opens with focused query input and closes with Escape.
    - Popup supports keyboard-first result navigation (Arrow keys + Enter).
    - Enter selection routes destination into active main-browser tab and closes popup.
  artifacts:
    - path: src/renderer/components/QuickSearchPopup.tsx
      provides: Popup query/results UI and keyboard interactions.
    - path: src/renderer/lib/quickSearch.ts
      provides: Result-building helpers using shared omnibox resolution semantics.
    - path: src/renderer/main.tsx
      provides: Route switch to render quick-search popup shell under `#quick-search`.
  key_links:
    - from: src/renderer/components/QuickSearchPopup.tsx
      to: src/preload/index.ts
      via: quickSearch submit/close methods invoked through window.pathfinder API.
      pattern: quickSearchSubmit
    - from: src/renderer/lib/quickSearch.ts
      to: src/renderer/lib/omnibox.ts
      via: resolveOmniboxInput reuse for query-to-target behavior.
      pattern: resolveOmniboxInput
---

<objective>
Build popup renderer experience and keyboard-first selection flow for quick-search behavior and tab routing.

Purpose: Deliver interactive quick-search UX that fulfills QSR-01 and QSR-02 end-to-end.
Output: Popup UI component, result-generation utility, route entry wiring, and styles.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-quick-search-popup/05-CONTEXT.md
@.planning/phases/05-quick-search-popup/05-RESEARCH.md
@.planning/phases/05-quick-search-popup/05-VALIDATION.md
@src/renderer/main.tsx
@src/renderer/components/CommandPalette.tsx
@src/renderer/lib/omnibox.ts
@src/shared/browser.ts
@copilot-instructions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create quick-search result utility and popup component behavior</name>
  <read_first>
- src/renderer/components/CommandPalette.tsx
- src/renderer/lib/omnibox.ts
- src/shared/browser.ts
- .planning/phases/05-quick-search-popup/05-CONTEXT.md
  </read_first>
  <files>src/renderer/lib/quickSearch.ts, src/renderer/components/QuickSearchPopup.tsx</files>
  <behavior>
- Test 1: Result utility returns deterministic rows including target URL for submit.
- Test 2: Popup input autofocuses on mount and Escape closes without navigation.
- Test 3: ArrowUp/ArrowDown/Enter keyboard flow updates selection and submits selected result.
  </behavior>
  <action>Create `src/renderer/lib/quickSearch.ts` with typed `QuickSearchResult` records and helper `buildQuickSearchResults(query, searchTemplate)` that uses `resolveOmniboxInput` and returns concrete rows for URL-direct and web-search actions. Create `src/renderer/components/QuickSearchPopup.tsx` as a controlled UI with local query state, selected index state, autofocus input, Arrow key navigation, Enter submission, and Escape close. On submit, call `window.pathfinder.quickSearchSubmit({ query, target })`; on Escape or close button, call `window.pathfinder.quickSearchClose()`.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/renderer/lib/quickSearch.ts exports `QuickSearchResult` and `buildQuickSearchResults(`.
- src/renderer/lib/quickSearch.ts references `resolveOmniboxInput`.
- src/renderer/components/QuickSearchPopup.tsx includes keyboard handling for `ArrowDown`, `ArrowUp`, `Enter`, and `Escape`.
- src/renderer/components/QuickSearchPopup.tsx calls `window.pathfinder.quickSearchSubmit` and `window.pathfinder.quickSearchClose`.
  </acceptance_criteria>
  <done>Popup interaction model is keyboard-first and aligned with locked dismissal/selection decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire quick-search route entry and popup styling</name>
  <read_first>
- src/renderer/main.tsx
- src/renderer/styles/global.css
- src/renderer/components/QuickSearchPopup.tsx
- .planning/phases/05-quick-search-popup/05-CONTEXT.md
  </read_first>
  <files>src/renderer/main.tsx, src/renderer/styles/global.css</files>
  <behavior>
- Test 1: Renderer entry mounts quick-search popup component when URL hash is `#quick-search`.
- Test 2: Default app shell still mounts for non-popup routes.
- Test 3: Popup styling defines focused panel, list rows, and active selection states.
  </behavior>
  <action>Update `src/renderer/main.tsx` to branch render target by hash: render `<QuickSearchPopup />` when `window.location.hash === '#quick-search'`, otherwise render existing `<App />`. In `src/renderer/styles/global.css`, add `quick-search__*` classes for container, panel, input, result list, item, active item, helper text, and close action. Use existing token variables (`--pf-*`) and include clear focus-visible styles to preserve keyboard usability.</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- src/renderer/main.tsx imports and renders `QuickSearchPopup` when hash equals `#quick-search`.
- src/renderer/main.tsx still renders `App` for non-quick-search routes.
- src/renderer/styles/global.css contains `quick-search__container`, `quick-search__panel`, `quick-search__input`, and `quick-search__item--active` selectors.
- Full command `npm run lint; npm run typecheck; npm run build` passes.
  </acceptance_criteria>
  <done>Popup renderer shell is routable and styled, completing QSR-01/QSR-02 UX delivery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| quick-search user input to result/submit logic | Untrusted query text is transformed into navigation targets |
| popup renderer submit action to main navigation handler | Renderer sends target payload for tab navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | T | src/renderer/lib/quickSearch.ts | mitigate | Derive targets through existing resolver utility and typed result model; avoid arbitrary script URI generation |
| T-05-08 | I | src/renderer/components/QuickSearchPopup.tsx | accept | Popup only handles user-entered query and generated targets; no sensitive data shown in panel |
| T-05-09 | E | src/renderer/components/QuickSearchPopup.tsx | mitigate | Submit through narrow preload API (`quickSearchSubmit`) and avoid direct Electron or eval APIs |
</threat_model>

<verification>
Run `npm run typecheck` after Task 1 and full suite after Task 2.
</verification>

<success_criteria>
1. Popup UI supports deterministic keyboard-first search and selection interactions.
2. Escape close and reopen-reset semantics align with D-11 and D-12.
3. Selection handoff to main-process submit path completes active-tab routing flow.
</success_criteria>

<output>
After completion, create `.planning/phases/05-quick-search-popup/05-03-SUMMARY.md`
</output>
