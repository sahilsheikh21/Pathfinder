---
phase: 05-quick-search-popup
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/main/quickSearchWindow.ts
  - src/main/main.ts
  - src/main/browserRuntime.ts
autonomous: true
requirements:
  - QSR-01
  - QSR-02
must_haves:
  truths:
    - Quick-search popup uses a single reusable always-on-top BrowserWindow instance.
    - Popup can be opened, closed, and toggled from typed IPC handlers.
    - Selected quick-search targets route into an active main tab (creating one if absent).
  artifacts:
    - path: src/main/quickSearchWindow.ts
      provides: Popup window lifecycle manager with create/show/hide/toggle behavior.
    - path: src/main/main.ts
      provides: IPC handlers that connect quick-search renderer requests to popup/runtime actions.
    - path: src/main/browserRuntime.ts
      provides: Active-or-new-tab navigation helper used by quick-search selection flow.
  key_links:
    - from: src/main/main.ts
      to: src/main/quickSearchWindow.ts
      via: quickSearch IPC handlers call popup manager methods.
      pattern: quickSearch
    - from: src/main/main.ts
      to: src/main/browserRuntime.ts
      via: quickSearch submit handler invokes runtime navigation helper.
      pattern: navigate
---

<objective>
Implement main-process quick-search popup lifecycle and result-routing execution path for QSR-01/QSR-02.

Purpose: Deliver always-on-top reusable popup behavior and deterministic navigation handoff into browser runtime.
Output: Popup manager module, quick-search IPC handlers, and runtime navigation fallback helper.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-quick-search-popup/05-CONTEXT.md
@.planning/phases/05-quick-search-popup/05-RESEARCH.md
@.planning/phases/05-quick-search-popup/05-VALIDATION.md
@src/main/main.ts
@src/main/browserRuntime.ts
@src/shared/ipc.ts
@src/shared/browser.ts
@copilot-instructions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build reusable quick-search popup window manager</name>
  <read_first>
- src/main/main.ts
- src/main/browserRuntime.ts
- .planning/phases/05-quick-search-popup/05-CONTEXT.md
- .planning/phases/05-quick-search-popup/05-RESEARCH.md
  </read_first>
  <files>src/main/quickSearchWindow.ts</files>
  <behavior>
- Test 1: First open lazily creates BrowserWindow; subsequent opens reuse same instance.
- Test 2: Toggle operation shows window when hidden and hides when visible.
- Test 3: Window is always-on-top and remains resizable/movable by native chrome behavior.
  </behavior>
  <action>Create `src/main/quickSearchWindow.ts` exporting a manager factory (for example `createQuickSearchWindowManager`) that accepts app lifecycle dependencies and provides `open`, `close`, `toggle`, `isVisible`, and `destroy`. Configure BrowserWindow with concrete options: `width: 520`, `height: 420`, `show: false`, `alwaysOnTop: true`, `resizable: true`, `movable: true`, `frame: true`, and preload path `../preload/index.js`. Load renderer with `#quick-search` route (`${process.env.VITE_DEV_SERVER_URL}#quick-search` in dev, `index.html` plus hash in prod). Persist last bounds in memory during app session and reuse on reopen.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/main/quickSearchWindow.ts exists and exports open/close/toggle lifecycle methods.
- src/main/quickSearchWindow.ts contains BrowserWindow options with `alwaysOnTop: true`, `resizable: true`, and `movable: true`.
- src/main/quickSearchWindow.ts contains route loading logic that targets `#quick-search`.
- src/main/quickSearchWindow.ts contains visibility guards to avoid duplicate BrowserWindow creation.
  </acceptance_criteria>
  <done>Reusable popup manager satisfies D-04 through D-06 lifecycle requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire quick-search IPC handlers and runtime navigation fallback</name>
  <read_first>
- src/main/main.ts
- src/main/browserRuntime.ts
- src/main/quickSearchWindow.ts
- src/shared/ipc.ts
- src/shared/browser.ts
  </read_first>
  <files>src/main/main.ts, src/main/browserRuntime.ts</files>
  <behavior>
- Test 1: IPC handlers for toggle/open/close call popup manager methods without renderer Electron access.
- Test 2: Submit handler routes selected target to active tab when one exists.
- Test 3: Submit handler creates a new tab before navigation when no active tab exists.
  </behavior>
  <action>In `src/main/main.ts`, instantiate the popup manager in `createWindow()` and register handlers for `IPC_CHANNELS.quickSearchToggle`, `IPC_CHANNELS.quickSearchOpen`, `IPC_CHANNELS.quickSearchClose`, and `IPC_CHANNELS.quickSearchSubmit`. Add a `quickSearchSubmit` handler that validates `target` is non-empty, then calls a new `browserRuntime.navigateActiveOrCreate(target)` helper, and closes popup on successful routing. In `src/main/browserRuntime.ts`, add exported public method `navigateActiveOrCreate(target: string): BrowserTabState[]` that: (1) uses current active tab when available and calls existing navigate path, (2) creates a tab if no active tab exists, (3) returns updated snapshot list.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- src/main/main.ts contains ipcMain handlers for `quickSearchToggle`, `quickSearchOpen`, `quickSearchClose`, and `quickSearchSubmit`.
- src/main/main.ts quickSearch submit handler calls popup close logic after successful navigation.
- src/main/browserRuntime.ts exports public `navigateActiveOrCreate(` method.
- src/main/browserRuntime.ts `navigateActiveOrCreate(` includes fallback behavior that creates a tab when `activeTabId` is null.
  </acceptance_criteria>
  <done>Main process can control popup lifecycle and execute selection-to-tab routing required by QSR-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preload quick-search request to main IPC | Renderer-provided payload crosses into privileged BrowserWindow/runtime operations |
| quick-search submit target to browser runtime | Untrusted target string can trigger navigation side effects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | T | src/main/main.ts quickSearchSubmit handler | mitigate | Validate `target` as non-empty trimmed string and route through runtime helper only |
| T-05-05 | D | src/main/quickSearchWindow.ts | mitigate | Enforce single-window instance guards to prevent resource exhaustion from repeated toggles |
| T-05-06 | E | src/main/main.ts IPC registration | mitigate | Keep quick-search channels scoped to predefined handlers and avoid exposing generic eval-like IPC entry points |
</threat_model>

<verification>
Run `npm run typecheck` after each task and full suite at wave close.
</verification>

<success_criteria>
1. Quick-search popup window lifecycle is managed in main process with always-on-top + drag/resize behavior.
2. Typed IPC handlers support open/close/toggle + submit flows.
3. Selection routing navigates active tab or creates one deterministically when needed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-quick-search-popup/05-02-SUMMARY.md`
</output>
