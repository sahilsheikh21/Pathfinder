---
phase: 07-action-recording
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/main/actionRecorder.ts
  - src/main/browserRuntime.ts
  - src/main/main.ts
autonomous: true
requirements:
  - AUTO-02
must_haves:
  truths:
    - Main-process recorder enforces single active recording session bound to one tab.
    - Event normalization emits deterministic `seq`-ordered canonical steps.
    - Persisted draft workflow payload validates against the versioned contract before storage.
  artifacts:
    - path: src/main/actionRecorder.ts
      provides: Recorder lifecycle manager, normalization pipeline, and workflow draft persistence logic.
    - path: src/main/browserRuntime.ts
      provides: Runtime hooks needed for recorder tab-bound lifecycle and target-loss handling.
    - path: src/main/main.ts
      provides: Recorder manager wiring and typed IPC handler implementation.
  key_links:
    - from: src/main/main.ts
      to: src/main/actionRecorder.ts
      via: ipcMain handlers for recorder start/stop/status route to recorder manager.
      pattern: record
    - from: src/main/actionRecorder.ts
      to: src/main/browserRuntime.ts
      via: tab resolution and lifecycle invalidation integration.
      pattern: tab
---

<objective>
Implement the main-process action recorder manager with deterministic normalization and strict session ownership.

Purpose: Deliver the core recording runtime required by AUTO-02 while preserving secure ownership and deterministic workflow output.
Output: New recorder service module, runtime integration hooks, and recorder IPC handlers wired in main.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-action-recording/07-CONTEXT.md
@.planning/phases/07-action-recording/07-RESEARCH.md
@.planning/phases/07-action-recording/07-VALIDATION.md
@.planning/phases/06-cdp-integration/06-02-SUMMARY.md
@src/main/main.ts
@src/main/cdpBridge.ts
@src/main/browserRuntime.ts
@src/shared/browser.ts
@src/shared/ipc.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement recorder manager lifecycle and deterministic normalization</name>
  <read_first>
- src/main/cdpBridge.ts
- src/main/browserRuntime.ts
- src/shared/browser.ts
- .planning/phases/07-action-recording/07-CONTEXT.md
  </read_first>
  <files>src/main/actionRecorder.ts</files>
  <behavior>
- Test 1: starting a second recording while one is active returns busy status.
- Test 2: recorder session binds to resolved tab and stops when target is invalidated.
- Test 3: emitted steps are canonical actions only and include monotonic `seq` ordering.
- Test 4: type input coalescing produces stable final-value steps and secret placeholders for sensitive fields.
  </behavior>
  <action>Create `src/main/actionRecorder.ts` implementing recorder lifecycle per D-01 to D-11 and D-15: single-session lock, tab-bound ownership, fail-stop semantics on target loss, canonical normalization (`navigate`, `click`, `type`, `wait`), `type` coalescing, monotonic `seq` assignment, and versioned workflow draft building. Keep implementation substrate-focused; do not add playback execution behavior or library CRUD (deferred scope).</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- New recorder module exports start/stop/status and draft retrieval primitives.
- Session ownership is single-active and deterministic.
- Output step stream is canonical and `seq`-ordered.
- Sensitive typed fields are represented as variable placeholders in draft workflow data.
  </acceptance_criteria>
  <done>Recorder core behavior is implemented in main process with deterministic output semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire recorder manager into runtime and IPC handlers</name>
  <read_first>
- src/main/main.ts
- src/main/browserRuntime.ts
- src/main/actionRecorder.ts
- src/shared/ipc.ts
  </read_first>
  <files>src/main/browserRuntime.ts, src/main/main.ts</files>
  <behavior>
- Test 1: main IPC handlers route recorder start/stop/status requests to recorder manager.
- Test 2: runtime tab-lifecycle events can trigger recorder stop on bound-target invalidation.
- Test 3: malformed session requests return structured failure status rather than throwing.
  </behavior>
  <action>In `src/main/browserRuntime.ts`, expose any minimal tab-lifecycle hooks/events needed by recorder invalidation handling per D-02 and D-03. In `src/main/main.ts`, instantiate recorder manager, wire recorder IPC handlers added in Plan 01, and integrate shutdown cleanup with existing bridge lifecycle. Ensure start/stop/status responses stay typed and deterministic per D-04 and D-18.</action>
  <verify>
    <automated>npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <acceptance_criteria>
- `src/main/main.ts` registers recorder lifecycle handlers.
- Recorder lifecycle cleanup runs during app shutdown.
- Target-loss behavior transitions recorder state to stopped/disconnected with explicit reason payload.
- Build and lint pass after wiring changes.
  </acceptance_criteria>
  <done>Main-process recorder runtime is fully reachable via typed IPC and lifecycle-safe in app runtime.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorder manager to browser runtime | Recorder depends on tab identity/lifecycle crossing process module boundary |
| main IPC handlers to recorder service | Untrusted lifecycle requests can attempt session spoofing or state desync |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | D | src/main/actionRecorder.ts | mitigate | Enforce single-session lock and fail-fast busy rejection to prevent recorder contention loops |
| T-07-05 | T | src/main/actionRecorder.ts | mitigate | Normalize and validate steps before persistence, rejecting unsupported action shapes |
| T-07-06 | S | src/main/main.ts | mitigate | Validate session IDs and bound tab identity for stop/status operations |
| T-07-07 | I | src/main/actionRecorder.ts | mitigate | Replace sensitive typed values with secret placeholders before workflow draft persistence |
</threat_model>

<verification>
Run `npm run typecheck` after recorder core implementation, then `npm run lint; npm run typecheck; npm run build` after main/runtime wiring.
</verification>

<success_criteria>
1. Recorder service supports deterministic start/stop/status lifecycle with single-session ownership.
2. Canonical action normalization and ordered step output are enforced in main process.
3. Recorder IPC handlers are wired safely with lifecycle cleanup and typed error responses.
</success_criteria>

<output>
After completion, create `.planning/phases/07-action-recording/07-02-SUMMARY.md`
</output>
