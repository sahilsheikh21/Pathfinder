---
phase: 07-action-recording
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/browser.ts
  - src/shared/ipc.ts
  - src/preload/index.ts
autonomous: true
requirements:
  - AUTO-02
must_haves:
  truths:
    - Recorder contracts define canonical actions and deterministic ordering metadata before runtime implementation.
    - Renderer can start, stop, and inspect recorder status only through typed preload methods.
    - Workflow payload shape is versioned and explicitly models secret placeholders.
  artifacts:
    - path: src/shared/browser.ts
      provides: Typed workflow, recorder lifecycle, and step schema contracts for Phase 7.
    - path: src/shared/ipc.ts
      provides: Recorder IPC channels and Pathfinder API signatures.
    - path: src/preload/index.ts
      provides: Secure renderer-facing recorder lifecycle methods.
  key_links:
    - from: src/preload/index.ts
      to: src/shared/ipc.ts
      via: ipcRenderer.invoke calls constrained to recorder channels.
      pattern: recorder
---

<objective>
Define Phase 7 recorder contracts and typed IPC surface before main-process recording logic is implemented.

Purpose: Establish compile-safe boundaries that enforce canonical action schema and secure command-driven recorder control.
Output: Shared workflow/recorder types, recorder IPC channel constants, and preload bridge methods.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-action-recording/07-CONTEXT.md
@.planning/phases/07-action-recording/07-RESEARCH.md
@.planning/phases/07-action-recording/07-VALIDATION.md
@src/shared/browser.ts
@src/shared/ipc.ts
@src/preload/index.ts
@copilot-instructions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define versioned workflow and recorder contract types</name>
  <read_first>
- src/shared/browser.ts
- .planning/phases/07-action-recording/07-CONTEXT.md
- .planning/phases/07-action-recording/07-RESEARCH.md
  </read_first>
  <files>src/shared/browser.ts</files>
  <behavior>
- Test 1: shared contracts expose canonical action union limited to `navigate`, `click`, `type`, and `wait`.
- Test 2: workflow contract includes `version`, required metadata (`id`, `name`, `createdAt`, `updatedAt`), and ordered `steps`.
- Test 3: step contracts include `seq` and placeholder support for secret-type input values.
  </behavior>
  <action>In `src/shared/browser.ts`, add recorder contracts per D-05 to D-15: action union, step interfaces, workflow document interface (`version: 1`), recorder status/result payloads, and secret placeholder variable shape. Keep key names deterministic and scoped to Phase 7 recording (do not add playback policy fields from deferred scope).</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- `src/shared/browser.ts` exports workflow and recorder interfaces used by main, preload, and renderer layers.
- Action vocabulary is restricted to `navigate`, `click`, `type`, and `wait`.
- Step ordering field `seq` is required in step contracts.
- Secret placeholders are modeled without raw cleartext password persistence fields.
  </acceptance_criteria>
  <done>Phase 7 workflow and recorder contracts are defined and compile-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add typed recorder IPC channels and preload bridge methods</name>
  <read_first>
- src/shared/ipc.ts
- src/preload/index.ts
- src/shared/browser.ts
- .planning/phases/07-action-recording/07-CONTEXT.md
  </read_first>
  <files>src/shared/ipc.ts, src/preload/index.ts</files>
  <behavior>
- Test 1: Pathfinder API exposes recorder start/stop/status methods with typed request/response contracts.
- Test 2: new preload methods invoke only dedicated recorder channels.
- Test 3: existing API methods remain unchanged.
  </behavior>
  <action>Extend `src/shared/ipc.ts` with recorder channels (for example `automation:record:start`, `automation:record:stop`, `automation:record:status`) and corresponding Pathfinder API method signatures per D-01, D-04, and D-16. Implement matching methods in `src/preload/index.ts` using `ipcRenderer.invoke` and the new channel constants, preserving all existing methods.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
- Recorder channels are present in IPC constants and used by typed preload methods.
- Pathfinder API includes recorder lifecycle and status methods.
- Existing tab/navigation/download/quick-search/automation bridge APIs are untouched.
  </acceptance_criteria>
  <done>Recorder control surface is exposed through secure typed IPC boundaries only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer to preload recorder methods | Untrusted renderer input crosses into privileged recording controls |
| preload to main recorder channels | IPC payloads may attempt malformed action/session shapes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T | src/shared/browser.ts | mitigate | Use strict typed action unions and required workflow fields to prevent tampered step shapes |
| T-07-02 | E | src/preload/index.ts | mitigate | Expose minimal recorder lifecycle methods only; do not leak raw CDP/event internals |
| T-07-03 | S | src/shared/ipc.ts | mitigate | Use explicit recorder channel constants and typed request payloads for ownership/session assertions |
</threat_model>

<verification>
Run `npm run typecheck` after each task and ensure recorder contracts compile across shared, preload, and renderer imports.
</verification>

<success_criteria>
1. Shared workflow and recorder contracts fully encode Phase 7 canonical actions and deterministic ordering.
2. Recorder lifecycle commands are available only through typed preload APIs.
3. No existing IPC surface regressions are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/07-action-recording/07-01-SUMMARY.md`
</output>
